# Dead-route registry

The refuted approaches to the (□) box-threshold, each with its one-line reason
and the battery witness that killed it. All four forward to `route-incidence-direct`.

- `route-shellspine-bridge` — shell-j ≤ pivotShell RHS is false for 1≤j<r (→ `route-incidence-direct`, witness `battery/w-333-bridge.py`)
- `route-fullblock-deepfloor` — divergent exponents in [13.5, 14) at (6,6,6) (→ `route-incidence-direct`, witness `battery/w-666-u4.py`)
- `route-subset` — circular: bounds B(M) by B(M) itself (→ `route-incidence-direct`, no battery — logical circularity)
- `route-b-s3floor` — w type error + hpiv ceiling at (2,2,3) (→ `route-incidence-direct`, witness `battery/w-223-hpiv.py`)
