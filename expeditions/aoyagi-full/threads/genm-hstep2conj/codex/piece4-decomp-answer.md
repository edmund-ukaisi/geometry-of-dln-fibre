1. The conjugate `split.symm ∘ coreAbsorb.symm ∘ Ψ ∘ coreAbsorb ∘ split` is analytically sound. Known in-repo: `contDiff_deepestSplit`, `contDiff_deepestSplit_symm`, `deepestSplitCLE`, and strict-derivative lemmas for both split directions exist; `deepestPsiCoreShear_isLocalDiffeoAt` gives `ContDiff`, `dΨ(0)=id`, `Ψ 0=0`. Known in-repo: `contDiff_coreShearHomeo` / `_symm`, `contDiff_schurCutoffShift`, `hasStrictFDerivAt_schurCutoffShift_zero`, and `hasStrictFDerivAt_coreShearHomeo_symm_zero` cover `coreAbsorb.symm`. INFERRING from search: I did not see a named forward `hasStrictFDerivAt_coreShearHomeo_zero`; add the mirror lemma for `(reg, core + shift, spec)` or derive it before assembling. The derivative composite should reduce to `deepestSplitCLE.symm ∘ id ∘ id ∘ id ∘ deepestSplitCLE = id`, so pass `ContinuousLinearEquiv.refl` after a small `congr_fderiv`/`ext` simplification. Main Lean nuisance is not calculus, but getting the chain-rule derivative term to syntactically match the single CLE expected by `rlctAtOn_diffeo_bridge_of`.

2. I would not trust `Cq s := fromBlocks (1+X_s) Y_s Z_s T_s` as the Score chain unless a standalone readback lemma already says exactly that. Known in-repo warning from the L2 readback: the bare `1 + gaugeReadX` per-layer chain was numerically false for Score; the true L2 dictionary used actual layer pivots `deepBlkA_s + gaugeReadX_s`. Endpoint frames `P0, QL` are removable by `schur_frame_transform`; deepBlk constants are not endpoint frames and are not removed by that lemma. So K can be endpoint-frame-free, but it must be built from the same normalized/actual chain that piece 5 proves equals Score. If you keep bare `1+X`, piece 5 must contain a real normalization theorem identifying `blockSchur(partProd Cq L)` with the unframed actual product Schur; otherwise this is the hidden wall. INFERRING: the clean chain is likely “actual/conjugated layer chain without endpoint frames,” not the bare normalized chain, unless general-L has stronger normalization than L2.

3. The cutoff strategy is sound as a germ argument. Define `K := χ • Kcoup` with `χ = 1` on a neighborhood where all layer and partial-product pivots needed by recursion/readback are invertible; then locally `K = Kcoup`, so the recursion identity rewrites by `one_smul`. `hK0` is preserved if `Kcoup _ 0 = 0`; for the bare chain this is immediate from zero off-blocks, but for an actual/deepBlk chain it must be proved separately. No global identity off the germ is needed for `Φcore ∘ Psi =ᶠ Φscore`. Lean-level issue: do not try to prove global smoothness of `Kcoup`; prove `ContDiffAt` on `tsupport χ` and use the same bump-gluing pattern as `schurCutoffShift`. Also ensure the cutoff lives on the full `DeepestSplit`, since partial-product pivots may depend on core coordinates.

4. Wall risk #1: the Score readback dictionary. The cheap check is to state the exact theorem `blockSchur(partProd C L) = ScoreSchur x` for your chosen `C` before proving anything else; if the statement needs `deepBlkA/Y/Z`, do not build bare `Cq`. Wall risk #2: dependent indexing through `Fin L ↔ ℕ` in `partProd`, especially `m k`, `m (k+1)`, `s.castSucc`, `s.succ`, and the final output width. The cheap check is a type-only skeleton defining `Cnat : (k : ℕ) → Matrix (r ⊕ m k) (r ⊕ m (k+1)) ℝ` plus the final theorem statement at `partProd Cnat L`; if this does not elaborate cleanly, solve casts before analytic work. A secondary risk is `hKcd` for partial-product inverses, but that is standard once the chain and cutoff support are fixed.

5. Do not build piece 4 fully around the bare `Cq` yet. First co-design the chain definition with piece 5’s readback statement, because the coupling is loose with endpoint frames but tight with deepBlk constants. Once the chain is fixed, piece 4 can and should become standalone: `C`, cutoff support, `K`, `hKcd`, `hK0`, and local `K = Kcoup`. The standalone module should expose only the coupling API plus a local equality lemma on the `χ=1` neighborhood. This avoids spending 300 lines proving smoothness for the wrong chain.

BUILD ORDER: First, lock the chain/readback theorem statement and prove/typecheck `Kcoup C 0 = 0`.
Second, build the cutoff coupling module for that exact chain: `K`, `hKcd`, `hK0`, local `K = Kcoup`.
Third, assemble the conjugated flat diffeo and the germ equality, then feed `rlctAtOn_diffeo_bridge_of`.