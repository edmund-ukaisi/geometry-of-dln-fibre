# Threads — `fibration-geometry`

Index: status / type / one-line subject. `open` / `in-progress` / `blocked` / `review-pending` /
`closed` / `abandoned`. Per-thread notes in `threads/<NN>-<slug>/thread.md`.

| NN | type | subject | status |
|----|------|---------|--------|
| 01 | formalisation (tide) | S1 — over-field "≤" minor criterion + prime/κ(P) rank bridge → `rankROpen = {rank = r}` | in-progress |
| 02 | formalisation (tide) | S2 — smooth-block certificate (Ω/conormal free rank = codim at top-component generic pts) | in-progress |
