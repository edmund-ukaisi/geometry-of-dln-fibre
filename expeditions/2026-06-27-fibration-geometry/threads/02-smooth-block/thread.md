# Thread 02 — S2 smooth-block certificate (formalisation / tide)

**Target:** at every top-component generic point of the fibre, the conormal/Kähler module is **free of
rank = codim** (the standard-smooth Jacobian local model), on top of the banked unconditional generic
smoothness `isSmoothAt_sweepFibre_topComponent`. RLCT-runway slab 1 (the local upper-bound model). Levers:
`IsSmoothAt.exists_notMem_isStandardSmooth`, `StandardSmoothCotangent.free_cotangent`/`basisCotangent`.

**Status:** in-progress (spawned tick 0). Independent of S1.

## Notes / read
(tide flushes progress here)
