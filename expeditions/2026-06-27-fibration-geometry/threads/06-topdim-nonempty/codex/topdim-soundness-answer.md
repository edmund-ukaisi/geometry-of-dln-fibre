Q1: SOUND. Coheight is antitone: if `q ≤ x`, then `V(x) ⊆ V(q)`, so every chain above `x` is a chain above `q`. This is pure order theory on `Spec A`, with no going-down or extension hypothesis.

Q2: SOUND. It is enough to choose some minimal prime `q ≤ x`; the chain need not start at a minimal prime. The inequality `coheight x ≤ coheight q` comes from inclusion of upper intervals, and `WithBot ℕ∞` causes no boundary issue.

Q3: SOUND. The quotient inequality gives `ringKrullDim (A ⧸ p) ≤ ringKrullDim A`. Conversely, since every `coheight x` is bounded by the maximal minimal-prime quotient dimension, the supremum formula `ringKrullDim A = ⨆ x, coheight x` gives the reverse inequality.

Q4: SOUND. No finite-dimensionality hypothesis is needed. The complete lattice/order structure of `WithBot ℕ∞` supports the same supremum and antisymmetry argument even when the dimension is `⊤`.

OVERALL: SOUND