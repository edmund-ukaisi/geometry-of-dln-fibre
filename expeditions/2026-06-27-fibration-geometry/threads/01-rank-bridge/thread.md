# Thread 01 — S1 rank-bridge keystone (formalisation / tide)

**Target:** the scheme-level identity `rankROpen = {rank = r}` for `Spec(sweepSigmaRing k d r)`, via the
prime/residue-field rank bridge `P ∈ rankROpen ↔ rank over κ(P) (universal matrix) = r`. New lever: the
over-field "≤" minor criterion (all `(r+1)`-minors vanish ⟹ `rank ≤ r` over a field), dual to
`RankMinorCover.exists_invertible_minor_of_rank`.

**Status:** in-progress (spawned tick 0).

## Notes / read
(tide flushes progress here)
