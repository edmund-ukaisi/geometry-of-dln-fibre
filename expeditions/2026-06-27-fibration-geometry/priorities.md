# Priorities — `fibration-geometry` (taste ledger)

Controller proposes a ranking by value-of-information × directed suspicion; **operator edits this file
directly** (highest-authority signal). Nothing unranked; "unclear-but-keep-going" is first-class.

## Active (this expedition's spine)

| # | item | status | VOI / note |
|---|------|--------|-----------|
| P1 | **S1 rank-bridge keystone** — `mem_rankROpen_iff_rank_universalMatrixResidue_eq` | **LANDED** · bedrock ✓ (hardener PASS-WITH-NOTES) | done — green/axiom-clean; "≤" reused banked `rank_le_iff_forall_submatrix_det_eq_zero`. Set-of-primes identity; gates S3/S4. |
| P2 | **S2 smooth-block certificate** — `fibre_smoothBlock_certificate` (Kähler Ω free, `rank+codim=ambient`) | **LANDED** · bedrock ✓ (hardener PASS-WITH-NOTES) | done — green/axiom-clean; caught + corrected the Kähler/conormal conflation. RLCT-runway slab 1. |
| P3 | **S3 flatness payoff** — `Flat π` over `rankROpen` + `UniversallyOpen.of_flat` + `rankAtStalk` loc. const. | pursue (tide live) | high — load-bearing global consequence; Mathlib coverage rich. **Test "cheap flatness?" inside build.** Unblocked by S1. |
| P4 | **S4 honest per-chart local product** — `RankROpenPerPivotLocalProduct` + `reducedFibre_existsProductChartAt_rankEq` | **LANDED** · fidelity PASS (hardener-pending) | done — green/axiom-clean; S1 folded in (rankROpen genuinely = rank-=r locus); honest name (NOT `locallyTrivial`: k-algebra-only + uncocycled). |
| P4b | **S4b over-base trivialization** (load-bearing completion of S4) — `O(U)`-algebra iso respecting the projection (base map `SchurLoc → Total`) | queued (decide after S3) | high — the genuine local-triviality content; S3 (flatness) engages the base structure, so sequence after it. |
| P5 | **S5 reusable scheme-bundle API** — `IsLocallyTrivialProduct` predicate + cocycle-engine spin-out | queued (after S3/S4) | medium-high — net-new; packages the spine; reusable. |
| P2c | **S2c — close S2's `TopDimMinPrimes`-nonempty residual** (hardener Just-Do-It) → `exists_smoothBlock_certificate` becomes a CLOSED existence theorem (drops `I, hI`) | pursue (tide live) | high — strengthens S2 to a hypothesis-free existence; within reach on banked `fibre_normalForm_nonempty` + a top-dim-min-prime-exists lemma. |
| P2b | **S2b conormal companion** — `I/I²` free of rank `= codim` (the RLCT-relevant dual of S2's Kähler side) | parked (roadmap) | medium — hardener confirms REAL-BUILD (banked engine drops the ambient ideal); conormal exact sequence + rank additivity. |

## Parked — roadmap (not this expedition unless a spine rung forces it)

| # | item | state |
|---|------|-------|
| R1 | full overlap-trivialization cocycle (`targetOverlapTransition`) | roadmap |
| R2 | singular-locus = deepest-stratum split (full anatomy; RLCT lower-bound start) | roadmap |
| R3 | `e` rung-2 `LocalizedChartDescent` | roadmap |
| R4 | `Mat^{=r}` target rebase | roadmap (redundant unless goal changes) |
| R5 | RLCT analytic bridge — replace `cited_aoyagi_dln` | next expedition |

## Open ambiguities (unclear-but-keep-going)

- **S3 flatness route:** atlas-routed vs cheap (miracle/generic) — resolve empirically in the tide; the
  atlas-routed statement is the structural deliverable regardless.
- **S4 vs R1:** whether the source-side product local-triviality over `rankROpen` is satisfying without
  the full overlap cocycle (R1). Decide at S4 integration; do not pre-pull R1.
