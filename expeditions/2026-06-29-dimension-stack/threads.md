# Threads — `dimension-stack`

Index: NN / type / subject / status. Per-thread notes in `threads/<NN>-<slug>/thread.md`.

| NN | type | subject | status |
|----|------|---------|--------|
| 00 | scout (recon) | AG-foundations library ladder — what to grow to Mathlib-grade | **closed** (2026-06-28) — ladder in [`priorities.md`](priorities.md) |
| 01 | lean-formaliser (rung 0) | de-risk: throwaway generalisation of `NullstellensatzCodim` → exact minimal hyps for the field-general core | **closed** (2026-06-29) — CONFIRMED: core is `[Field k] [Finite σ]`, clean 3-band split; consumers' `[IsAlgClosed]` dead weight; verdict in [`priorities.md`](priorities.md) R0 outcome |
| 02 | lean-formaliser (R1) | extract integral-extension dimension invariance → `Core.Dimension.Integral` | **closed** (`f447c0ce`) — 9 decls re-homed (byte-identical statements) to `Core.Dimension.{Integral,Basic}`; 8 consumers re-pointed; green 3819 / sorry-0 / axiom-clean; reviewer + Codex PASS. Card: `threads/02-integral-dim/statement-card.md` |
| 03 | lean-formaliser (R2) | polynomial-ring catenary + monic positioning → `Core.Dimension.Catenary` | **closed** (`574a9056`) — `height p + dim(R/p) = n` (`@[stacks 00OS]`) + monic-positioning crux (private Mathlib substitution re-derived in-repo, Codex xhigh PASS); 7+aggregator consumers re-pointed (4 transitive, L2); green 3819 / sorry-0 / axiom-clean. Card: `threads/03-catenary/statement-card.md`. **Re-gate PASS (3819) + crux-review PASS (04).** |
| 04 | reviewer (crux audit) | R2 fidelity: verbatim private-substitution copy + "any field" generality | **closed** — **PASS-with-notes**: verbatim copy byte-faithful (only `T` de-privatised); any-field generality sound (zero closure/char/cardinality hyps); minor non-blocking (card tag-claim + 2 dangling `§` refs → folded into R3). Verdict: `threads/03-catenary/review.md` |
| 05 | lean-formaliser (R3) | finite-type-domain catenary (L4d) + closed-point corollaries → `Core.Dimension` | **closed** (`f3670561`) — new `Core/Dimension/AffineDomain.lean` (any-field dimension formula `@[stacks 00OS]` + closed-point corollaries + integral-height-transport brick `height_under_eq_of_isIntegral`); reuses R2 catenary as black box; both R2-rider cosmetics fixed; module green standalone. Card: `threads/05-affine-domain-catenary/statement-card.md` |
| 05r | reviewer (peer session) | R3 fidelity audit | **closed** — **PASS-with-notes** (6/6 checks pass, no hole): minor wording (00OS headline = restatement not verbatim; Codex notes 00P2 closer) + 5 longLines → both folded into R4. (Peer session — verdict relayed via teammate-message, not in this team's roster.) |
| 06 | lean-formaliser (R4) | codim bridge — extract field-general core (R0 split) → `Core.Dimension.Codimension`; bundled R3+R4 re-gate | **closed** (`2a610f8f`) — field-general core (8 decls) at `[Field k][Finite σ]`, no closure; `[IsAlgClosed]` layer + DLN consumers stay in `NullstellensatzCodim` (re-`export`); `vanishingIdeal_univ_eq_bot` weakened to `[Infinite k]`; reviewer PASS 6/6 + Codex CLEAN; controller re-gate green 3820. Card: `threads/06-codim-bridge/statement-card.md`. **ENTRY-1 COMPLETE.** |
| 07 | lean-formaliser (E1) | entry-2: étale local-dimension bridge + `height_eq_under_of_etale` → `Core.Dimension.Smooth` | **closed** (`614b8838`) — `Ideal.height_eq_under_of_etale` + `ringKrullDim_localizationAtPrime_eq_of_isSmoothAt` (non-circular, via étale-over-affine-space); verbatim re-home, no closure hyp; L2 sweep (FibreSmoothBlock bite); re-gate green 3819. Card: `threads/07-etale-localdim/statement-card.md` |
| 08 | lean-formaliser (E2) | entry-2 capstone: smooth point ⟹ regular local ring, `[IsAlgClosed]→[PerfectField]` → `Core.Dimension` | **closed** (`b9affa4a`) — `smooth_point_isRegularLocalRing [PerfectField k]` (`@[stacks 00TV]`) + cotangent-finrank companion → new `Core/Dimension/Regular.lean`; `[PerfectField]` already banked upstream (verify + re-home + drop vestigial closure import); non-circular via E1; re-gate green 3819. Card: `threads/08-smooth-regular/statement-card.md`. **ENTRY-2 content-complete; capstone audit (08r) pending.** |
| 08r | reviewer (capstone audit) | E2: `[PerfectField]` soundness + `@[stacks 00TV]` iff-tag + re-home fidelity | **closed** — **PASS 3/3** (no hole, no statement drift, no silent `[IsAlgClosed]` re-pull; Codex xhigh concurred on `[PerfectField]`; honest forward-only tag). Verdict: `threads/08-smooth-regular/review.md` |
| 09 | lean-formaliser (RF) | retrofit: drop dead-weight `[IsAlgClosed]` on `codimRep_*`; fix `DeepChartRing:138` prose + all aggregator/Dimension longLines | **in flight** |

## Roadmap (future expeditions — not this one)
orbit-geometry capstone de-`Tuple` (recon entry 3); type-A Gabriel/`Ext` (4); q-series (5); mathlib4 upstream
PR; shared-package extraction.
