# priorities.md — `rlct-payoff` taste ledger

Ranked by value-of-information (how much does the next action change across resolutions?) × directed
suspicion. Controller proposes; operator edits directly (highest-authority signal).

## P0 — pursue NOW (highest VOI; gates the whole ladder)
- **The sizing recon.** Two decorrelated probes, before any build:
  - **Mathlib coverage** (scout): `irreducibleComponents` / `TopologicalSpace.IrreducibleComponents`,
    variety-of-a-union, Krull-dim of components; and — the swing factor — **any** SLT / real-log-canonical-threshold /
    zeta-function / `Watanabe` scaffolding (almost certainly absent → determines whether Phase R is
    cite-and-assemble or a sub-library).
  - **Math sizing** (pen-and-paper): the `Σ^r = ⋃ Ō_M` stratification (precise corner-rank constraint,
    which Kostant partitions index the components); components = maximal-orbits argument; the `(C/2, θ)`
    RLCT assembly statement + exactly what is Cited vs proved.
  - **Output:** a sized ladder + the verdict on Phase R feasibility → re-scope `brief.md`.

## P1 — pursue after sizing (the new geometric content; clearly in-scope)
- **Phase G + θ** (`Σ^r`-as-variety + stratification + components + `θ = numTop`). The genuinely-new
  geometry; consumes the LANDED Voigt codim. Suspicion: G2 (`Σ^r = ⋃ Ō_M`) is the hard structural piece;
  G3 (components = maximal orbits) hinges on Mathlib's `irreducibleComponents` coverage (P0 resolves).

## P2 — pursue / scope after Phase R sizing
- **Phase D + R** (DLN application + the rlct payoff). Feasibility of R gated by P0's SLT-machinery
  verdict. If R is a multi-month analytic sub-library → SURFACE (grind vs Cited-rlct-interface decision).

## unclear-but-keep-going (parked, not collapsed)
- Whether the rlct *definition* is buildable zero-cited or must be a Cited/interfaced object (the
  `≤ ½codim` bound is already Cited; the definition itself is the open question). Parked until P0.
- Whether `Σ^r` (corner-rank ≤ r) needs `r > 0` general or whether `r = 0` (zero-product locus) is the
  load-bearing case for the paper's headline. Parked; pen-and-paper to confirm from the paper.
