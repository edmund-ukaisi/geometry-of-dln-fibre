# Statement Card - A2 Case 2 source-chart CenterCoord alignment

## Claim

Aoyagi's displayed Case 2 source-chart map for the old center
`case2ResidualBlockPivotEntries n S J` is definitionally the same finite map
as `SelectedEntrySignedBox.CenterCoord.chartMap` specialized to the displayed
pivot `(J+1,J+1)`.

## Lean Name

```text
case2DisplayedSourceChartMap_eq_selectedEntrySignedBoxCenterCoord_chartMap_apply
```

## Boundaries

Definitional vocabulary alignment only.  No post-pivot product readout,
successor source-chart readout, source production, chart coverage, measure
transport, normal crossings, pole order, or RLCT is proved.
