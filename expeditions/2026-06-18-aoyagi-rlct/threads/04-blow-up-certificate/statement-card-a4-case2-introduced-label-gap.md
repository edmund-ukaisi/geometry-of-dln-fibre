# Statement card - A4 Case 2 introduced-label gap

## Lean artifacts

File: `lean/DLNFibre/DLN/Aoyagi/BlowupArithmetic.lean` @
`ed54c99d21ec99202f5a440ca251a1a26b0057ea`.

Names:

- `DLNFibre.DLN.Aoyagi.actualWidthLabelFinset`
- `DLNFibre.DLN.Aoyagi.mem_actualWidthLabelFinset`
- `DLNFibre.DLN.Aoyagi.introducedLabelFinset`
- `DLNFibre.DLN.Aoyagi.mem_introducedLabelFinset`
- `DLNFibre.DLN.Aoyagi.levelProductStep_introducedLabelFinset_eq_one_of_gap`
- `DLNFibre.DLN.Aoyagi.case2ResidualRow_introducedLabel_monomialRec_eq_pivot_of_gap`
- `DLNFibre.DLN.Aoyagi.exists_case2DisplayedQP_mul_sourceSubstitution_of_introducedLabelGap`

## Statement

Lean now instantiates the finite label-product Case 2 gap bridge with the
finite domain of introduced labels at state `(S,J)`. The new finite set is the
actual-width label domain filtered by the existing predicate
`introducedLabel L n S J s k`.

If every introduced label avoids the Case 2 gap interval
`J+1 <= level(s,k) < mu_S`, then the recurrence factors over that interval are
`1`; hence displayed residual row weights are flat and the displayed
source-substitution `Q/P` theorem applies.

## Source role

Aoyagi's recurrence product ranges over active exceptional variables at the
current state: all earlier layers and the current layer through `J`. This
checkpoint makes that finite active-label domain explicit in Lean.

## Proved

- Actual-width labels have a finite sigma-domain enumeration.
- Introduced labels have a finite sigma-domain enumeration.
- Membership in these finite domains is equivalent to the existing
  `actualWidthLabel` and `introducedLabel` predicates.
- A Case 2 gap over all introduced labels makes the finite introduced-label
  recurrence factors equal to `1` on `J+1..mu_S-1`.
- The displayed Case 2 row-weight and source-substitution `Q/P` wrappers apply
  to the recurrence generated by this introduced-label finite product.

## Assumed

- The Nat-valued `level s k` is the source recurrence level `tilde_t_(s,k)`.
- The variable map `var s k` is the source recurrence variable `u_(s,k)`.
- The introduced-label Case 2 gap holds.
- Row weights are already represented by the monomial recurrence using this
  finite product.
- Residual rows are `J+1..mu_S`, residual columns are actual-width
  `J+1..n_(S+1)`, and the selected variable is counted once in `u*b_i`.

## Not proved

- No proof that the recursive invariant supplies the level/variable maps.
- No proof that the recursive invariant establishes the introduced-label gap.
- No proof of the source's Case 2 comparability sentence; the label gap alone is
  not enough.
- No repair of the printed Case 2 vector mismatch.
- No arbitrary selected-entry pivot chart, chart coverage, coordinate
  regularity/Jacobian, exponent update, transition invariant, termination proof,
  normal-crossing certificate, or RLCT extraction.

## Reproduction and review

- Reproduction artifact:
  `reproduction-case2-introduced-label-gap-a4.md`.
- Reproduction check:
  `review-case2-introduced-label-gap-a4.md`.

## Verification

- `lake env lean DLNFibre/DLN/Aoyagi/BlowupArithmetic.lean`: passed.
- `lake build DLNFibre`: passed.
- `./scripts/sorries`: `0 sorry`, `0 #exit`, `0 native_decide`, `0 axiom`.
- `git diff --check`: passed.
