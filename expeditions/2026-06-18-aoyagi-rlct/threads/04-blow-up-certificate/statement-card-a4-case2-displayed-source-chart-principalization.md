# Statement card - A4 Case 2 displayed source-chart principalization

## Lean Artifacts

File: `lean/DLNFibre/DLN/Aoyagi/BlowupArithmetic.lean`.

Names:

- `DLNFibre.DLN.Aoyagi.case2DisplayedSourceChartMap_value_mem`
- `DLNFibre.DLN.Aoyagi.case2DisplayedSourceChartMap_center_dvd`
- `DLNFibre.DLN.Aoyagi.case2DisplayedSourceChartMap_centerIdeal_eq_span_singleton`
- `DLNFibre.DLN.Aoyagi.Case2DisplayedSuppliedChartFamilyBoundary.displayedPivot_sourceChartMap_value_mem`
- `DLNFibre.DLN.Aoyagi.Case2DisplayedSuppliedChartFamilyBoundary.displayedPivot_sourceChartMap_center_dvd`
- `DLNFibre.DLN.Aoyagi.Case2DisplayedSuppliedChartFamilyBoundary.displayedPivot_sourceChartMap_centerIdeal_eq_span_singleton`

## Statement

Lean now exposes finite-center principalization in the names of the displayed
Case 2 source-coordinate chart map. The source map sends the displayed pivot
`(J+1,J+1)` to `u` and every other residual-block center coordinate to
`u` times a residual coordinate. Under displayed continuation, the transformed
finite residual-block center values generate exactly `Ideal.span {u}`.

The displayed supplied boundary package exports the same value, divisibility,
and finite ideal-principalization projections.

## Source Role

This records the elementary center part of Aoyagi's displayed Case 2 blow-up
chart on PDF pp. 19-21 in source-coordinate names. It depends only on displayed
pivot membership in the residual-block center and the selected-entry
substitution pattern.

## Proved

- The displayed source-coordinate chart map has `u` as a transformed finite
  center value.
- Every transformed residual-block center value is divisible by `u`.
- The ideal generated by transformed finite center values is `Ideal.span {u}`.
- The displayed supplied boundary exposes these facts using its continuation
  hypotheses.

## Assumed

- Displayed continuation `J+1 <= prefixMinNat n (S+1)` and `1 <= S`.
- A displayed supplied boundary when using the boundary projections.

## Not Proved

- No arbitrary-pivot source chart.
- No affine chart production or atlas coverage.
- No coordinate regularity theorem.
- No Jacobian or volume-form exponent calculation.
- No normal crossings, RLCT extraction, termination, or transition invariant.
- No repair of the printed Case 2 vector mismatch.

## Reproduction and Review

- Reproduction artifact:
  `reproduction-case2-displayed-source-chart-principalization-a4.md`.
- Review artifact:
  `review-case2-displayed-source-chart-principalization-a4.md`.

## Verification

- `lake env lean DLNFibre/DLN/Aoyagi/BlowupArithmetic.lean`
