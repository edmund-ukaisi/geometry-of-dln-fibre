# A4 Case 2 Source Terminal Product Candidate

Status: reproduced the one-based source-row packaging of the stopped displayed
Case 2 terminal product candidate.  This checkpoint reindexes already supplied
terminal data; it does not prove chart production.

## Source Anchor

On PDF pp. 21-22, after the stopped Case 2 branch, Aoyagi writes the terminal
display in source order:

```text
diag(b_1,...,b_J,b'_(J+1),...) C'^(S+1) prod_{s=S+2}^L C^(s).
```

The visible row order of the displayed `C'^(S+1)` begins with the old source
rows `1,...,J`, followed by the surviving transformed pivot row `J+1`.

## Pen-And-Paper Reproduction

Let

```text
I_old = {1,...,J},
I_term = {1,...,J+1}.
```

The previous stopped terminal candidate used the stacked row type

```text
I_old ⊕ {*},
```

where the `Unit` row is the surviving pivot row.  The source row order is the
equivalent one-based type `I_term`, with

```text
inl(i) ↦ i,    inr(*) ↦ J+1.
```

Thus the source-row terminal next matrix is the row reindexing of

```text
[ C(1,-); ...; C(J,-); C0(-) ],
```

where `C0` is the already named surviving transformed top row.  Similarly, the
source-row terminal product candidate is the row reindexing of the previously
defined stacked product candidate

```text
(blockdiag(diag(b_1,...,b_J), [b'_(J+1)]) * [Cold; C0]) * F.
```

Since row reindexing is by an equivalence, it preserves the ideal generated by
matrix entries.  Therefore the stopped source old-top theorem can be restated
with a one-based source-row terminal product candidate on the right hand side.

## Lean Shape

The generic finite-algebra lemma is:

```text
matrixEntryIdeal_submatrix_equiv
```

The source row equivalence is:

```text
case2SourceTerminalRowEquiv J :
  case2SourceOldTopRowIndex J ⊕ Unit ≃ case2SourceTerminalRowIndex J.
```

The source terminal next matrix and product candidate are:

```text
case2DisplayedSourceTerminalCprimeCandidate
case2DisplayedSourceTerminalProductReindexedCandidate
```

The final wrapper is:

```text
Case2DisplayedSuppliedChartFamilyBoundary.
  exists_sourceOldTopSourceSuffix_entryIdeal_eq_sourceTerminalProduct_of_not_next_cont
```

## Boundaries

- This is a row-reindexing presentation of the existing stopped terminal
  candidate.
- It does not prove the chart produces the source coordinates.
- It does not prove that Aoyagi's full `C'^(S+1)` has been constructed from
  all source rows and transformations.
- It does not prove chart coverage, coordinate regularity, Jacobian
  arithmetic, normal crossings, RLCT extraction, termination, transition
  invariance, automatic Case 2 gap/tail transport, or printed-vector repair.

## Kill Conditions

- Do not treat the source-row terminal candidate as chart-produced.
- Do not infer full `C'^(S+1)` construction beyond the old rows plus surviving
  displayed pivot row that are explicitly packaged here.
