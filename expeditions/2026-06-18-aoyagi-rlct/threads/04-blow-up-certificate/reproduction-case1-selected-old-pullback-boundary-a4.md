# A4 Case 1(2) Selected-Old Pullback Boundary

Status: reproduced the supplied boundary that combines the selected-old
pullback with the displayed row-strip local handoff.

## Source Situation

Aoyagi Case 1 first blows up the center generated by the chosen old
exceptional variable and the row-strip entries:

```text
u_(s,k) = 0,
d_ij = 0,    J+1 <= i <= J+J1,    J+1 <= j <= M^(S+1).
```

In Case 1(2), the displayed chart divides the row-strip block by the new
selected variable and rewrites the old exceptional variable as

```text
u_(s,k) = u_(S,J+1) * u'_(s,k).
```

The existing Lean center uses

```text
Case1CenterGenerator = Unit + (Nat x Nat).
```

The `Unit` branch is only the old-generator token. It does not itself remember
the source label `(s,k)`, the introduced-label proof, the level `J+J1`, or the
source pullback recurrence.

## Pen-And-Paper Boundary

The useful source-facing boundary is therefore:

```text
selected old pullback data
+ displayed row-strip local handoff data specialized to factoredBase.level.
```

The selected-old pullback data supplies that `source` is already the recurrence
after `old = u * old'`, while `factoredBase` uses the residual variable `old'`.
The displayed row-strip handoff supplies first-jump data, post recurrence data,
and exponent post-data.

Specializing the local handoff to `factoredBase.level` removes a separate
level-identification hypothesis. The combined package proves only projections:

```text
source.step = mulStepAt factoredBase.step u (J+J1),
(Sum.inl () : Case1CenterGenerator) is in the finite Case 1 center,
case1RowStripOldWeight ... factoredBase.weight = source.weight ...,
the displayed source-order identity with source.weight on the left,
the exponent-domain extension to (S,J+1).
```

No chart is constructed. The relationship between the `Unit` center token and
the source label `(s0,k0)` is supplied by the package fields rather than
derived from the token.

## Lean Boundary

Lean now proves

```text
Case1DisplayedRowStripSelectedOldPullbackBoundary
```

and projection theorems:

```text
factoredBaseFirstJump
selectedOld_mem_center
sourcePullback_selectedIntroduced
selectedLevel
source_step_eq_mulStepAt
residualRowStripOldWeight_eq_sourceWeight
sourceOrder_identity
extendExponentDomain
```

## Caveats

- This is an assumption-interface package.
- This does not construct the selected-old chart.
- This does not prove that the `Unit` generator determines the source label
  `(s0,k0)`.
- This does not construct the raw-coordinate-to-`source` pullback.
- This does not prove chart coverage, chart regularity, transition regularity,
  Jacobian accounting, normal crossings, or RLCT extraction.
