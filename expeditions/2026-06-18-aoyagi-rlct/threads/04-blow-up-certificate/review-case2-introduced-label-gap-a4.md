# A4 Case 2 Introduced-Label Gap Review

Date: 2026-06-19.

Scope: independent xhigh review of the finite introduced-label domain bridge for
the displayed Case 2 row-weight recurrence. This review covers source fidelity,
pen-and-paper scope, and Lean/API shape.

## Reviewers

- Source/pen-and-paper reviewer: `Wegener the 2nd`.
- Lean/API reviewer: `Banach the 2nd`.

## Verdict

The supplied finite labels from the previous bridge can be instantiated with
Lean's finite introduced-label domain at a fixed state `(S,J)`. This removes the
arbitrary-`Finset` gap at the finite-domain level.

The bridge remains conditional. It assumes that the supplied `level` and `var`
maps are the current recurrence data, respectively Aoyagi's `tilde_t_(s,k)` and
`u_(s,k)`, and it assumes the introduced-label Case 2 gap. It does not prove that
the recursive invariant supplies those maps, that the gap holds, or that the
row weights have already been generated by this recurrence.

## Required Caveats

- The finite domain is now Lean's `introducedLabelFinset`; this matches the
  already-modeled active introduced labels, not an arbitrary supplied label set.
- The Nat-valued `level` used by the recurrence is not yet connected to the
  integer `leastValue` fields in `LabelExponentCertificate` except through
  separate bridge assumptions.
- The Case 2 comparability sentence in the source is not justified by the label
  gap alone; it needs a stronger vector invariant.
- The printed Case 2 vector mismatch remains unresolved.
- This is not a transition invariant, arbitrary-pivot chart, chart-coverage
  theorem, coordinate-regularity/Jacobian theorem, exponent update, termination
  proof, normal-crossing certificate, or RLCT extraction.

## Checks

- `lake env lean DLNFibre/DLN/Aoyagi/BlowupArithmetic.lean`: passed.
- `lake build DLNFibre`: passed.
- `./scripts/sorries`: `0 sorry`, `0 #exit`, `0 native_decide`, `0 axiom`.
- `git diff --check`: passed.
