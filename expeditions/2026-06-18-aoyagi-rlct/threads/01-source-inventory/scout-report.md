# Scout report - source inventory

Status: xhigh scout `Aquinas`. Read-only scout; no files edited by the scout.

Verified worktree root:

```text
/home/ubuntu/workspace/geometry-of-dln-fibre/.claude/worktrees/aoyagi-rlct
```

Source used: Aoyagi 2023 PDF only.

## Claim key

- `A0`: analytic RLCT/normal-crossing interface.
- `A1`: block elimination.
- `A2`: product reduction.
- `A3`: deepest singular point.
- `A4`: blow-up transition certificate.
- `A5`: arithmetic minimisation/order count.
- `A6`: final formula assembly.

## Inventory

| PDF page | Item | Exact hypotheses / content | Dependencies | Type | Claim |
|---|---|---|---|---|---|
| 5 | Definition 1 | RLCT/local learning coefficient for analytic `F` and `C^\infty` compactly supported `phi` near `w*`; `k=1` real, `k=2` complex; order `theta`; ideal RLCT defined by sum of squares of generators. | none stated | analytic definition | A0 |
| 5 | Lemma 1 | If ideal `J=<F_1,...,F_n>` and analytic `G_i in J`, then `lambda(G_1^2+...+G_m^2) <= lambda(F_1^2+...+F_n^2)`; equality if same ideal. | cited [30-32] | analytic, not elementary | A0 |
| 5 | Definition 2 | Matrix norm `||C|| = sqrt(sum |c_ij|^2)` and ideal `<C>` generated by entries. | none | elementary notation | A0/A6 |
| 6 | Hironaka / normal-crossing extraction | For `K(pi(u)) = product u_j^(2k_j)` and Jacobian/prior factor `pi'(u)phi(pi(u)) = product u_j^(h_j)`, `lambda = min (h_j+1)/(2k_j)` and `theta` is the max number attaining the minimum on a chart. | Hironaka resolution | analytic cited boundary | A0 |
| 6-7 | Theorem 1 | Three-layer/reduced-rank formula from [12]. Prior `phi` is `C^\infty`, compact support, positive at `(A*^(1),A*^(2))`; `r=rank(A*^(1)A*^(2))`; `M^(s)=H^(s)-r`, selected index set by triangle/degenerate cases; formula for `lambda` and `theta=a(ell-a)+1`. | A0 plus older cited result [12] | analytic final formula for special case | A6 context |
| 8 | Main DLN setup | Matrices `A^(s)` of size `H^(s) x H^(s+1)`, `s=1..L`; parameter tuple `w`; Gaussian linear network `Y=(prod A^(s))X + noise`; true parameter `w*`; true density included in model; prior `phi` compactly supported `C^\infty`, `phi(w*)>0`; target RLCT of `||prod A^(s)-prod A*^(s)||^2`. | statistical setup | mixed notation/hypotheses | A6 |
| 8-9 | Definition 3 | `M^(s)=H^(s)-r`; choose `M={M^(S_j): j=1..ell+1}` satisfying the paper's strict/weak inequalities against unchosen `M^(s)`; integer `M_int` with `M_int-1 < (sum M^(S_j))/ell <= M_int`; `a=sum M^(S_j)-(M_int-1)ell`. | main setup | elementary finite data | A5/A6 |
| 9 | Theorem 2 | Multi-layer main formula: `lambda = (-r^2+r(H^(1)+H^(L+1)))/2 + a(ell-a)/(4ell) - ell(ell-1)/4 * (avg M^(S_j))^2 + 1/2 sum_{i<j} M^(S_i)M^(S_j)` and `theta=a(ell-a)+1`. | A0-A5 | final synthesis | A6 |
| 10-11 | Lemma 2 | Matrix `A` size `h1 x h2`, rank `r1`; `r<=r1`; block `A1` is invertible `r x r`. Then block-regular `Q1,Q2` reduce `A` to `[A1 0; 0 C4]`, with `rank C4=r1-r`. | Schur complement | elementary matrix algebra, local chart | A1 |
| 11-13 | Theorem 3 | Near matrices of ranks `r^(s)` with product true rank `r`; after normalising true product to `[I_r 0;0 0]`, there are regular block matrices `P1,P2` with `P1(prod A^(s))P2=[C1 0;0 prod C^(s)]`. | Lemma 2; ideal/RLCT invariance | algebraic, with analytic interface at RLCT use | A2 |
| 13 | Regular-variable contribution | Converts Theorem 3 into `lambda<prod A - prod A*> = (-r^2+r(H^(1)+H^(L+1)))/2 + lambda<prod C^(s)>`. | Theorem 3, A0 | mixed | A2/A6 |
| 14 | Theorem 4 | Homogeneous `F_i` in first `j` variables; smooth `phi` satisfying the stated comparison/homogeneity condition. Gives RLCT at zeroed first `j` variables `<=` RLCT at general point. Used to set `r^(s)=r`. | cited [22], not proved here | analytic | A3 |
| 14 | Reduced matrices | Define `C^(s)` of size `M^(s) x M^(s+1)` and reduce to RLCT of `||prod C^(s)||^2`. | Theorem 4, Theorem 3 | mixed | A2/A4 |
| 14-15 | Blow-up induction invariant | Defines `M(S)=min{M^(s):1<=s<=S}`, vectors `T_{S,k}`, monomials `b_i`, matrix `D_J`, Jacobian exponents `M_{s,k}`, and total comparability of the `T_{s,k}`. | reduced product setup | elementary bookkeeping once blow-ups are encoded | A4 |
| 15-19 | Case 1 | Hypothesis `b_{J+1}=...=b_{J+J1}` and next `b` differs; blow up along rows `J+1..J+J1` plus chosen `u_{s,k}`. Case 1(1) decreases variables at a fixed level; Case 1(2) pivots, updates exponents, and increments `J` or `S`. | induction invariant | algebraic/blow-up coordinate work | A4 |
| 19-22 | Case 2 | Hypothesis `b_{J+1}=...=b_{M(S)}`; blow up along remaining `D_J` block; pivot chart updates `T`, `b`, `M_{s,k}`, then increments `J` or `S`. | induction invariant | algebraic/blow-up coordinate work | A4 |
| 22 | Terminal form | At `S=L+1`, ideal becomes diagonal: `<prod C^(s)>=<diag(b_1,...,b_{M(L+1)})>`, where `M(L+1)` is the prefix minimum, not the actual width `M^(L+1)`. Candidate exponents use `M_{s,k}` and variables with `tilde t_{s,k}=0`. | Cases 1/2 termination | normal-crossing certificate | A4/A5 |
| 22-24 | Terminal exponent formula | `M_{s,k}=(M^(1)-t^1)(M^(2)-t^1)+sum_{j=2}^L(t^{j-1}-t^j)(M^(j+1)-t^j)`, then rewritten via `H_j,S_j,F_j` into the quadratic expression used for minimisation. | terminal form | elementary arithmetic after A4 | A5 |
| 24 | Lemma 3 | For natural `ell` and `0<=a,b<=ell-1`, quadratic `A(b)` satisfies `min_b A(b)=A(a-1)=A(a)=a ell(ell-a)`. | finite quadratic calculation | elementary | A5 |
| 24-25 | Reduced `lambda` formula | Applying Lemma 3 gives `2 lambda_O(||prod C^(s)||^2)` as the closed expression, hence reduced `lambda`. | Lemma 3, terminal exponents, A0 | mixed | A5/A6 |
| 25 | Lemma 4 | If `T~ <= T_{s,k} <= T~'` and each corresponding increment is `M_int-1` or `M_int`, then `T_{s,k}` corresponds to `lambda`. | definitions of `T~`, `T~'`, Lemma 3 setup | elementary | A5 |
| 25-27 | Lemma 5 | Shows `theta=a(ell-a)+1`: upper bound by counting interval sizes, lower bound by explicit vectors in equations (1)-(5), using Case 1(2) charts. | Lemma 4, Case 1(2), terminal normal crossings | elementary count plus A4 certificate | A5 |
| 27 | Completion | Constructs blow-up process with selected `T_{s,k}` from equations (3),(4), closing the main proof. | A4, A5 | synthesis step | A6 |

## Main caution

Aoyagi's multi-layer final theorem is Theorem 2 on PDF p. 9. Theorem 1 on
PDF pp. 6-7 is the cited earlier three-layer formula.
