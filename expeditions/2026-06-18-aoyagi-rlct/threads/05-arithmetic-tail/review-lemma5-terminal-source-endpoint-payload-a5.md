# Review - A5 Lemma 5 terminal source endpoint payload

Review mode: xhigh Lean API scout, xhigh hardener, and controller
integration.

## Verdict

Accepted at the supplied terminal endpoint payload scope.

The theorem packages two existing terminal consequences from the same explicit
source-realisation hypothesis.  It does not construct the source-realisation
equality and does not prove any no-extra or exactness field.

## Checks

- The payload contains terminal Eq5 finite-set coverage and terminal
  introduced-label bookkeeping only.
- The admissible-family theorem keeps `a<=ell`, the selected-width sum, source
  range, width positivity, branch membership, and source realisation explicit.
- The binary-family theorem uses the binary terminal chain-zero field only for
  `F.fullH x (Fin.last ell)=0`; the terminal interval/source-label side still
  carries the selected-width sum.
- The theorem names do not mention source-backed construction, classifier,
  exactness, pole order, normal crossings, or RLCT.

## Residual Risk

Future uses must not treat this payload as branch construction.  The remaining
source-backed Lemma 5 frontier is still the construction of source branches,
source-label legality beyond explicit local hypotheses, classifier/injection,
and back-to-label coverage.
