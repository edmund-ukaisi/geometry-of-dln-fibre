# Post-recovery source-frontier audit

Date: 2026-06-25.

Basis: controller reorientation after interruption, xhigh A4 scout `Godel`,
xhigh A2/A6 scout `Ampere`, and xhigh p.13 residual-factor scout `Hilbert`.

## Verdict

The next high-value work is source/math reproduction, not another Lean wrapper.
The current Lean sockets already expose the honest supplied boundaries for the
nearest A2, A4, and A6 frontiers.

## A2 Selected-Entry Source Boundary

The selected-entry original-loss endpoint is already reduced to explicit
inputs:

```text
Ulocal ∩ sourceStratum =
Ulocal ∩ chartMap pivot '' signedBoxSet Rres
```

and a residual-coordinate readout or matrix/product identity.  The p.13
multi-edge residual-factor audit confirms that a terminal selected-entry
matrix is insufficient by itself; compatible intermediate residual factors and
a product identity are required.

Do not add a convenience wrapper that merely replaces the residual-readout
hypothesis by the already-proved residual-product matrix readout.  It would
still leave source coverage, residual-index equivalence, and the
residual-product identity supplied.

## A2 Regular-Suspension Boundary

The finite and one-sided integrability infrastructure is already substantial:
literal/cleaned square-sum comparison, fixed-base smallness, finite-side
regular-square integrability, residual-power shift sockets, and bounded-density
p.13 adapters are in Lean.

The remaining regular-suspension work is not a finite wrapper.  It requires
producing the analytic product chart/source coverage, density or
Jacobian-prior compatibility, residual-base integrability for the reduced
Aoyagi product, and eventually the endpoint/divergence or normal-crossing
certificate needed for equality and pole order.

Do not claim the supplied regular-suspension interface constructs `Cfull` from
`Cred`; extraction must stay on the actual supplied full certificate.

## A4 Source Production

The only non-vacuous A4 final-socket consumer is already landed:

```text
SelectedEntryCase2DisplayedA0SourceProductionData
SelectedEntryAnalyticAtlasBoundary.theorem2SuppliedChartFinalBoundary_of_case2DisplayedA0SourceProduction
```

The remaining A4 frontier is genuine atlas/source production: produced
successor/source data, suffix/terminal data, coverage, transition regularity,
analytic domains, and branch/termination coverage.  Wrappers around
`SourceProductionObligation` constructors are formula-level packaging unless a
new theorem produces one of those fields.

## A6 Final Assembly

The source-rank/rank-width regular-shift bridges and supplied
regular-suspension final bridge already exist.  Definition 3 branch selection
remains source-ambiguous; final statements must keep selected source data
supplied or prove it in a source-backed branch.

## Next Controller Move

Pick one source-moving reproduction target:

1. regular-suspension analytic chart/source production for p.13;
2. selected-entry source/image equality plus compatible residual factors;
3. residual-base integrability/normal-crossing production for the reduced
   product;
4. A4 analytic atlas/source-production machinery beyond formula-level
   obligations.

Do not spend the next slice on thin final-socket or readout wrappers unless a
downstream theorem consumes exactly that statement and removes a real
hypothesis.
