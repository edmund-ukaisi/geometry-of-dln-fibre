# synthesis.md — controller's integrative read (ext-codimension)

The controller's internal integrative ground; flushed each tick (recovery substrate).

## OPEN — recon in flight (2026-06-17)

Goal: the geometric codimension C of the orbit closures (Cor 3.5), via standard machinery raised on
Mathlib. Two-phase plan (A: Ext algebra on kQ-modules; B: orbit-dimension AG + Voigt). Architecture:
`Tuple d` = representation variety (geometry); `kQ`-Mod = homological algebra; the equivalence is the
bridge (Voigt lives on it). Engine NOT refounded — stacked + bridged. Steering: well-established
mathematics, controller makes the calls, frequent local-codex-consult on standardness.

Recon threads 01 (Mathlib coverage) + 02 (Ext design) dispatched. Awaiting reports → Phase-A ladder.
