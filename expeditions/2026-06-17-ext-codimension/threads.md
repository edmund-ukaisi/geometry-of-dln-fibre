# threads.md — thread index (ext-codimension)

`NN-slug` · type · status · subject. Status ∈ open / in-progress / review-pending / closed / abandoned.

**Execution mode:** controller in the `ext-codimension` worktree; teammates via the Agent tool
(`scout` / `pen-and-paper` / `lean-formaliser` / `reviewer`), reporting to the controller, who
integrates and green-gates. Stacked on `expedition/core-quiver-engine`.

## Opening recon

| Thread | Type | Status | Subject |
|---|---|---|---|
| 01-mathlib-coverage | scout | in-progress | Mathlib map: path algebras / quiver reps / ModuleCat / derived Ext / projective resolutions (algebra); affine varieties + codim / algebraic-group actions / orbit dimension / tangent spaces (AG). |
| 02-ext-design | pen-and-paper | in-progress | Ext¹ between interval modules; Euler form for equioriented Aₙ; verify dim Ext¹(M,M) = Σ m_{i-1,j-1} m_{uv} on (2,2,2); the Voigt orbit-dimension requirements; standardness check (Codex). |
