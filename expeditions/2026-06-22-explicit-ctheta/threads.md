# threads — explicit `(C, θ)` for arbitrary `d`

| NN | slug | type | status | subject |
|----|------|------|--------|---------|
| 01 | arbitrary-ctheta | formalisation (tide) | closed | explicit `cCodim`/`numTop` for arbitrary `d` via the sort bridge + a non-monotone `decide` witness — green, axiom-clean, AUDIT cleared |

Status vocabulary: `open` / `in-progress` / `blocked` / `review-pending` / `closed` / `abandoned`.
