# Lean 4 + Mathlib v4.29: lowest-friction formulation of a localized graph-ideal elimination

You advised on this build before (direction-B, explicit-inverse iso, headline = ideal equality). I'm
now grinding the elimination iso and hitting `Fin`-cast / `RepCoord`-index friction. I need the
LOWEST-FRICTION concrete formulation. Be specific with Mathlib v4.29 lemma names. Adversarial.

## What's already LANDED (sorry-free, axiom-clean)
- `A_eng := MvPolynomial (RepCoord ![q,p]) k`, where `RepCoord ![q,p] = Σ _ : Fin 1, Fin p × Fin q`
  (entries of one `p×q` matrix; the generic matrix is `multPoly`).
- `detPivotPoly` = det of the top-left `r×r` submatrix of the generic matrix (the pivot minor `detΔ`).
- `Iad := (sigmaIdeal ![q,p] r).map (algebraMap A_eng (Localization.Away detΔ))`, and
  **`height Iad = C = (p−r)(q−r)`** (landed, via localization-height transport + a witness).
- `det_submatrix_multPoly_mem_sigmaIdeal`: every `(r+1)`-minor of `multPoly` is in `sigmaIdeal`.
- `Iad`, `sigmaIdeal` are PRIME (alg-closed k). `A_loc := Localization.Away detΔ`.

## The remaining target (the bulk)
Expose `{Iad = J, A_loc/Iad regular of dim δ}` where `δ = r(p+q−r)`, with `J` = the Schur graph ideal
of `B22 = B21·adj(Δ)·B12 / detΔ`. The plan: `A_loc/J ≅ free Schur localization Sd` (eliminate B22) ⟹
`height J = #(B22 vars) = C` + `J` prime + `A_loc/J` regular dim δ; then `Iad = J` by `J ⊆ Iad`
(from the minor lift) + `height J = height Iad = C` + both prime (`Ideal.height_strict_mono_of_is_prime`).

## The friction I'm hitting
Direction-B comorphism `ψ : A_eng → Sd` via `aeval psiGen`, where `psiGen` classifies each `RepCoord`
index `⟨0,(a,b)⟩` (`a : Fin (dStratum q p (Fin.succ 0))`, `b : Fin (dStratum q p (Fin.castSucc 0))`)
into its block (Δ/B12/B21/B22) by `a.val < r` / `b.val < r`, sending it to the matching free Schur
generator or to `forcedB22` (= `(B21S·adjΔS·B12S) /detΔS` in `Sd`). Problems:
1. `omega` can't discharge the `Fin` bounds (e.g. `b.val − r < q − r`) because `b : Fin (dStratum q p
   (Fin.castSucc 0))` and `omega` doesn't see `dStratum q p (Fin.castSucc 0) = q` (it's `rfl` but not a
   hypothesis). Manually extracting `a.isLt` + rewriting the `dStratum` dim is fiddly × 4 cases.
2. The whole `ψ`/inverse/factor-through-J/height chain is `Fin`-cast-heavy.

## My candidate lower-friction route (validate or replace)
The block-decomposition equiv ASSEMBLES cleanly (verified):
`Fin p ≃ Fin r ⊕ Fin (p−r)` (`finCongr` + `finSumFinEquiv`), then
`Fin p × Fin q ≃ ((Δ)⊕(B12))⊕((B21)⊕(B22))` (`Equiv.sumProdDistrib`/`prodSumDistrib`), so
`RepCoord ≃ (SchurVar) ⊕ (B22block)` (folding the Σ over `Fin 1`).
Then `MvPolynomial.renameEquiv` relabels `A_eng ≃ₐ MvPolynomial (SchurVar ⊕ B22block) k`, and
`MvPolynomial.sumAlgEquiv` gives `≃ₐ MvPolynomial B22block (MvPolynomial SchurVar k)`. Localize at
`detΔ`. Now the B22 variables are isolated, and `J` is generated by `X_{B22} − forced` — a coordinate
translation. Eliminating each B22 variable (multivariate `Polynomial.quotientSpanXSubCAlgEquiv`-style)
gives `≅ (MvPolynomial SchurVar k)_detΔ = Sd`.

## QUESTIONS — be concrete
1. Is the `renameEquiv` + `sumAlgEquiv` relabeling route ACTUALLY lower friction than the per-index
   `aeval psiGen` route, or does the `RepCoord = Σ _ : Fin 1, …` wrapper + the 4-way distribution
   `Equiv` make the relabeling just as painful (lots of `Equiv.trans` bookkeeping + `rename_rename`)?
   Which would YOU write?
2. For the MULTI-VARIABLE localized graph-ideal quotient `MvPolynomial B22block (MvPolynomial SchurVar k)
   localized / (X_b − forced_b : b ∈ B22block) ≅ Sd` — Mathlib v4.29 has single-variable
   `Polynomial.quotientSpanXSubCAlgEquiv` (quotient by `X − c` ≅ base). Is there a clean multivariate
   analogue, or a `MvPolynomial.eval`-kernel route (`ψ` surjective with `ker ψ = J`)? Concretely: is it
   easier to (a) iterate the single-var elimination over `Finset B22block`, or (b) build the iso via
   `aeval` + an explicit inverse and prove the round-trips by `MvPolynomial.algHom_ext` +
   `IsLocalization.algHom_ext`? Name the cleanest path.
3. Is there a way to get `height J = C` + `J` prime + `A_loc/J regular dim δ` WITHOUT the full iso —
   e.g. `J` is generated by `#(B22) = (p−r)(q−r)` elements (the Schur relations), so `height J ≤ (p−r)(q−r)`
   by Krull's height theorem (`Ideal.height_le_...`?), and combined with `J ⊆ Iad` + `height Iad = C =
   (p−r)(q−r)` ⟹ `height J = C` (squeeze) ⟹ `J = Iad` (height comparison, since `J ⊆ Iad` both prime equal
   height) — WITHOUT ever proving the iso? Does Mathlib v4.29 have the Krull height bound
   `height (span of n elements) ≤ n` (`Ideal.height_le_card` / `Ideal.span` + generators)? If so this
   collapses the whole thing: NO elimination iso needed for `Iad = J`, only `J ⊆ Iad` + a height upper
   bound + the landed `height Iad = C`. Then `A_loc/Iad ≅ Sd` (regularity) can be a SEPARATE, optional
   exposure. Sanity-check this squeeze — is `height J ≤ #generators` the right Krull statement and is it
   in Mathlib v4.29?

Pick the single lowest-friction path to `{Iad = J}` and (separately) to `{A_loc regular dim δ}`. If the
Krull-squeeze (Q3) works, that's almost certainly it — confirm the exact Mathlib lemma for
`height ≤ #generators` and any hypotheses (Noetherian? the ring is a polynomial ring so yes).
