# Decorrelated review: localized determinantal base presentation + full ring-level height composition

You are an independent second model (decorrelated from Claude). I am formalising,
in Lean 4 + Mathlib v4.29, a chain that computes the codimension of a fibre of the
multiplication map of deep linear networks. I need you to RED-TEAM two things and
return GO / NO-GO with the precise mathematical obstruction if NO-GO. Be concrete
and adversarial; do not be agreeable. Cite standard theorems by name.

## Background (the setting, precisely)

Over an algebraically closed field `k` of characteristic 0. Fix `p, q, r` with `r ≤ p, r ≤ q`.

- `Mat = Mat_{p×q}(k)`, affine space of dimension `pq` (coordinate ring `A := k[x_{ij}]`, `pq` variables).
- `Σ̄^r := {M ∈ Mat | rank M ≤ r}` — the classical determinantal variety, irreducible,
  `dim Σ̄^r = r(p+q−r) =: δ`, `codim_Mat(Σ̄^r) = (p−r)(q−r) =: C`.
- `R_base := A / I(Σ̄^r)` where `I(Σ̄^r)` is the vanishing ideal (radical) of `Σ̄^r`.
  Classically `I(Σ̄^r)` is the ideal `I_{r+1}` generated by the `(r+1)×(r+1)` minors,
  which is prime; this is the Hochster–Eagon / classical determinantal-ideal theory.

I have ALREADY proved in Lean, via a quiver-orbit-closure engine (NOT via
determinantal-ideal generators):
- `R_base` is a DOMAIN (`I(Σ̄^r)` is prime) — from "`Σ̄^r` = one orbit closure ⟹ irreducible".
- `dim R_base = δ` and `codim = C` (the height of `I(Σ̄^r)` = `C`), via a catenary-type
  bridge `height(I) + ringKrullDim(A/I) = pq` for irreducible varieties (this bridge is LANDED).
- The engine has the height transport lemmas `RingEquiv.height_comap`, `IsLocalization.height_comap`.

NOTE: I do NOT have, and Mathlib v4.29 does NOT have, the determinantal-ideal GENERATING-SET
theorem `I(Σ̄^r) = I_{r+1}` (the explicit `(r+1)`-minor generators). I only have that the
vanishing ideal is prime and its height/quotient-dimension, via the orbit machinery.

## The pivot chart (G2-1, already formalised at the matrix level)

Write a `(p×q)` matrix in blocks `M = [[Δ, B12],[B21, B22]]` with `Δ` the top-left `r×r` block.
On the chart `U := {det Δ ≠ 0}`, I proved (matrix level): `rank M = r ⟺ B22 = B21·Δ⁻¹·B12`.
So set-theoretically the chart `Σ̄^r ∩ U` is the graph of `(Δ,B12,B21) ↦ B21 Δ⁻¹ B12` over
`{Δ invertible} × Mat_{r×(q−r)} × Mat_{(p−r)×r}`, which is `δ`-dimensional.

## QUESTION (a): the localized base presentation

I want to prove, as a Lean `AlgEquiv`:

> `R_base` localized at the image `δ̄ := det Δ` of `det Δ`  ≅ₐ[k]
>   the localization of the FREE polynomial ring `k[Δ_{ij}, (B12)_{ij}, (B21)_{ij}]`
>   (`r² + r(q−r) + (p−r)r = δ` free variables) at `det Δ`.

i.e. on the chart, `R_base` becomes a free polynomial localization in the Schur coordinates
(the `B22` block is eliminated by the Schur relation `B22 = B21 Δ⁻¹ B12`).

RED-TEAM THIS:
1. Does proving this `AlgEquiv` REQUIRE the determinantal-ideal generating set `I(Σ̄^r)=I_{r+1}`,
   or can it be done from the prime ideal + the chart geometry alone? Specifically: to know that
   localizing `A/I(Σ̄^r)` at `det Δ` gives the free Schur localization, do I need to know the
   GENERATORS of `I(Σ̄^r)` so I can check they all become consequences of the single Schur
   relation `B22 − B21 Δ⁻¹ B12` after inverting `det Δ`? Or is there a generator-free route
   (e.g. via the explicit graph parametrization being an open immersion / the comorphism of an
   iso of the LOCALIZED varieties), exploiting that I already have primality + `dim = δ`?
2. The honest concern: an iso of `k`-POINTS (a bijection of the chart with the free affine space,
   which G2-1 gives) is NOT an iso of COORDINATE RINGS without knowing the scheme structure /
   that `I(Σ̄^r)` localizes to exactly the Schur relation. Over a reduced/integral domain with
   a known dimension, does the bijection-of-points + matching-dimension + irreducibility upgrade
   to a ring iso? (Zariski's main theorem / a birational + normal argument?) Be precise about
   what extra hypothesis is needed (normality? the variety being smooth on the chart?).
3. Is there a SHORTER route to what the downstream chain ACTUALLY needs (see (b)) that AVOIDS
   the explicit free presentation entirely — e.g. proving flatness / going-down of the comorphism
   `R_base → R_total` on the chart directly, without the free-localization `AlgEquiv`?

## QUESTION (b): the full ring-level height composition (the chain this feeds)

The downstream target is `codim_Rep(fibre over E) = C + δ` where `E ∈ Σ̄^r` is a closed point and
`fibre = mult⁻¹(E)` in `Rep_d` (the space of composable matrix tuples), via:

1. **Catenary composition (in `A_Rep = MvPolynomial(RepCoord) k`):**
   `codim_Rep(fibre) = C + codim_{Σ̄^r}(fibre)` for each top component, claimed "by catenary".
   (`codim_Rep(Σ̄^r) = C` is the LANDED Brick A.)
2. **Height-additivity on the comorphism `R_base = A → B = R_total` (`R_total` = coordinate
   ring of `mult⁻¹(Σ̄^r)` or `Rep`):** for `P` a minimal prime of `m_E·B`,
   `height P = height(m_E) + height(P / m_E·B)`, with `height(m_E) = dim R_base = δ` (E a closed
   point of the irreducible `δ`-dim base) and `height(P/m_E·B) = 0` (P minimal over `m_E·B`),
   giving `codim_{Σ̄^r}(fibre) = δ`.
3. **Going-down only on the chart:** mult is NOT globally flat (fibre dim jumps), so localize at
   `det Δ`; on the chart the Schur `AlgEquiv` gives `B_loc ≅ A_loc ⊗ F_E` (free ⟹ flat ⟹
   going-down). Localization keeps the height of primes meeting the chart.

RED-TEAM THIS COMPOSITION:
1. Step 1 "catenary": Mathlib v4.29 has NO catenary ring theory and NO general dimension
   formula `dim(A/p) + height(p) = dim A` beyond the polynomial-ring bridge I have. Is the claim
   `codim_Rep(fibre) = C + codim_{Σ̄^r}(fibre)` actually a CATENARY statement, or is it just the
   `pq`-bridge applied twice? Identify exactly what theorem is needed and whether it follows from
   the polynomial-ring height+krulldim bridge alone (which I have) or needs genuine catenary-ness
   (which I do NOT have).
2. Step 2 "height-additivity `height P = height(m_E) + height(P/m_E B)`": this is the fibre
   dimension formula / the going-down + going-up local height identity. Is this TRUE in the
   generality stated, and what are the EXACT hypotheses (flatness of `A→B` at P? `B` Cohen–Macaulay?
   equidimensional fibres? catenary?)? Mathlib has `Algebra.HasGoingDown` + `HasGoingDown.of_flat`
   but I see no packaged additive height identity. Is `height(m_E) = δ` correct — `m_E` is the
   maximal ideal of a CLOSED POINT in `R_base`, and for an affine domain of dim δ over alg-closed
   k, every maximal ideal has height δ (Nullstellensatz + catenary/equidim). Do I have that, or
   does it ALSO need catenary?
3. Is the OVERALL decomposition `codim_Rep(fibre) = C + δ` even the right target, and is the
   "localize on the chart, transport height" bookkeeping sound — i.e. does a minimal prime `P` of
   the fibre necessarily MEET the chart `{det Δ ≠ 0}` (so its height is preserved under
   localization)? If a top component of the fibre lies entirely in `{det Δ = 0}` the chart misses
   it. Is that possible, and does it break the height computation?

## What I need back

For EACH of (a) and (b): a GO / NO-GO, the precise theorem(s) required, whether each is in
Mathlib v4.29 or must be built, and — critically — whether the chain can be completed WITHOUT
the determinantal-ideal generating set `I=I_{r+1}` and WITHOUT a general catenary theory, using
only: (i) primality + dimension of `R_base` from the orbit engine, (ii) the polynomial-ring
`height + krullDim = #vars` bridge, (iii) Mathlib's flat ⟹ going-down, (iv) the height-transport
lemmas. If NO-GO, state the single most load-bearing missing piece. If there is a strictly
simpler architecture that reaches `codim = C + δ`, propose it.
