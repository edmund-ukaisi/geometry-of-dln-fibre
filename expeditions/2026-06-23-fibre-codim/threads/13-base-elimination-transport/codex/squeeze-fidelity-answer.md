**Q1.** **FACT:** Yes. If `J ≤ K`, `J.IsPrime`, `J.FiniteHeight`, and `height J = height K`, then a strict containment `J < K` would force `height J < height K` by the stated Mathlib lemma, contradiction. **FACT:** `eq_of_le_of_not_lt` with `J ≤ K` proves `J = K`; using `.symm` is the correct way to close a goal `K = J`.

**Q2.** **FACT:** The height squeeze alone does not identify the intended graph ideal; many graph ideals can have height `C`. **INFERENCE:** If `forcedB22` were wrong, `height J = C` and `J.IsPrime` could still hold, so the decisive content is step 5: proving each generator `X b - C(forcedB22 b)` lies in `K` via the bordered-minor/Schur-unit computation. If `forcedB22 = 0` or otherwise wrong, the proof should fail exactly at that generator-membership/unit-cancellation step, unless the wrong formula coincidentally gives the same class modulo `K`.

**Q3.** **FACT:** If Lean proves `height J = C` as a finite `Nat` value and derives `J.FiniteHeight` from that, then the strict-height lemma is not being applied with `⊤` hidden in the background. **FACT:** If `C = 0`, the squeeze is still logically valid: height-zero prime plus containment in an equal-height ideal forces equality. **INFERENCE:** The real risk at `C = 0` is not vacuity but whether the mathematical setup still matches the intended determinantal/Schur situation; Lean would still only prove the stated equality for the degenerate finite type.

**Q4.** **FACT:** If `K = Ψ(Iad)` and `Ψ` is an ideal-preserving algebra equivalence, then `K = J` transports back to `Iad = Ψ.symm(J)`; inclusions transport contravariantly only if one is careless, but equality through an equivalence is robust. **INFERENCE:** So the hard inclusion `Iad ⊆ Ψ.symm(J)` is genuinely delivered, provided the formalisation uses the actual ideal map/comap induced by `Ψ` and not a separately named ideal merely intended to be that transport.

**Q5.** **INFERENCE:** Step 5 is not circular if it proves generator membership from explicit bordered-minor identities plus multiplication by the Schur determinant unit; that is the intended “easy direction” `J ⊆ K`, not the reverse. **INFERENCE:** The main hidden audit points are whether `height Iad = C` was imported from an independent determinantal-codimension theorem rather than from the desired presentation, and whether the Schur unit-cancellation proof uses the correct localization/equivalence data. **FACT:** Given those are independent, the hard direction is earned by prime finite-height rigidity, not assumed.