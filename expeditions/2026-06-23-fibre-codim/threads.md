# threads — fibre-codimension bundle-shift (LR Lemma 4.6)

| NN | slug | type | status | subject |
|----|------|------|--------|---------|
| 01 | recon | explore (scout) | closed | scope Lemma 4.6 reachability → **VERDICT: GENUINE BLOCKER** — needs a from-scratch finite-type-morphism dimension theorem (~8–15 modules) absent from Mathlib v4.29; landed orbit-codim tools don't transfer (general fibre not `GL_d`-stable). Decorrelated-Codex-confirmed. Operator committed to building it; thermometer first. |
| 02 | thermometer | formalisation (tide) | closed | **LANDED** `Core.DeterminantalStratumDim.varietyDim_productRankLocusLE_stratum` = `r(n+m−r)`, via the **N=1 specialisation** of the quiver engine (no new orbit map). Green, axiom-clean, reviewer+controller PASS. Reading: **layer 1 (morphism-dim wall) NOT de-risked** — rode the single-variety catenary. |
| 03 | layer1-design | explore (scout) | closed | layer-1 DESIGN recon → **GO, REFRAMED.** We don't need the general morphism theorem — only `dim fibre`, via the going-down height-additivity lemma (`@[stacks 00ON]`, landed pattern in `AffineDomainDimension`/`FlatQuasiFiniteHeight`). Scope collapses to a **~5–8-module flat-height squeeze**; risk localized to **L1-0 flatness of `mult` on a rank-`r` chart**. Codex-converged. |
| 04 | l1-flatness | formalisation (tide) | checkpointed | **Tide A verdict** (`Core.FlatTrivialProductProbe`, builds green): the `Module.Flat` API is fully viable at v4.29, BUT the flat route needs a from-scratch coordinate-ring map `A →+* B` for `mult` (the engine has none) + chart localisation + trivialisation — an ~8–12-module affine-AG build, NOT an API gap. Revises L1-0 cost UP. RESUME = operator re-scope: ring-map build / two-inequality sandwich / `r≤1`+roadmap. (Two overload deaths obscured that the verdict was reached.) |
| 05 | comorphism | formalisation (tide) | closed | **Tide F1 — the keystone. LANDED** (`Core.MultComorphism`, green/sorry-free/axiom-clean; reviewer + decorrelated-Codex fidelity PASS; controller green-gated the aggregated build). `multPoly`/`eval_multPoly`/fibre=zero-locus/`vanishingIdeal=radical(fibreGenIdeal)`/`multComap`+`Ideal.map(maxIdealOfPoint B)`. Built the ring map thread-04 found missing (constructible: `mult` over `MvPolynomial`). Aggregation surfaced a `genericTuple` dup with `RankLocusClosed` → consolidated into shared `Core.GenericTuple` (CommRing). |
| 06 | height-squeeze | formalisation (tide) | closed | **Tide F2 — sandwich is a NO-GO (Codex-confirmed); landed the honest reachable half.** The controller's "sandwich" steer had a direction error: Krull's height theorem bounds codim *above* by `#gens = d_N·d_0` (≫ shift), NOT below; and `mult` is **not flat** — fibre dim jumps as rank drops (witness `(2,2,2)`: rank-1 fibre dim 4, rank-0 dim 5), so going-down additivity can't apply globally. The shift only appears after the exact-rank **chart trivialization** (absent at v4.29). LANDED instead (`Core.FibreCodim`, green/axiom-clean): the inclusion lower bound `codimRepCanonical Σ̄^r ≤ codimRepCanonical(mult⁻¹ B)` from `mult⁻¹B ⊆ Σ̄^r` — honestly named one-sided, NOT the identity. Full identity stays Cited; **roadmap = the rank-chart local-trivialization build** (the genuine LR submersion content). r≤1 fallback also not clean (only r=0, already landed). |

| 07 | chart-design | explore (scout) | closed | **G0 — design recon. VERDICT: CONDITIONAL GO.** Chart build is buildable but is the real submersion made AG-concrete: **~6–12 modules, HIGH-risk core = chart-flatness proof.** Shortcuts dead (inner-group NO-GO; `fibre(E)=Σ⁰_{d−r}` FALSE). `varietyDim` sees the closure not the open chart ⟹ NEW localized-chart layer; Mathlib lacks `Localization.Away` Krull-dim + tensor-dim additivity. Recommended route **M-goingdown** (localize `multComap` on the pivot chart where `mult` is flat → landed going-down additivity). Plan: gate the flatness core with a cheap prototype tide (G2a) before the full G2. Decorrelated-Codex convergent. |
| 08 | normal-form | formalisation (tide) | closed | **G1 — reduce to normal form. LANDED** (`Core.FibreNormalForm`, green/sorry-free/axiom-clean; controller fidelity-read + green-gated + aggregated). `codimRepCanonical_fibre_eq_of_rank_eq`: same rank ⟹ same fibre codim (needs `hN: N≥1`, correct — false at N=0; aligns with the interface's `0<N`). Built the rank-normal-form from scratch + a reusable codim-invariance-under-base-change lemma (`codimRep_baseChange_image`, fills an OrbitCodim gap). Reduces Lemma 4.6 to the single fibre over E. |

| 09 | chart-flatness-proto | formalisation (tide) | closed | **G2a — gating flatness prototype. VERDICT: NO-GO this run** (tide + decorrelated Codex convergent; a clean thermometer reading). Localized comorphism REACHABLE; flatness reachable GIVEN the trivialization (tensor closer `R_t≃R_b⊗F_E`, `F_E` reducible). **WALL = the Schur `AlgEquiv`**: the engine's exact-rank coordinate rings are opaque `vanishingIdeal`-quotients with no explicit presentation. Full G2 = ~8–12 modules of from-scratch determinantal-AG (no API gap). Durable probe `Core.ChartFlatnessProbe` (green, un-aggregated). Per gate contract ⟹ land + roadmap. |

| 10 | base-chart-presentation | formalisation (tide) | closed | **G2-1 — base pivot-chart presentation. LANDED** (`Core.DeterminantalChart`, green/axiom-clean; tide's reviewer+Codex PASS + controller fidelity-read + aggregated). Headline `rank_fromBlocks_eq_card_iff_schur` (rank=card m ⟺ B22=B21·Δ⁻¹·B12), the chart parametrization Equiv, + reusable bricks (block-diag rank additivity, rank=0↔=0; both missing in Mathlib). Matrix/variety-level — variety-dim + localized AlgEquiv honestly deferred. |
| 11 | base-ring-presentation | formalisation (tide) | closed | **G2-2 base-codimension content — LANDED** (`Core.DeterminantalChartRing`, 3 seams, green/axiom-clean/aggregated; controller green-gated + fidelity-read). Bordered Schur minor identity over any ring (universal-coefficient route), the `(r+1)`-minor lift `∈ sigmaIdeal` (general-N, via the multPoly bridge — dodges the dependent-Fin wall), and **`height Iad = C`** (localized base codim, via the `diag(I_r,0)` witness ⟹ detΔ∉sigmaIdeal). Dodged determinantal-ideal theory (absent at v4.29) via the generator-free Schur-graph route. Codex composition NO-GO refuted (engine has the pieces). Closed at a clean seam; elimination → thread 12 with a rich handoff. |
| 12 | base-elimination | formalisation (tide) | closed | **G2-2 elimination engines + reindex/detΔ bridge — LANDED** (3 modules, green/axiom-clean/aggregated; controller green-gated + fidelity-read `blockAlgEquiv_detPivot`; reviewer-audited the first two seams). `MvPolynomialKerAeval` (`ker_aeval_eq_graphIdeal` arbitrary ι via the translation identity — cleaner+more general than planned; + `graphIdealQuotientEquiv`, `graphIdeal_isPrime`), `GraphIdealHeight` (`height_graphIdeal_eq` catenary), `DeterminantalBaseElimination` (`repCoordReindex`, `blockAlgEquiv`, `blockAlgEquiv_detPivot` = detΔ↦C detSchurS). Context consumed at the reindex/detΔ boundary → final transport handed to thread 13. |
| 13 | base-elimination-transport | formalisation (tide) | closed | **G2-2 COMPLETE — `A_loc/Iad ≅ₐ[k] Sd` LANDED** (`Core.DeterminantalBasePresentation.basePresentationEquiv`, green/axiom-clean; reviewer PASS + controller fidelity-read + aggregated). The localized base ring mod its base ideal IS the free Schur localization (regular, dim δ), via the localized Ψ (`blockAlgEquivLoc`), the bordered-minor identity `(*)` (`blockAlgEquiv_borderMinor`), and the height squeeze `Iad = J`. **Correctness guard satisfied**: hard direction `Iad ⊆ J` earned by the squeeze (heights from independent engines, no circularity), not assumed. Heavy multi-tide coordination churn (3 collisions, controller-caused) but ZERO damage; lesson logged (one tide per rung). |

| 19 | endpoint-normalization | formalisation (tide) | open | **R2-3b-3 — the endpoint-normalization AlgEquiv** (the coordinate change, GENERAL in `H`,`L`,`R`). Substitution of the first/last factors by invertible matrices over the coefficient ring (`A₁ ↦ A₁·H⁻¹`, `A_N ↦ L⁻¹·A_N`), round-trip via matrix-inverse, + the mult-transport `multPoly ↦ L⁻¹·multPoly·H⁻¹` (the bridge `mult(A)=LEH ⟺ mult(Ã)=E`). Contract pre-staged in `Core.DeepChartRing` (`AlgEquiv.ofAlgHom (aeval toSub)(aeval fromSub)`). SPECIFY-first + decorrelated Codex (the `N=1` endpoint coincidence). R2-3b-4 instantiates `H`,`L` with the Schur-data unipotents. |
| 18 | deep-chart-ring | formalisation (tide) | closed | **R2-3b-1+2 LANDED + `IsReduced Sred` capstone** (`Core.DeepChartRing`, green/sorry-free/axiom-clean; aggregated `056e28da`+`4345e9e9`; reviewer+Codex+controller fidelity PASS). The DEEP chart ring `Sred = Localization.Away(ΔPdeep) ⧸ IadDeep` for general `d` (engine base presentation was N=1 only); `repStratumEquiv`/`deepBaseComap` bridges; the load-bearing `deepBaseComap_detPivot` (det transport) + `deepBaseComap_sigmaIdeal_le` (base→deep `sigmaIdeal`, NON-circular: deep tuple → base matrix via `mult`); `schurToSred` + `sredSchurAlgebra` (R2-3a's `R=SchurLoc`, `S=Sred` slots); the R2-3b-3 contract pre-staged. **`isReduced_Sred` [IsAlgClosed k]** (via `IsLocalization.map_radical` + `isRadical_iff_quotient_reduced`) — name=content + **discharges R2-3a's `hSred`**, so R2-3b-4 needs only the iso `e`. |
| 17 | trivialization-algequiv | formalisation (tide) | closed | **R2-3a — route-(b) reducedness chain LANDED** (`Core.FibreReducedTrivialization`, green/sorry-free/axiom-clean `[propext,Classical.choice,Quot.sound]`; aggregated `ef9b78c9`; controller fidelity-read PASS). `isReduced_of_tensor` (tensor-with-a-field reducedness descent — the one pinned Mathlib dependency, closed outright via `includeRight_injective`+`isReduced_of_injective`), `fibreGenIdeal_isRadical_of_trivialization`, `vanishingIdeal_fibre_eq_fibreGenIdeal_of_trivialization` (radical-collapse of MultComorphism pt4) — all wired against `e : S ≃ₐ[k] R⊗_k F_B` + `IsReduced S` as EXPLICIT hypotheses (not sorry/axiom). The deep iso `e` itself = the OPEN residual **R2-3b** (thread 18). Honesty locked: no downstream discharges BundleShiftInterface on `e` until R2-3b proves it. Decorrelated-Codex build order in `codex/algequiv-answer.md`. |
| 16 | cut-ideal-radical | pen-and-paper | closed | **R2-2/R2-3 ideal-transport — CERTIFIED.** Verdict (no hedge): cut ideal `I_E` RADICAL / `F_E` REDUCED for all N≥1/dim/target (Singular `primdecGTZ` on 11 cases + decorrelated Codex). Mechanism: reducedness INHERITED from the reduced ambient rank locus (type-A, Buch–Fulton/Kinser–Rajchgot) via faithfully-flat local triviality — NOT smoothness/CM. Route (b) deferred: build R2-3 against the reduced `Sred`, DERIVE `F_E` reduced (so `Scut=Sred` is the conclusion). Caught: `sigmaIdeal` is radical-but-REDUCIBLE on the chain (not prime). One Mathlib dep to pin (tensor-field reducedness descent). **The wall has no hole.** | Adjudicate the load-bearing truth-value: is the cut ideal `(mult(Ã)−B_univ)` RADICAL (⟺ `F_E` reduced ⟺ `Scut = Sred`)? Exact algebra (Gröbner on the `(2,2,2),r=1` anchor + general argument) + decorrelated Codex. Witness ⟹ certify the formaliser mechanism (route a direct vs b deferred); obstruction ⟹ the re-route. No Lean. |
| 15 | total-ring-presentation | formalisation (tide) | closed | **R2-2 — VERDICT L** (probe-tide; no library Lean, no sorry — clean STOP per protocol; tide + Codex converged). The localized total-ring quotient presentation does NOT cleanly reuse G2-2: the engine has `Sred = (MvPol/sigmaIdeal)[1/ΔP]` (reduced) but the route wants `Scut = R[Ã]/(mult(Ã)−B_univ)` (cut), and `Scut = Sred` needs the cut ideal RADICAL — new content. Routed to pen-and-paper (thread 16) to certify the radical/reducedness mechanism before any formaliser commits. | Present `S = O(Σ̄^r ∩ chart) ≅ R[factor entries]/(mult(A)−B_univ)` over `R = Sd` (G2-2's ring). SPECIFY/probe-first: decides M (reuses G2-2's `blockAlgEquivLoc`/`IsLocalization` machinery → R2-3 solid) vs L (new affine scaffolding → pen-and-paper certify R2-3 first). Checkpoint with the M/L verdict before grinding. Decorrelated Codex. |
| 14 | total-presentation-design | explore (scout) | closed | **G2-3a — design recon. VERDICT: REACHABLE, materially harder than G2-2 (~7–10 modules); endpoint-normalization route** (`mult(A)=B ⟺ mult(Ã)=E` → `S ≅ R⊗F_E` → flat → going-down → `codim_{Σ̄^r}(fibre)=δ`). G2-2's graph trick doesn't port (total rank ideal factors). Wall = R2-2/R2-3 (explicit total-ring quotient presentation + the normalization AlgEquiv; no engine quotient-presentation of Σ̄^r). CI/`d_N·d_0` shortcut KILLED (confound: wrong codim at r=0). Rung-ladder R2-1..R2-6; G2-4/G2-5 fold into R2-6. Decorrelated-Codex convergent. | (total-space presentation + flatness). Scope, given G2-2's landed base presentation + the reusable engines: the total ring `B_total` (Σ̄^r ⊆ Rep_d) on the chart, the trivialization `B_loc ≅ A_loc ⊗ F_E` / flatness ⟹ going-down ⟹ `codim_{Σ̄^r}(fibre)=δ`, the reducibility (min-prime) correspondence, + the G2-3 rung-ladder + reachability verdict. Decorrelated Codex. No Lean. |

Status vocabulary: `open` / `in-progress` / `blocked` / `review-pending` / `closed` / `abandoned`.
