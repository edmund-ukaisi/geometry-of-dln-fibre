# Thread 03 — Rm Mathlib coverage (scout)

**Target:** what Mathlib v4.29 has for the smooth-locus local model — regular sequence (`RingTheory.Sequence.IsRegular`), lci / Koszul / regular-immersion scheme API, `KaehlerDifferential`/conormal — and what is a gap (the anatomy scout flagged lci scheme API as an upstream TODO). Sizes R2. Output: coverage map + the build route for "lci/regular-sequence at smooth points → local quadratic model".

## Notes / read
