# Red-team a Lean/Mathlib coverage-and-route question (DLN RLCT bridge, R2 upper-bound geometry)

You are a decorrelated reviewer. I am sizing the build route for "R2" in a Lean 4 + Mathlib (v4.29) formalisation. Be terse, adversarial, and concrete. Flag any logical gap I am glossing.

## Setting
We formalise the geometry behind the RLCT (real log-canonical threshold) of a deep-linear-network square loss. The loss `K(A) = ‖mult(A) − B‖²_Frobenius` over ℝ. Its zero-set is the fibre `mult⁻¹(B)`, an affine variety of codimension `c = C + δ` in the representation space (this codim is already PROVED in Lean). We want to retire a monolithic cited axiom `rlct(K) = ½·c` into [thin cited analytic interface] + [proved DLN geometry].

R2 = the UPPER-bound geometry. The stated plan: "at a smooth point of the fibre, the c defining equations form a regular sequence (lci) → the loss has a local quadratic normal form → local rlct = c/2."

## What is already banked in Lean (proved, sorry-free)
- At a smooth closed point `m` of the fibre component ring `A` (finite-type over alg-closed char-0 `k`): `Localization.AtPrime m` is a REGULAR LOCAL RING; `Ω[A_m/k]` is free of rank = relative dim = ambient − c; and `finrank_κ(m)(CotangentSpace) = ringKrullDim(A_m)` (the cotangent comparison). Also a generic Jacobian-cokernel identity: at a rational point of `V(span g)`, `finrank(CotangentSpace(AtPrime m_A)) = finrank(ker Jacobian(g))`, with the dual cokernel form `(σ→k)/range(Jᵀ)`.

## Relevant Mathlib v4.29 facts I have confirmed exist
- `RingTheory.Regular.RegularSequence`: `IsWeaklyRegular`/`IsRegular` (lists of ring elts on a module), `isRegular_cons_iff`, etc.
- `RingTheory.KrullDimension.Regular`: `ringKrullDim_add_length_eq_ringKrullDim_of_isRegular (rs) (reg : IsRegular R rs) : ringKrullDim (R ⧸ ofList rs) + rs.length = ringKrullDim R`.
- `RingTheory.Ideal.KrullsHeightTheorem`: full Krull height theorem — `height_le_spanFinrank`, `height_le_card_of_mem_minimalPrimes_span`, `exists_spanRank_eq_and_height_eq`, etc.
- `RingTheory.Smooth.StandardSmoothCotangent`: for a SUBMERSIVE presentation `P` (the standard-smooth chart; Jacobian a unit), the conormal `I/I²` (`Extension.Cotangent`) is S-FREE on the `σ` relations (`basisCotangent`, `free_cotangent`), and `H1Cotangent = 0`. `card σ = #relations = codim`.
- NO Koszul complex, NO `IsCompleteIntersection`, NO regular-immersion / lci SCHEME API exist in Mathlib v4.29.
- QuadraticForm normal-form lemmas exist (`equivalent_weightedSumSquares_of_isAlgClosed`).

## My current read (red-team these)
1. The "c defining equations form a regular sequence" framing is partly a RED HERRING for what R2 actually needs. The genuine analytic content (loss has a local quadratic model ⟹ local rlct = c/2) is ANALYTIC over ℝ and NOT in Mathlib — it must stay CITED. The ALGEBRAIC facts (conormal `I/I²` free of rank c at smooth points; regular local ring) are already banked or directly available via `free_cotangent`. So R2's Lean-provable part is the conormal-free / smooth-local-model statement, NOT an rlct value.
2. The number of defining equations of the fibre is `d_N·d_0` (the entries of `mult(A) − B`), which EXCEEDS the codim `c` in general. So "the c equations form a regular sequence" cannot literally mean the entry-functions; it means a minimal generating set / regular system of parameters of the LOCAL ideal at the smooth point. The c here is `height`, not `#generators`. Is this a real subtlety that the plan's wording hides?
3. Given `free_cotangent` (conormal free of rank c on a submersive chart) ALREADY in Mathlib, plus the banked regular-local-ring fact, is the Koszul/regular-sequence step even NEEDED for the geometric upper-bound, or is it only needed if one insists on literally exhibiting a length-c regular sequence (which `ringKrullDim_add_length_eq...` would then consume)?

## Questions
- Q1: Is my read #1 correct that the rlct=c/2 conclusion is irreducibly analytic and the ONLY Lean-provable R2 content is the algebraic smooth-local-model (conormal free rank c / regular local ring), which is essentially already banked? Or is there a legitimate purely-algebraic intermediate (e.g. "the local ring of the fibre at a smooth point, completed, is a power series ring, so the defining ideal is generated by a regular sequence of length c") that meaningfully de-risks the cited analytic step and is worth building?
- Q2: What is the SIZE of going from `free_cotangent` (Mathlib, on the abstract submersive chart) to a usable "conormal of the DLN fibre is free of rank c at a smooth point" statement? The DLN fibre is presented by minors / mult-entries, not as an abstract submersive presentation — is producing a submersive presentation at a smooth point a bounded chart-construction or a sub-wall? Note a banked "smooth point exists + standard-smooth chart" already underlies the regular-local-ring proof.
- Q3: Is there a cleaner route to "length-c regular sequence at the smooth point" that AVOIDS Koszul entirely — e.g. the regular local ring `A_m` has its maximal ideal generated by a regular system of parameters (dim-many), and the c fibre equations extend to such a system? Does Mathlib's regular-local-ring API expose a regular system of parameters?
- Q4: Anything I am missing — a Mathlib API, or a logical hole in the "[cited analytic] ∘ [proved algebraic]" factorisation for the UPPER bound specifically?

Answer Q1–Q4 directly, each with a one-line verdict then ≤4 lines of reasoning. Then one paragraph: the single biggest risk in this R2 plan.
