I am designing a thin, citable interface in singular learning theory (SLT) for a Lean formalisation. I want your independent, rigorous read on the LOWER-bound side of the real log-canonical threshold (RLCT), framed generally. Please reason from your own knowledge; do NOT just agree with a framing.

SETUP. Let F: X -> R be real analytic, nonneg, X a real-analytic manifold (think R^n). Define rlct_x(F) = sup{ s>0 : |F|^{-s} integrable near x }, and rlct(F) = inf_x rlct_x(F). For a sum of squares F = sum_i f_i^2 with common zero set V = F^{-1}(0), the universal Watanabe bound gives rlct(F) <= codim(V)/2. We are interested in when EQUALITY holds (the "mildly singular" case), proven via resolution of singularities (Hironaka): pull F back by a proper birational/analytic map pi so that pi*F and the Jacobian of pi are simultaneously locally monomial (normal crossings); then rlct = min_j (h_j+1)/(2 k_j) over exceptional divisors, where 2k_j = order of vanishing of pi*F along divisor j and h_j = order of vanishing of the Jacobian (pi*dvol) along divisor j (so the divisor contributes (h_j+1)/(2k_j)).

QUESTIONS — please be precise and separate what is standard/citable from what is subtle:

1. State as sharply as you can the LOWER-bound criterion: a clean sufficient hypothesis on a resolution (a normal-crossing / log-resolution datum, divisor multiplicities k_j, h_j) under which one concludes rlct(F) >= codim(V)/2. What exactly must hold of EVERY exceptional divisor (and the strict transform) for the per-divisor ratios (h_j+1)/(2k_j) to all be >= codim/2? Is "k_j = 1 and h_j = codim - 1 on the divisor over the smooth generic stratum" the relevant condition, and what controls the divisors sitting over the DEEPER (more singular) strata?

2. THE CRUX. What is the precise GEOMETRIC hypothesis on V's singularities (NOT on a chosen resolution) that guarantees such a good resolution exists and yields rlct >= codim/2? Candidates I want you to weigh: (a) V has only normal-crossing singularities; (b) V is a complete intersection cut out by a regular sequence (lci) with the singular strata themselves smooth of the expected codimension; (c) a Newton-polyhedron nondegeneracy condition (Varchenko/Kushnirenko) — the Newton polyhedron of F controls rlct and equals codim/2 when the principal face is "convenient"/the diagram meets the diagonal correctly; (d) F is "equimultiple" / the multiplicity of F along each stratum equals 2 (quadratic). For a sum of squares of a regular sequence, which of these is the right minimal sufficient condition, and is any of them actually NECESSARY?

3. Is it TRUE in general that a real analytic complete intersection (sum of squares of a regular sequence f_1..f_c) has rlct = c/2? Give the cleanest counterexample if FALSE (I suspect it can fail — e.g. f = x^2 - y^3 type, or a single non-quadratic equation), and pin exactly which extra hypothesis rescues it. I want to know whether "lci + codim c" alone forces rlct = c/2, or whether one genuinely needs control on the higher singularities.

4. Practical citation question: to assert "rlct >= codim/2" for a SPECIFIC variety in a paper, does one in practice (a) exhibit an explicit resolution and check all divisor ratios, (b) verify a Newton-polyhedron nondegeneracy condition, or (c) invoke a structural theorem? Which is the most citable as a clean lemma vs. which genuinely requires a bespoke resolution computation?

Be concrete with the multiplicity arithmetic (h_j+1)/(2k_j) and give counterexamples where the naive "codim/2" guess fails. Flag anything where my framing is subtly wrong.
