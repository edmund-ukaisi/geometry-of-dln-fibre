# Thread 01 — R0 interface design (scout: pen-and-paper + literature)

**Target:** pin the exact form of the three CITED analytic theorems (rlct-of-a-quadratic = c/2; Watanabe `rlct ≤ ½·codim` for a sum-of-squares; the resolution criterion "all exceptional divisors ≥ ½·codim ⟹ rlct ≥ ½·codim") AND the **precise geometric hypothesis** the lower-bound criterion consumes (Newton-polyhedron / toric-resolution / normal-crossing mildness datum) — so the DLN geometry can prove it. Output: the interface spec (a certificate; no Lean).

## Notes / read
