<task>
Lean 4 / Mathlib (v4.29) formalisation. Paper: Lehalleur–Rimányi "Geometry of the fibers of the
multiplication map of deep linear networks". Target payoff:
  rlct(square-Frobenius DLN loss with target B) = ½ · codim(mult⁻¹(B)),
the real-log-canonical-threshold = ½·geometric-codimension of the multiplication fibre (Aoyagi Thm 1
/ LR Thm 8.6). I am refactoring an existing "monolith" that CHEATS by citing
`rlct = ½·codim_K` directly (codim_K = geometric codim over an algebraically closed char-0 field K),
in ONE structure field. The operator wants to cite ONLY the analytic `rlct = ½·codim_ℝ` and PROVE
all the geometry up to it. So I must DEFINE an honest real codimension `codim_ℝ` of the real fibre.

THE LOAD-BEARING DECISION: how to define `codim_ℝ : Set (Tuple ℝ d) → ℕ∞` (or ℕ/ℚ) so that BOTH:
 (a) `rlct(loss) = ½·codim_ℝ(real fibre)` is the NATURAL Watanabe/Aoyagi citation (the cited analytic
     fact's RHS), AND
 (b) `codim_ℝ(real fibre) = codim_K(complex fibre)` is PROVABLE (the transfer T), ideally via the fact
     that each top-dimensional minimising orbit component has a RATIONAL (0/1 entries) point
     `realizerD m` ⟹ real points exist / are dense.
Watch the CIRCULARITY between (a) and (b): if codim_ℝ is defined via the COMPLEX codim, then (b) is
trivial but (a) is no longer an honest "real" statement (it hides the real↔complex passage inside the
definition); if codim_ℝ is the genuine REAL-LOCUS dimension, then (a) is the clean Watanabe citation
but (b) becomes the substantive transfer.

WHAT THE REPO ALREADY HAS (facts, not inference):
 - `codimRepCanonical (Z : Set (Tuple k d)) : ℕ∞ := Ideal.height (vanishingIdeal k (canonicalCoord '' Z))`
   — parametric in field `k`. Over K (alg-closed char 0) it is the geometric codim, and the full chain
   `codimRepCanonical (fibre K d B) = cCodim d r + shift = 2·(Aoyagi λ)` is PROVED.
 - `fibreGenIdeal d B : Ideal (MvPolynomial (RepCoord d) k) := Ideal.span {multPoly d r c − C (B r c)}`
   — the ideal generated by the residual polynomials. `multPoly d r c` are FIXED integer-coefficient
   polynomials (the generic matrix-product entries); only the constants `C (B r c)` depend on the field
   via B's entries. Over alg-closed k: `vanishingIdeal (image fibre) = (fibreGenIdeal).radical`
   (Nullstellensatz, PROVED), and `Ideal.height` is radical-invariant, so
   `codimRepCanonical (fibre k B) = height (fibreGenIdeal k B)`.
 - `Ideal.height : ℕ∞`, `RingEquiv.height_comap` available. `Matrix.rank_map_eq_of_injective` (rank
   preserved by injective field hom) PROVED.
 - The realizers `realizerD m` are built from 0/1 interval-direct-sum combinatorial data ⟹ literally
   rational ⟹ real points of the relevant components.

MATHLIB REALITY (verified by grep at v4.29):
 - HAS `Ideal.height` (Krull), `Ideal.primeHeight`, `minimalPrimes`, catenary-dimension API.
 - HAS Hausdorff dimension `dimH` / `MeasureTheory.dimH` (a real-metric dimension on subsets of ℝ^n).
 - NO semialgebraic dimension, NO real-Nullstellensatz, NO "real dimension of a real variety",
   NO "real points Zariski-dense ⟹ real dim = complex dim" bridge.

CANDIDATE DEFINITIONS for codim_ℝ (critique each for (a)-honesty, (b)-provability, Lean-feasibility):
 C-alg-ideal:  `codim_ℝ Z := height (fibreGenIdeal ℝ B)` (height of the SAME residual ideal over ℝ).
    Then (b) = "height of the residual ideal is stable under the field extension ℝ→K". Note ℝ→K is
    faithfully flat (field ext). Is `height (I·ℝ[x]) = height (I·K[x])` for I generated by polynomials
    with real coefficients, under a field extension — is this TRUE in general, and is there v4.29
    Mathlib API for height under flat/faithfully-flat base change of polynomial rings? (I doubt the
    base-change-height lemma exists; flag it.) Honesty of (a): does Watanabe's bound RHS = this
    algebraic ideal-codim, or the real-locus dimension? Be precise about what Watanabe's `λ ≤ d/2`
    universal bound's `d` actually is (codim of the analytic SET = real-analytic local dim, NOT the
    ideal's algebraic height in general — for x²+y² the real set is {0}, real codim 2, but the ideal
    (x²+y²) has height 1; so height(ideal) ≠ real-set-codim here!). Does this kill C-alg-ideal for (a)?
 C-vanishing-ℝ: `codim_ℝ Z := height (vanishingIdeal ℝ (image Z))` (height of the REAL-points vanishing
    ideal). For x²+y² this vanishingIdeal_ℝ({0}) = (x,y), height 2 = real codim — so (a) is honest. But
    (b): over non-alg-closed ℝ this can DIFFER from codim_K (the real locus can be lower-dim than the
    complex variety) — so (b) is FALSE in general and only holds for the DLN fibre via real-density.
    What's the cleanest provable form, and does it need the absent real-density bridge?
 C-Hausdorff: `codim_ℝ Z := (ambient) − dimH Z` using Mathlib `dimH`. (a) honest (real metric dim). (b)
    needs "dimH of a real algebraic set = complex Krull codim" — almost certainly absent + heavy.
 C-opaque-field: make codim_ℝ an OPAQUE FIELD of the interface (like the cited rlct), characterized
    only by what's needed, with T = `codim_ℝ(fibre) = codimRepCanonical_K(fibre)` a named hypothesis.
    Cleanest for green/sorry-free NOW, but does it abdicate the "define codim_ℝ honestly" mandate?

QUESTIONS (rank, be concrete + Lean-aware):
 1. Which candidate is the RIGHT honest codim_ℝ — one where (a) is the faithful Watanabe citation AND
    (b) is provable-or-cleanly-deferrable? Resolve the x²+y² tension: for the DLN fibre specifically,
    does the real-locus codim EQUAL the complex codim (so C-vanishing-ℝ's (b) holds via realizerD
    density), making C-vanishing-ℝ honest for BOTH? Or is the residual ideal RADICAL+real-reduced for
    DLNs so C-alg-ideal coincides with C-vanishing-ℝ?
 2. Is the transfer T (whichever def) provable IN ONE TIDE from realizerD rationality + Mathlib v4.29,
    or does it genuinely need the absent real-density / real-dim=complex-dim bridge? Give the PRECISE
    gap (the one missing lemma) and whether it's a "named hole hypothesis" (acceptable: T is the one
    permitted hole this tide) vs requiring new heavy machinery.
 3. If T must be a named hole, what is the cleanest honest STATEMENT of codim_ℝ + the T-hypothesis so
    that NOTHING is smuggled (the def is genuinely real, (a) is the honest citation, and T is the
    visible single hole)? Pseudo-Lean.
 4. Name=content / circularity traps specific to THIS choice — where could the def secretly encode the
    complex answer and make T vacuous, or make (a) an overclaim?
</task>

<output_contract>
Four sections:
 (1) DEF VERDICT — pick ONE candidate (or a refinement); one paragraph why; resolve the x²+y² tension
     for the DLN fibre explicitly (is real codim = complex codim here, and why).
 (2) TRANSFER T — provable in one tide? yes/no; the PRECISE missing lemma; named-hole vs heavy.
 (3) HONEST STATEMENTS — pseudo-Lean for the chosen codim_ℝ def + the T statement + how (a) attaches.
 (4) TRAPS — 1–3 circularity/name=content risks for this choice.
Terse. Pseudo-Lean over prose where a statement is the answer.
</output_contract>

<grounding_rules>
Distinguish DERIVED math facts (state as fact) from INFERENCE about the Lean codebase / Mathlib you
cannot see (flag explicitly). If a claim rests on a Mathlib v4.29 lemma you are unsure exists, say so;
do NOT invent confirmed lemma names. The x²+y² (ideal-height ≠ real-set-codim) point is a known math
fact — use it to discriminate, don't hand-wave it.
</grounding_rules>
