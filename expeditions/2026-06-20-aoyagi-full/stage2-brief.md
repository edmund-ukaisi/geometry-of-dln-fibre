# ★ STAGE 2 CHARTER (operator GO, 2026-07-09) — discharge `(□)`, then mint the unsuffixed headline

**READ THIS on every Stage-2 re-ground (also pointed from `loop-prompt.md`).** Binding charter for
Stage 2. Build-to-the-end charge; adapt autonomously inside the strategy; record route changes in the
ledger as I make them; surface ONLY genuine non-labour walls (decorrelated-confirmed, standing
decision 7). The operator reads the ledger; no sign-off needed for tactical reroutes.

## GOAL
∀ `M` nondegenerate, ∀ `c′ < minAdm M / 2` : `routeMLayerBoxIntegral M c′ 1 < ⊤`
(= discharge `(□) = RouteMBoxThresholdFinite`). Then `aoyagi_learning_coefficient` (unsuffixed,
UNCONDITIONAL) := `_gen ∘ the discharge`. **Standing invariant:** canonical stays 0-sorry / 0-axiom
(gaps only on tide branches); every landing AxCheck-gated clean-three.

## STRATEGY (firm suggestion; evidence in-repo)
Work **Aoyagi §5 natively**: the `(S,J)` recursion — coordinate radial blow-ups, det-1 unit clears,
absorption-by-renaming into the next factor, the `diag(b)` ledger as internal recursion state.
**Route-uniqueness compass** (own record: pure-vs-atom-adj verdict A; #83 seam NO; #70 front-peel NO;
the carrier stops; #59→#60): every refuted route shares one signature — **a determinant inverse in a
Jacobian**. Use it as a COMPASS, not a brake: when a step grows a det-inverse, that is the map saying
"re-express in native form (radial + unit clear + rename) and continue" — the native re-expression has
existed every single time. Reroute and keep building; note in the ledger.

## CAMPAIGN SHAPE (suggested, adapt freely)
- **Light consolidation** pass merging scattered design artifacts (outer-construction-cert,
  chart-lemma-probe, pure-vs-atom-adj, the UPDATE-668 plain-contract decision, STEP-0 artifacts) into
  one build cert — then charge; don't hold the build for it if the skeleton can start in parallel.
- **Early vertical slice:** the composed recursion END-TO-END on `(3,3,3,4)` corank 2 (cover → radial →
  clear → absorb → recurse → monomial endpoint → accounting = 7 = minAdm). All probes validated
  per-chart behaviour there; NONE has run the composed machine. Shake out the carrier before the
  width-general grind — or substitute a better first slice.
- **Statements-first skeleton** with an itemized tide-plan early (a checkpoint to EMIT, not a gate to
  wait at) — so the effort estimate gets a real answer while momentum builds.

## CASE 2 (the printed-paper typo)
Build the **prefix-min** form; land a **fidelity card** naming the printed-paper divergence (per the
captured Case-2 pitfall reference note: pivot vector in ACTUAL widths vs PREFIX-MIN increment,
discrepancy `(n_S − μ_S)(n_{S+1} − J)`). Named deviation, NOT silent. Likely de-risk to check en route:
the trusted-spine lemma (`2λ ≤ M⁽ⁱ⁾M⁽ʲ⁾` — Case-2 never binds) suggests exponent slack exactly in that
zone, so COARSE bounds may suffice where the printed text is unreliable — use it if it holds, build the
sharp form if not.

## CARRIER (discipline, from the velocity record)
Plain `sjJointResolution`-shaped contract; ledger / `SJState` as INTERNAL invariant, NEVER statement
decoration (the UPDATE-668 decision); termination via the captured `(S,J)` kernel reference note
(`reference-notes-sj-kernel.md`), taken through the reference-only protocol (re-derive, then adopt);
carrier file SINGLE-WRITER, incremental push, honest partials landing continuously.

## ESCALATION BAR (deliberately high)
Adapt THROUGH: exponent-bookkeeping surprises, carrier re-shapes, chart re-orderings, case-split
refactors, Case-2 sharpness — all labour, all mine. Surface to the operator ONLY: an obstruction that
survives (a) my own dissolve-or-confirm stress-test AND (b) a decorrelated check, AND (c) no native
re-expression routes around it — the genuine article (this expedition has met exactly ZERO after
adjudication). If one appears: isolate it as a minimal named lemma, keep building everything above it,
and say so in the ledger.

## SOURCES + METHOD
- Refer to **Aoyagi's paper OFTEN** (ONLY that paper — `paper-sources/…aoyagi…`; NOT other repo PDFs).
  Careful with math-heavy PDF→text extraction artifacts. Case-2 has typos; know the correction (above).
- **Pen-and-paper the math** to be correct AND Lean-friendly BEFORE/alongside formalisation, so the
  formaliser works smoothly (pen-and-paper scout produces a certificate → formaliser builds it).
- Reread dispositions (CLAUDE.md + bedrock/precision). Adapt. Go the distance. GLHF.
