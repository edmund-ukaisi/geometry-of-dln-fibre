# priorities.md — Aoyagi-Full taste ledger

Controller proposes (VOI × directed-suspicion); operator edits directly (highest-authority signal).

**★ NOW (2026-07-15 LATE-14 — WHOLE DEEP ATLAS COMPLETE; NEXT = tide D rlct realization) — the entire deep-atlas geometric construction is formalised, sorry-free clean-three, banked @b458bb430 (genm-deepatlas): Tide A (reviewed 8/8) + (α) codim=minAdmRec (reviewed) + Tide B [leaf + general-pivot bridge + chain descent + §3.1 CR-tree index + §3.2 coverage set-equality + §3.3 gluing]. The substantial geometric construction the native route needed is DONE (a design flaw self-caught+fixed mid-build — the invariant doesn't survive descent → invariant-free redesign; guards all held). (□) TRUE (no wall), fork NATIVE. Strategy Route A → (□) → mint #108 UNCHANGED.** **LIVE:** deepcov-rev (coverage/gluing fidelity, in flight) + seamrlct (STANDBY, tide-D coordinate consults); deepatlasA standing by for reviewer findings then released. **NEXT (substantive) = TIDE D: per-cell rlct realization** — discharge the coverage's per-cell finiteness hyp `hfin` via seamrlct's NATIVE result (unit-Jac Schur charts ⟹ rlct=codim/2 by the elementary calculus). DESIGN-FIRST (charter); KILL guard = atomic unit-Jac coords + CARRY |det J| (seamrlct standby); ℝ/ℂ codim-source probe (compute ℝ codim from chart block dims directly — field-independent — not importing (α)'s ℂ codim; confirm at design). Then LINK → re-route `deeperFlag_shell_le` → coherent-unit integration (cherry-pick genm-deepatlas + genm-crstrat + genm-sj5-stepbuild) → waist → mint #108 (monomial_rlct-only). DONE+REVIEWED+banked: codim/Nat gate (stepbuild+crstrat), whole deep atlas. All bounded. Full detail = synthesis.md LATE-14 + endgame-lanes.md TIDE B COMPLETE.
**[SUPERSEDED LATE-14]** ~~★ NOW (2026-07-15 LATE-13 — FORK RESOLVED: NATIVE; endgame = bounded geometric labour, no open math) — seamrlct's fractional-gate follow-on (decorrelated Codex + verify_codex.py) CLOSED the native route: the odd-cycle "deficit" was a COORDINATE ARTIFACT (rlct is [T-inv]-invariant; the unweighted vertex-cover formula in radial coords dropped the |y| Jacobian). In the atlas's UNIT-JACOBIAN Schur charts (tide A) the seam incidence is bipartite ⟹ rlct=codim/2 via the elementary calculus, NATIVE (monomial_rlct-only, cited_aoyagi_dln dropped). (□) TRUE (no wall) — core risk RETIRED. Strategy Route A → (□) → mint #108 UNCHANGED.** DECISION = NATIVE (charter's native mandate + #5 + achievability + maximal-push; RLCT-runway "future" framing subordinate; surfaced w/ redirect-to-cited option in discuss-at-close). **LIVE (2 lanes + 1 standby):** deepatlasA (β UN-HELD → TIDE B: unit-Jac atlas completeness + index §3.1 + gluing §3.3 + pinned reindex; incremental) + deepcodim-rev ((α) fidelity, in flight) + seamrlct (STANDBY for tide-D coordinate consults). **tide-D KILL guard (baked into tide B): ATOMIC unit-Jac Schur coords + CARRY |det J|, never radialize-and-drop-it.** DONE+REVIEWED(-ish)+banked on origin: codim/Nat gate (stepbuild+crstrat), tide-A charts (SURVIVED 8/8), (α) codim=minAdmRec @7b006cb95. AFTER: tide D (per-cell rlct=codim/2 + gluing) → LINK → re-route `deeperFlag_shell_le` → coherent-unit integration → waist → mint #108. All bounded. Full detail = synthesis.md LATE-13 + endgame-lanes.md FORK RESOLVED NATIVE.
**[SUPERSEDED LATE-13]** ~~★ NOW (2026-07-15 LATE-11 — SEAM DE-RISK: NO WALL, (□) core risk RETIRED; only-live-question = the DISCHARGE's native-vs-cited AXIOM footprint) — seamrlct (decorrelated + Codex xhigh) proved the wall CANNOT EXIST: (□) is TRUE (K homogeneous ⟹ rlct_0=rlct=minAdm/2 [cited Aoyagi]=inf_x rlct_x ⟹ every rlct_x≥minAdm/2 ⟹ box finite <minAdm/2). Single-peel FE seam benign+native (x²+y⁴ refuted, biquadratic); binding stratum k=1 charge-inert Morse-Bott ⟹ rlct=C_1/2 NATIVE; DDᵀ charge bounded factor. Strategy Route A → (□) → mint #108 UNCHANGED.** THE ONE LIVE KNOWING-DECISION: the mint's AXIOM FOOTPRINT — closing criterion #5 wants only `monomial_rlct`, but support-1's no-wall proof cites Aoyagi; the deeper-strata rlct LOWER BOUND (≥ (minAdm−ab)/2, WEAKER than the =codim/2 open conjecture) is the native-vs-cited fork. **LIVE (3 lanes):** seamrlct RE-TASKED (sharper Q: is the deeper-strata native lower bound achievable via complete rank stratification + biquadratic Morse-Bott? → decides native-only vs cite-Aoyagi) + deepatlasA (tide B: index §3.1 + coverage-completeness §3.2 [discharges crstrat Finding B(2)] + gluing §3.3; reindex pinned here) + deepatlasA-rev (tide-A fidelity). Tide D HELD on the sharper Q (WALL question CLOSED). DONE+REVIEWED: codim/Nat gate (stepbuild 4 modules + crstrat (I) @66a54b428), steps 1+2, exponent gate, hGae, tide-A charts. AFTER: tide D (rlct realization) + LINK/density/front-gluing → re-route `deeperFlag_shell_le` → coherent-unit integration → waist → #108. Full detail = synthesis.md LATE-11 + endgame-lanes.md SEAMRLCT VERDICT + discuss-at-close LATE-11.
**[SUPERSEDED LATE-11]** ~~★ NOW (2026-07-15 LATE-10 — CODIM/Nat gate COMPLETE + reviewed; RLCT-level seam obligation REOPENED (decorrelated), headline QUALIFIED, de-risking) — the Route-B CODIM gate is DONE (stepbuild's γ_s+(II)+branches [reviewed PASS] + crstrat's (I) `minAdm_le_compositeRank_add` @b5af33f2e [clean-three], zero-bridge). BUT deepgate's BOUNDED verdict was CODIM-level (C_k≥minAdm−ab); (□)-finiteness needs the RLCT level (rlct=C_k/2), and codim=CR does NOT auto-give rlct=CR/2 at the telescoping F·E seams (Codex x²+y⁴). NO CONFIRMED WALL; ONE decorrelated-flagged, expected-benign ((2,2,2) rlct=3/2=CR/2; native lower-bound of cited Aoyagi), un-discharged RLCT obligation — de-risking. Strategy Route A → (□) → mint #108 UNCHANGED.** SEQUENCING (charter: pen-and-paper math BEFORE formalising): A–C charts' math VERIFIED (0-mismatch) ⟹ formalise now; tide-D rlct math OPEN ⟹ de-risk first. **LIVE (2 lanes, parallel):** `seamrlct` (pen-and-paper §4.4 one-peel local-zeta de-risk — is the seam quadratic-nondegenerate per component → rlct=CR/2, uniform over F·E/pivot-collapse seams + coupled charge R=DDᵀ? KILL = binding stratum rlct<C_k/2 ⟹ (□) FALSE at some c′ ⟹ WALL; verify (2,2,2)/(2,2,2,2)/(2,3,3,3)) + `deepatlasA` (lean-formaliser, tide-A geometric charts: per-level Schur big-cell + product-layer reduction unit-Jac CoV; HOLD tide D; STOP if A–C entangles with the seam). DONE+REVIEWED (unaffected): steps 1+2, exponent gate, FULL hGae, stepbuild's 4 modules, crstrat's (I). Then (pending de-risk benign): tide B (index/coverage/gluing) + tide D (rlct gate) + LINK/density/front-gluing → re-route `deeperFlag_shell_le` → coherent-unit integration → waist → #108. Full detail = synthesis.md LATE-10 + endgame-lanes.md RLCT-LEVEL REOPENING + discuss-at-close LATE-10.
**[SUPERSEDED LATE-10]** ~~★ NOW (2026-07-15 LATE-9 — deep-stratum GATE (Nat part) GREEN + banked; (□) still has NO KNOWN WALL)~~ — Route-B deep-stratum gate's Nat part landed (stepbuild, `RouteMSJDeepGate.lean` @bbb274da9, sorry-free clean-three); Nat gate remaining = only crstrat's (I). Strategy Route A → (□) → mint #108 UNCHANGED.** GATE DELIVERED: γ_s (`chargeExp`) + γ_s=0/γ_0=ab + arity-3 QIP + **(II) `minAdm ![M₀,M₁,s]+γ_s ≤ ab+us` (2412/0)** + `deepGate_branch`(C_k≥minAdm−ab, consumes (I)) + `deepGate_uρ_branch`. **CR = ABSTRACT-PARAM (κ abstract ℕ, gate takes (I) as hyp) ⟹ ZERO CRhead=CRlast bridge by construction**; crstrat's head-peel `CRrec` is canonical (defeq minAdm at s=0, head==last 65k/0), its (I) must land as `minAdm M ≤ CRrec(deepchain)(ρ−k)+minAdm ![M 0,M 1,ρ−k]` (exact shape relayed). FIDELITY REVIEW commissioned (Codex-decorrelated: γ_s A_cor-integral-charge fidelity + (II) casework + branch (I)-consumption). **LIVE (3 lanes):** crstrat (discharges (I), in flight) + stepbuild (post-gate: deep atlas hand-off + per-stratum radial gate + LINK/density/front-gluing) + deepatlas-design (the DEEP stratified-resolution atlas design → then a MULTI-TIDE formaliser build; the one substantial remaining piece, bounded). BANKED+REVIEWED: steps 1+2, exponent gate, FULL hGae ((c1) done+reviewed + (c2/c3) + B4), stepbuild's 3 route-agnostic modules (fidelity-passed), Brick F closed, mint composition type-checks, deep-stratum gate Nat part. AFTER: LINK/density/front-gluing → re-route `deeperFlag_shell_le` (hbind + hGae + hEtopae) → coherent-unit integration (cherry-pick genm-sj5-{c1,stepbuild,crstrat CR}+deep-atlas onto @12a7ae38a) → waist reversal-wiring → #108. Full detail = synthesis.md LATE-9 + endgame-lanes.md DEEP-STRATUM GATE LANDED.
**[SUPERSEDED LATE-9]** ~~★ NOW (2026-07-15 LATE-8 — (□) has NO KNOWN WALL; Route B decomposed into a bounded-labour finish)~~ — every soundness/scope question (b<j, j=r, general-L rank, step-3 completeness, deep-stratum det-charge) RESOLVED POSITIVELY; (□) = coupled INCIDENCE-DIRECT for `deeperFlag_shell_le` + bounded labour. Step 3 (CoV-heavy finiteness) via **ROUTE B** (direct per-stratum RLCT gate C_k≥2T1_q; deepgate BOUNDED, no gap — charge~k dominated by deep codim κ_k~k²). 3 lanes: stepbuild (`RouteMSJDeepGate`) + crstrat ((I)) + deepatlas-design (atlas). Full detail = synthesis.md LATE-8 + endgame-lanes.md DEEPGATE RESOLVED / ROUTE-B DECOMPOSITION.
**[SUPERSEDED LATE-8]** ~~★ NOW (LATE-5 — route verified incidence-direct; general-L gate pinning)~~ — Route B DEAD (type error); the descent is GENUINE + non-circular (recursion identity minAdm(M) ≤ ab+minAdm(redChain u M), 0/75411). BYPASS `headSplit_domination` (route B) + L1 decoupled core.** Capstone base @f483692ef VERIFIED (Brick F closed, 5 charts wired machine-checked clean-three, mint composition type-checks). **gaugelift** PINNING the general-L exponent-gate identity (brick B, routeverify's least-verified piece) before the capstone tide. Remaining bricks: (A) top-level assembly [capstone tide, largest — wire orphaned charts into deeperFlag_shell_le], (B) gaugelift, (C) Lean coverage/atlas, (D) Brick F likely droppable, (E) j=0/j=r bases kept. WAIST branch = connector-wired (deeperFlagWaist_finite sorry-free, reduces via reversal to good-(d)), NOT wide-open. VM restart: reoriented CLEAN (main==origin, all work on origin). Capstone tide HELD pending gaugelift. Full detail = synthesis.md LATE-5 FRONTIER + endgame-lanes.md ROUTEVERIFY RESOLVED.
**[SUPERSEDED 2026-07-15 LATE-5]** ~~★ NOW (LATE — b<j cleared; 1 brick; SD-7 satcover)~~ — [route re-verified: route B was a dead detour; incidence-direct is the route; SD-7 unifies as the ab=0 corner but the whole is proven via the incidence route not the separate D-A assembly] bltj CLEARED the b<j gate (outcome-(b) ABSORBED, (□) sound as routed, NO (v) reshape, C_{ℓ,s} enumerates Q_p-degeneration ⟹ exponent-collapse vindicated; decorrelated + rigorous subset no-go). Crux superseded (shell-0, off-path); DecoratedStep (e)/(c)/(d) done-or-impl'd, (b)-modulo-SD-7, (a)=Brick D; mint `_prestage` sorry-free modulo hDescent. BUILD: chart-4 DONE hole-free; chart-5 in flight (chart5, block-LU core banked); (iii)+(v) UNGATED for 1≤j<r (brickdcont, dependency-inverted on chart5, hcT-corrected target). ★ LIVE GATES (handled, NOT walls): (1) **satcover** — does Brick D cover j=r at min(a,b)=0 or need the arity-IH? (the LAST coverage question, decorrelated — this shape has broken 4×); (2) **hcT LANDMINE** — Brick D built against brickdfin @9c748b6cd corrected statement (canonical @544 is wrong-statement, missing hcT), merged at integration; (3) constructive per-stratum K_{ℓ,s}<⊤ = (v) LABOUR (subset no-go already gives finiteness). Lanes: brickdcont((iii)/(v) 1≤j<r) | chart5(big-cell) | satcover(j=r gate). Full detail = synthesis.md 2026-07-15 LATE FRONTIER + endgame-lanes.md ENDGAME SORRY-MAP.
**[SUPERSEDED 2026-07-15 LATE — b<j gate cleared]** ~~★ NOW (2026-07-15 BRICK-D BUILDING + b<j SOUNDNESS GATE)~~ Strategy (Route A DecoratedDescent → (□) → mint #108) UNCHANGED. The terminal incidence estimate was PROVEN (incidencepp: joint (z,A_cor,front) resolution + explicit determinantal atlas §3b) and Brick D is being formalised: **piece (i) `transverseSchurGram` + projection chain LANDED** clean-three @genm-sj5-brickdcont 1c6298148; the **exponent-gate COLLAPSED to a `ring` identity** `C_{ℓ,s}=(M₀−s)(M₁−s)+s·M₂−ab` (incidencepp §3, brickdbuild-decorrelated — decide-over-332 retired). brickdcont building **(iv)** in standalone `RouteMSJIncidenceExponent.lean` (arithmetic core SAFE; the aggregate `min=T1` = the coverage under test). **★ THE GATE:** operator flagged (2026-07-14) that on **b<j** cuts (r≥3, upper half of the j-range) the shell forces j−b weak singular values into **Q_p** (pivot block) — a locus the (ℓ,s) charts may NOT index; probe M=(3,3,7),u=2,j=2,b=1 may drop the local threshold 4.5→3 as σ_min(Q_p)→0 while decLoss stays O(1) (⟹ domination false there + contradicts the collapse's min). Three outcomes: missed (ℓ,s) stratum [OBSTRUCTION → reshape (v)] / absorbed-by-a-CoV [witness] / charged-by-another-term. **(v) statement-shape + (iv) aggregate canonical-wiring are GATED on bltj.** PARALLEL: **rvprep** pre-stages the mechanical rendezvous (turns post-(□) into a MERGE). Lanes mid-flight — re-ground and let them run; bltj's verdict is the next decision. Full detail = synthesis.md 2026-07-15 FRONTIER + endgame-lanes.md ⚠SOUNDNESS GATE + BRICK D BUILD tracker + discuss-at-close.

Nothing unranked; "unclear-but-keep-going" is first-class. **Refreshed 2026-07-11 (late) — STAGE 2 (A)-BUILD;
(□) via the DECORATED (S,J) DESCENT (Route A, coupled diag(b) ledger). ★ The spine RE-POINTED (□) onto the new
sole contract `DecoratedDescent = ∃ adm, (trivial admissible) ∧ DecoratedStepHyp adm ∧ DecoratedBaseHyp adm`
(the plain-IH `DecoratedPeelStep` was UNPROVABLE/Q2 → marked dead; `routeMBoxThresholdFinite_of_decoratedDescent`
is the live driver, clean-three @b142a966). Q_D CERTIFIED no-obstruction (#142, both anchors, exact toric-ray);
the peel-closure invariant + the (i) incidence are NATIVE/α (`minAdm_eq_backPeel` re-expression — #97 HONORED,
NO cite; the last Proved-vs-Cited fork RESOLVED to Prove, #114). **6 clean-three modules banked** (spine +
measure bedrock RouteMSJInnerDescent + invariant-(ii) RouteMSJTransversality + (P)/(T)-guard RouteMSJAdmEncoding
+ Rayleigh core RouteMSJLeafRayleigh + leaf finiteness RouteMSJLeafFinite). DecoratedBaseHyp's LOSS part DONE by
proof (`corankLeaf_rpow_lt_top`, route A, bypasses sjLoss_terminal + (P)/(T)). **REMAINING:** the invariant α-build
(`minAdm_eq_backPeel` + chain → rank≥b) + adm def + wire corankLeaf into DecoratedBaseHyp [mostly landing] →
**`DecoratedStepHyp` — THE remaining substantial analytic core** (the decorated peel + the intersection COUPLED
estimate = deepest soundness; NOT a codim freebie per cover's x²(x²+y^{2N}) correction) → `DecoratedDescent` →
(□) → mint #108. BUILD-TO-THE-END; bounded labour, no wall. Decorrelated adversarial hunts caught 5 errors on
paper (atom-first / plain-hIH / decoration / full-rank / higher-codim-slack).**
**★ ROUTE-I SOUNDNESS FULLY FORMALISED (2026-07-12) — NON-critical-path but banked.** The codim SOUNDNESS
is now machine-checked, native: Part 1 `minAdm` PERMUTATION-INVARIANCE (`minAdm_comp_perm`/`_comp_sort`,
@da4d264f) + Part 2 `minAdm = cCodim` ∀ width (`minAdm_eq_cCodim`, @c25b0b73) — both clean-three
(`[propext, Classical.choice, Quot.sound]`) + cover decorrelated fidelity-PASS (Codex-concurred). The
route-i (□)-soundness is now a theorem MODULO cited Aoyagi (RLCT=½·cCodim). This is route-i cite-completion,
NOT the #97 mandate. **THE CRITICAL PATH is unchanged:** the route-ii NATIVE (□) discharge = `DecoratedDescent`
(#4 `DecoratedBaseHyp` base → #5 `DecoratedStepHyp` step). **#4 awaits a fresh engagement** (desc4 preferred;
a fresh tide is a poor substitute for the fidelity-critical base invariant — held fixed-Z checkpoint uncommitted);
#5 gated on #4; then `DecoratedDescent` → (□) → mint #108.
Live integrative read: `synthesis.md` (UPDATE-971 newest); operator-review items: `discuss-at-close.md` (#97–#122).

## ★★★ RECALIBRATION 2026-07-13 (T-Obl3b mountain: corrected deeper-cut route, wall cleared) — NEWEST state
The (□) discharge = `DecoratedDescent` (#4 base `DecoratedBaseHyp` DONE/banked; #5 step `DecoratedStepHyp` = THE mountain).
**#5 = the T-Obl3b off-sector shell-stratification mountain.** ROUTE CORRECTED 2026-07-13 (synthesis UPDATE-1002/1003):
- **§6's bordered-Gram / strong-minor-Jacobian corner-shrink primitive was REFUTED** (pen-and-paper pin a7e939de + decorrelated Codex, anchor (3,3,3)@t★=1,j=1: divergent non-sequitur, 3 exact facts). `tobl3b-cornershift-chart-cert.md`.
- **CORRECTED mechanism = deeper-cut (`t★+j`) re-peel + banked PSD-monotonicity** (`det_le_det_of_posSemidef_sub`, generalising `uniformWenn_le` from 1 to the strong projection), landing the strictly-convergent reduced weight `(b−j,M₂−j,a−j)` via `detGram_lintegral_box_lt_top`. Bordered-Gram is DIAGNOSTIC, not load-bearing.
- **WALL-CHECK CLEARED (a5cc889, decorrelated Codex 99%):** the (S,J) T-peel spine ADMITS peeling at `t★+j` per shell (`redChain` drops 1 layer ∀cut; `DecoratedStepHyp`'s IH is universal over one-shorter chains; freed `Γ` live) — NO measure-disintegration wall. `tobl3b-deepercut-admissibility-cert.md`. **Mountain = pure labour to the end.**
- **LANDED clean-three (canonical @500a054a):** M1 `borderedGram_det` (diagnostic) + M2 `RouteMSJShellCover` (LOAD-BEARING — stratify-Z-first) + M3 `gramDet_eq_prod` (diagnostic) + OWED-1 `uniformWenn_proj_le` (PSD-mono weak-elim) + OWED-2 crux `exists_ortho_ext` (orthogonal extension) + `detGram_eq_prod_rows`/`offSector_cover_le`. All `[propext, Classical.choice, Quot.sound]`.
- **IN FLIGHT (fresh sub-tide a5d58f3b):** OWED-2 reduction `strongBlock_lintegral_lt_top` (measure plumbing: `exists_ortho_ext`→CoV→column-Fubini→`detGram_lintegral_box_lt_top`) + OWED-3 `deeperFlag_shell_le` (DECORATED `DecoratedStepHyp adm` assembly, NOT the dead plain-IH hole; chain: freed-corner peel→OWED-1→OWED-2→drop residual→`cornerComparator_adm`; cover/shell-0/saturated banked).
- **THEN:** the compose (T-good good-sector + T-peel spine + T-step) — DESIGNED — → `DecoratedStepHyp` → `DecoratedDescent` → (□) → mint unsuffixed `aoyagi_learning_coefficient` (#108).
- **Process win:** the pen-and-paper-before-formalise pin caught a WRONG divergent primitive before the tide built (iii) on it (9th consecutive pre-build catch); the potential wall it exposed was decorrelated-cleared (labour). discuss#127/#128.
- Route-i soundness (`minAdm=cCodim` ∀width + perm-invariance) FULLY FORMALISED, clean-three, cover-PASS'd — banked, NON-critical-path (route-i cite-completion, not the #97 native mandate).

## ★★★ RECALIBRATION 2026-07-12 (post-independent-hunt, UPDATE-961 / discuss #120) — superseded by 2026-07-13 above
The (□) discharge splits into **#4 (base leg, DecoratedBaseHyp)** + **#5 (step, DecoratedStepHyp = off-sector)**.
- **#4 base leg** — fixed-Z `decoratedBaseHyp_faithful` GREEN/axiom-clean (base stepping-stone); the carried adm needs the z-dependent `Z_tail(r)` generalization (cover Q1: fixed-Z #5-unpreservable — decorrelated-confirmed). desc4-scoped 4-piece plan banked (UPDATE-960); desc4 executes paced (its timing). INDEPENDENT of the off-sector route. Held uncommitted (not in canonical build).
- **#5 off-sector — ★ §7 "airtight" REFUTED by the independent hunt (UPDATE-961).** cover's naive-corner arity-recursion (§7) is NOT an airtight proof: dichotomy (C) is a category error (Eckart–Young = matrix-distance ≠ parameter-order; single matrix can be order-2); charges-ADD is units-only; corank-≥2 SHARED-DIVISOR COLLAPSE (R1-proven, binds at (3,3,4)) is missed. **PIVOT: the off-sector PROOF routes through Aoyagi's coupled `diag(b)`/symbolic-divisor-support recursion (= task #122)** — the R1 thread's decided route (banked-adjacent: `verify-r1-diagb-334.md`), NOT §7's corner. The saturation/integrate-Γ-first joint estimate (UPDATE-954) + the value ½minAdm are NOT refuted.
- **★ SOUNDNESS DE-RISK — ✓ SETTLED (UPDATE-964/965, ★★★).** The general-width QIP no-collapse is RESOLVED POSITIVELY, both dialectic-sides converged: **PROOF** (cover, UPDATE-964) — minAdm PERMUTATION-INVARIANCE `minAdm(M)=minAdm(σ·M)` (induction via adjacent transpositions; crux `g(a,b,c)=g(a,c,b)` via exact difference-of-squares + elementary interior⟺triangle-ineq + non-interior=ab; controller-spot-verified), closing (A) minAdm=cCodim ∀ width; **REFUTATION** (decorrelated hunt a0667acb, UPDATE-965, held independent) — NO COLLAPSE constructible + positive mechanism "sharing RAISES never lowers the ratio" (shared-radial Jacobian r^{#shared} makes charges ADD → ½Mval; §7 "collapse" was naive OVER-counting). **⟹ (□)-soundness a THEOREM MODULO AOYAGI ∀ width, no collapse.** The sole residual = the resolution-EXHAUSTIVENESS (cited-Aoyagi) = exactly what the native descent (route ii, #97) BUILDS.
- **PHASE NOW = FORMALISATION of a now-SOUND target (no wall, no soundness risk).** Sequential: **#4** z-dependent generalization (desc4, fresh, self-timed) FINALIZES the FaithfulSJAt shape → **#5** off-sector = the decorated descent (route B / coupled diag(b), carrier carries supp+coeff = diag(b) support; cover §6″ plan; GATED on #4's shape) → assembly → (□) → mint #108. Both fidelity-critical (base audited hardest; descent = the substantial analytic core) — FRESH engagements, no session-tail rush; clean plans banked (#4 UPDATE-960, #5 §6″/@3fad7eee).
- Disposition note: the §7-refutation arc landed FULLY POSITIVE — independent-hunt caught the premature "§7 airtight" → honest reconciliation (pivot to route B / decorated descent) → localized to (A) → PROVED (A) (perm-invariance) → decorrelated-CONFIRMED no collapse. The bedrock refutation-dialectic (prove ‖ refute → converge) worked as designed; a scary residual resolved into a settled, de-risked soundness result.

## ★ STATE ANCHOR — Stage 1 COMPLETE (S2-free), Stage 2 = BUILD-discharge `(□)` via native (S,J)
Stage 1 delivered the fully-general Aoyagi learning coefficient (honest Lean 4 + Mathlib, BEDROCK-OK,
clean-three, **zero axiom declarations**), on `expedition/aoyagi-full` (draft PR #26 → dev):
`aoyagi_learning_coefficient_gen` (general L≥2, conditional on `(□) = RouteMBoxThresholdFinite (H−r)`
ALONE) + the UNCONDITIONAL `_gen_le` / `aoyagi_deepest_reduction_gen` / `_L2`. **Stage 2 discharges
`(□)`** → `_gen` unconditional → the (KEPT) unsuffixed `aoyagi_learning_coefficient` re-points to the
honest fully-general result. **OPERATOR (A) DECISION (#97, 2026-07-10, BINDING): BUILD the (S,J) atom
from scratch, do NOT cite. Route-search around the (S,J)/determinantal core is CLOSED** (cornrev
equivalence cert). LABOUR on established math (Aoyagi §5); the bar for "wall" is HIGH.

## Now (ranked)

1. **★ [STAGE 2 · ACTIVE GOAL] Discharge `(□)` via the DECORATED (S,J) DESCENT (Route A) — prove `DecoratedDescent`.**
   `(□)`-gate = `routeMBoxThresholdFinite_of_decoratedDescent : DecoratedDescent → ∀M (□)` (clean-three @b142a966,
   RouteMSJDecoratedRec). `DecoratedDescent = ∃ adm, (trivial admissible) ∧ DecoratedStepHyp adm ∧ DecoratedBaseHyp adm`
   (adm-abstract decorated recursion; the plain-IH `DecoratedPeelStep` is DEAD — Q2: peel emits `[reduced]·[H⁻⁴]`, plain
   IH can't carry it). Controller bedrock/vacuity review PASS (non-vacuous: ∀D→false base, trivial-only→unprovable step;
   forces the peel-closed p=0-transversality family). **ROUTE CONFIRMED (#140):** front-first g(Q)/qPeelIntegral is
   FAITHFULNESS-CONTESTED (shared-A₂, disjoint-A₂ FALSE); `#123`/Cauchy–Binet a TOOL not the closer.
   - **DONE / banked (clean-three):** the spine (`DecoratedDescent` + driver); measure bedrock `RouteMSJInnerDescent`;
     `DecoratedBaseHyp` LOSS part = `corankLeaf_rpow_lt_top` (route A, `RouteMSJLeafFinite`+`_Rayleigh`, `∫(frobSq ΓZ)^{−c'}<⊤`
     at ½minAdm(base) given the `Z·Zᵀ≽c·I` units interface); invariant-(ii) `RouteMSJTransversality`; (P)/(T)-guard +
     regression-test-2 `RouteMSJAdmEncoding`; **the A₂ α-unlock `RouteMSJBackPeel`** (`minAdm_eq_backPeel` re-expressed as
     the banked front-peel #117 + convexity ⟹ co-minimizing ρ≥b, @c90dcab5, cover PASS). Q_D CERTIFIED (#142); invariant
     width-general CERTIFIED (#144).
   - **★ adm-FORK RESOLVED → (B) valuation-predicate (UPDATE-936, cert §10/§10.1).** #144 peel-closes the VALUATION predicate
     (not just the reachable set), so #3 (adm def) DECOUPLES from #5 (peelOp). **FINAL #3 adm def:**
     `adm M D := genuineCarrier D ∧ (a=0 ∨ b=0 ∨ ∀η∈Crit D, ⨅_j ν_η(corankGen D j)=0)`, Crit incl. intersection rays (C1).
     **genuineCarrier LOCKED (i)** (cover-decided, corrected the earlier 2-drop + my (ii)): `∃ e : D.Z ≃ᵐ Params M, ctx traces
     prod M ∧ dom = e⁻¹box` — stores the FULL Params M; the deeper TAIL (#144/#2/leaf consume it) is DERIVED from A=e z, not
     stored (dissolves the trivial-0-drop tension). Pins M's EXACT chain (LOAD-BEARING; ν/ζ aligned via hν▸, HEq-free). LANDED @0ba81dcb.
   - **#2-core `corank_survival_ae` LANDED @ef8a1528 (clean-three; controller bedrock + cover DECORRELATED fidelity PASS).**
     AG-free (free-var-A genericity, single b×b minor-cut null set, Zdeep.rank a hypothesis — NO component-decomposition;
     Codex "AG wall avoided: YES", #114 does not recur). `RouteMSJCorankSurvival` (corank_survival_ae + ae_matrix_eval_ne_zero,
     both `[propext,Classical.choice,Quot.sound]`), wired into aggregator+AxCheck.
   - **LANDED clean-three (genm-sj5-desc3):** units bridge #147 (`RouteMSJUnitsBridge` @cab04ba5, eigenvalue-free Loewner) +
     #3 adm def (B) #148 (`RouteMSJAdm` @0ba81dcb: genuineCarrier (i) + admValuation=`d=0 ∨ pSimultaneous` + htriv PROVED;
     cover DECORRELATED fidelity-PASS). DecoratedDescent witness typechecks; only #4 (hbase) + #5 (hstep) OPEN.
   - **#4 #149 `DecoratedBaseHyp` — SETTLED (S2 resolved UNIFORM, UPDATE-952); desc4 green-lit to COMMIT + wire the clean invariant.**
     OPT-A: adm carries `FaithfulSJAt` (the (S,J) resolution-state invariant, a **general-arity LOOP INVARIANT**; #4 closes it at width-2).
     **★ S2 SETTLED + VERIFIED (cover, NEUTRAL Codex + peel-buildplan:188, DERIVING forced-uniform; faithfulsj §★★★/§★★★★): #4's width-2
     base is UNIFORM/SPANNING** — route-A `corankLeaf(Z_tail=I)` closes #4 at c'<½M₀M₁=½minAdm, **#5-INDEPENDENT AS A LEMMA** (given adm, closes).
     **anisotropic-leaf fallback DISCHARGED.** cover CORRECTED Codex's false uniformity mechanism (radial scales the FRONT A₀=u·V₀, not the tail;
     true mechanism = coranks integrated OUT peel-by-peel via `freedSchurLoss`). **★ PRECISION FIX (UPDATE-953, corrects my earlier conflation):**
     `genuineCarrier` gives SPANNING (residuals read prod M); **δ≡0 (`residualSupport≡0`) is a SEPARATE SUPPORT property, NOT a genuineCarrier
     consequence** (counterexample: residuals span with one δ>0, dropping it leaves rank 1) — so **δ≡0 MUST be a CARRIED clause** #4 consumes,
     #5 establishes. **INVARIANT (converged):** carry {genuineCarrier + provenance `res_i=[Γ·Z_tail]_{ρ(i)}` (WeightedLeafForm) + α + β +
     `minAdm≤a·n` + `residualSupport≡0`}; the WEIGHTED identity is DERIVED (`decLoss_commonDivisor_factor`, banked; δ = banked `residualSupport`).
     #4 CONSUMES δ≡0 → δ=0 subfamily = all gens → spans → clean frobSq(Γ) → `decoratedBase_routeA_of_leafForm(Z=I)`. The honest chain:
     genuineCarrier→spanning; carried δ≡0 [faithful #5]→δ=0 subfamily spans; together→clean bound. dim clause `minAdm≤a·n` (width-general, cover Q1
     CONFIRMED: t=0 cut). **desc4 GREEN-LIT** (4th-flip guard LIFTED): COMMIT {WeightedLeafForm + residualSupport≡0 + d=0 observable} into RouteMSJAdm
     + wire #4; `RouteMSJBaseHyp.lean` (6+ thms green, axiom-clean) substrate. cover audits committed build → I integrate one-shot.
     Recalibration: discuss-at-close #115/#116/#117. Process lesson (UPDATE-945/948/950/953): accept only what's PROVEN, gate fidelity clauses on
     the decorrelated verdict — the audit gate caught my genuineCarrier→δ≡0 conflation BEFORE desc4 committed (no wrong build; 2 wrong-γ, clean-frobSq GO, Codex's false mechanism all pre-empted).
   - **★ THE REMAINING SUBSTANTIAL CORE = `DecoratedStepHyp` #5 (the decorated peel) = THE MOUNTAIN (post-S2, UPDATE-952).** Now owns
     TWO things (both fold into `peelOp` preserves FaithfulSJAt): **(a) the STRUCTURAL half** — each reduced D' is uniform (residualSupport≡0,
     coranks integrated OUT per-peel via `freedSchurLoss`), the S2-mechanism cover established; **(b) THE COUPLED CORNER** — the binding cut
     `minAdm M = a + minAdm(redChain)`, the residual exponent EXACTLY SATURATING the reduced-IH threshold. **★ SATURATION SOLVED (UPDATE-954,
     `joint-corner-cert.md`, decorrelated-Codex-confirmed):** the coupling is ASYMMETRIC — integrate Γ FIRST (Route B, banked-heavy: `loss_blockSplit`
     + `frobSq_mul_ge` + `freedSchurLoss_inner_peel_lt_top`), shifting the reduced threshold by ½peelCharge so the IH applies STRICTLY (not at
     saturation), IH as a **BLACK BOX** (`DecoratedBoxThresholdFinite D'` — NO resolved-radial carry ⟹ invariant SIMPLIFICATION). Route A (projective
     radialization, anchor (3,3,3,4)→7/2) cross-checks. **★ RESIDUAL = the OFF-SECTOR recursion (the LAST genuine soundness surface;
     BIGGER than the §3 one-liner — UPDATE-956):** cover's decorrelated skeptical Codex CAUGHT A REAL GAP in the first-pass corank-of-Z cover —
     m>1 (higher-order σ_min vanishing) does NOT force a corank drop (exact counterexample `Z=ab`: corank=1 but σ_min≍t², first-order fails), so
     corank-recursion MISSES the m>1 loci. **dmcheck's QIP ≥½minAdm is NECESSARY but NOT SUFFICIENT** for the measure cover (deleting {σ_min<ε} as
     null is illegitimate). **FIX = TRANSVERSALITY-augmented ARITY recursion:** GOOD tube {corank=q, transverse, m=1} → density ≥½minAdm (needs N1
     STRENGTHENED to a UNIFORM transversality bound); BAD locus {m>1} = INTERSECTION RAYS (σ_min=coupled-corner monomial) → iterated coupled corner
     (Q1, banked-adjacent), recursing on ARITY↓ (well-founded; corank-of-Z does NOT decrease at the crossing). **Two OWED:** (1) N1-STRENGTHENED uniform
     transversality [geometric]; (2) the ARITY-recursion assembly [well-foundedness + per-level uniform constants + finite subcover, measure]. LABOUR,
     not a decorrelated-confirmed wall. **cover DESIGNING it airtight** (decorrelated; gate is a PROOF for this exhaustiveness claim) → then **controller
     runs an INDEPENDENT hunt (fresh seat, NOT cover's Codex) before formalize** (bedrock: exhaustiveness at the foundation, one gap already surfaced).
     PRECISION: sector-coercivity FAILS POINTWISE on the rank-drop locus → PER-PEEL-SUPPLIED, NOT a carried adm clause (carried adm = the 6-clause set).
     **★ #5 ρ-EQUIV PRODUCTION OBLIGATION (cover re-audit, UPDATE-959-adjacent):** the ρ-Equiv `D.ι ≃ Fin a × Fin Dt` (generators ↔ Γ·Z entries, load-bearing for Σres²=frobSq) is a fine CONSUMED hypothesis for #4, but #5's peel must PRODUCE it — exactly one generator per resolved-corner entry, NO spurious/omitted generators (`|ι|=a·Dt`). A #5 fidelity requirement on the resolved carrier. Below: the coupled corner blow-up → charges-ADD
     to `sjLoss_terminal`, closing on the reduced-chain IH at the ½peelCharge-shifted threshold. **SOUNDNESS RESOLVED (dmcheck #146
     + cover, decorrelated): NO COLLAPSE** — the invariant form is the QIP identity + m=1 + D_q≤M₀q, per-cell `½(M₀ρ+min(M₀q,D_q))`
     (NOT bare D/m≥n₀ [view-dependent], NOT codim-slack). **Build-plan BANKED** (`stephyp-buildplan.md`, 6-node DAG, mostly-banked):
     owed = N1 (m=1 reduced full-rank normal slice, banked-adjacent to `normalSlice_transfer` #109) + N4 (peelOp centerpiece);
     driver/shift/QIP/units-bridge/terminal/corank-survival/peel-preservation all BANKED. **★ FLAG-2 (cover): the peel MUST keep
     ν = product-type FIXED** (record row-elimination in coeff/supp), else genuineCarrier preservation fails — or reformulate
     genuineCarrier with an equivalence (ν ≃ product). 3 misread-traps (T-a/T-b/T-c) baked in as "do NOT" guards.
     ★ #5 is now framed as **`peelOp` PRESERVES FaithfulSJAt** (stephyp-buildplan §5′): α banked (radialStep shared-divisor) /
     β charges-ADD (#144 + carrierThreshold_shift) / **γ = whole-Z `Z·Zᵀ≽c·I` [σ_min(Z), NOT σ_min(A₂) — γ-lock correction]**;
     cover's γ-audit focus = whole-Z (rank-drop-of-Z complement recurses, dmcheck P3). Owed Lean: N4′ peelOp + N1′ m=1 slice.
   **THEN:** `DecoratedDescent` proved → `(□)` ∀M → mint unsuffixed `aoyagi_learning_coefficient` (#108, on the FULL general
   (□) — precision guard). Certs: `threads/genm-sj5/` (descent-buildplan, route-reconcile, toric-ray-cert, transversality-recursion,
   cov-ledger-design, obligation1-spec); live read: synthesis UPDATE-931.
1b. **[banked-but-reusable, off critical path] det-monotone (`RouteMSJDetMono`, #112 rebuild, unwired);
   corank-Gram (`RouteMSJCorankGram`, one isolated sorry, untracked) = the det-Gram-weight TOOL usable INSIDE
   Route A (#140), not the closer.** Historical (superseded framings): the front-first g(Q)/qPeelIntegral route
   (faithfulness-contested #140 — shared-A₂) and the earlier front-first cover are BOTH superseded by the
   decorated (S,J) descent (item 1). Their banked spectral machinery (`twoBlock_radial_le`/`FrontSpectral`/
   `ProductTube`/RadialPolar) + the corank-q `qPeelIntegral_lt_top` (sound abstract lemma, (□)-application
   contested) remain reusable tools, not the (□) route. No separate lane remains — the route IS the decorated descent.
2. **[RESOLVED 2026-07-09] Peel-stack naming — KEEP the unsuffixed `aoyagi_learning_coefficient`.**
   Operator decision: keep it. It stays a sorried placeholder until Stage 2 discharges `(□)`, then it is
   re-pointed to the honest unconditional fully-general result. No action now.
3. **[OPERATOR-GATED] `dev` PR — DRAFT opened (#26).** Stage-1 milestone up for review; merge on operator
   decision (not before Stage 2 or whenever the operator chooses). Controller does not merge.
4. **[DEFERRED · roadmap] θ analytic-multiplicity seam.** `cards/theta-analytic-multiplicity-seam.md` +
   ROADMAP Bundle 4. Separate future direction; `λ` unaffected. NOT Stage 2.
5. **[NON-BLOCKING] stale-docstring residue.** Central retirement note (AxCheck:101) + prominent files
   swept; any remaining `[…, monomial_rlct]` per-result prose is minor polish (emitted `#print axioms`
   is ground truth).

## Watching (suspicion / risks)

- **★ SOUNDNESS WATCH-POINT (T4, the (□)-core) — the A₂-units RLCT-collapse — RESOLVED: NO COLLAPSE
  (UPDATE-939, dmcheck #146 @c7fe44dd + cover's stephyp cert @f98903f6, both decorrelated w/ Codex).**
  dmcheck swept 23 chains (anchor + 4 bottlenecks + corank-2/3 + L=3,4,5): NO collapse; linchpin
  `min_ρ(cCodim+M₀ρ)=minAdm` holds (0 violations, geometric cCodim) ⟹ our decomposition is FAITHFUL.
  Forced by 3 pillars: (P1) tube codim=cCodim; (P2) m=1 via the reduced full-rank normal slice; (P3) binding
  ⟹ D_q≤M₀q ⟹ charges ADD → ½minAdm. **★ FRAMING for #5 (dmcheck's decorrelated correction to cover's cert
  — ADOPT):** state the soundness as the **QIP identity + m=1 + D_q≤M₀q**, per-cell threshold
  `½(M₀ρ+min(M₀q,D_q))` — NOT bare `D/m≥n₀` (VIEW-DEPENDENT — inverts top-level vs decorated), NOT ½(D_q+d_q).
  **3 misread-traps #5 must avoid** (T-a σ_min² per-direction not det≍t^{2q}; T-b no free-front-layer prepend —
  transversality §1 licenses the deeper-product reading; T-c ½(D_q+d_q) over-claims on a narrow front but stays
  ≥½minAdm). **Owed to Lean for #5:** (P2) the reduced full-rank normal slice (m=1), banked-adjacent to
  `normalSlice_transfer` #109. cover to CONFIRM the framing correction; audit #5's estimate against the
  invariant form + the 3 traps. No wall.
- **`(□)` climbability (Stage-2 crux).** Aoyagi §5 is established ⇒ labour, not a wall; the corank-2 rung
  is decorrelated-confirmed finite at 7/2 (3 ways). The deepest grind is closing `sjJointResolution` at
  the corner monomial (the geometric corner resolution — the GAP). A genuinely-new obstruction here (not
  labour) would be a real escalation — watched, not expected. Dead routes (do NOT revisit): the ∧²-
  compound tube (asymmetry, corank2-cert §4); the SEAM det-Jacobian (UPDATE-771); the front-peel
  shortcut (#93).
- **Tide reliability.** A background formaliser can idle-loop (burst→idle, no commit) — watch FROZEN
  worktree edits + no commits (not just idle pings); take over + drive directly rather than nudge.
- **Branch hygiene.** Controller stays in the MAIN checkout ON `expedition/aoyagi-full` (per operator
  2026-07-09); build via `scripts/lb` (never bare `lake`); PRs/dev-master operator-gated.
- **Cadence.** Fewer higher-signal ticks; honor the ≥20-min heartbeat; resist reflexive doc-churn.

## Operator notes

(empty — operator injects here)
