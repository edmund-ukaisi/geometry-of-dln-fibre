# native-engine-lanes.md — the STAGE-3 journal: reaching native (□) via socket-then-fill (controller-owned, durable)

**Opened 2026-07-17 at the LATE-102 operator steer** (checkpoint). Supersedes `endgame-lanes.md` as the
active journal; that file is the historical record LATE-1..102 (the design arc that located + named the
hard object). This file tracks the phase that BUILDS it.

---

## THE GOAL (operator, binding — 2026-07-17)

The box theorem: **∀ architecture `M`, ∀ exponent `c′ < ½·minAdm M`, the box integral is finite**, proven
with kernel footprint **`[propext, Classical.choice, Quot.sound]`** and **NO carried Aoyagi hypothesis of
any kind**. The mint `aoyagi_learning_coefficient` then stands **unconditional modulo the standard
nondegeneracy hypotheses only**.

A theorem carrying `cited_aoyagi_product_corank` is **not this goal reached — it is a station on the way.**

### Why the cite is a station, not a boundary
The min-corank-≥2 product-corank / joint-Vandermonde box-finiteness is a genuine wall **for this route's
shortcut space** — no cheap native patch exists at that arm (prodcorank, 4 proved lines: joint-center
survives single-factor resolution; codim `C_m = m²−⌊m²/4⌋` undershoots naive `m²`; tightness at the
balanced binding cell). But the object behind it is **worked-out mathematics in Aoyagi's paper** — Aoyagi
computed the DLN RLCT, and the log-resolution behind that computation is what we transcribe. **Worked-out
mathematics is not a boundary; it is unbuilt work.**

### BINDING RULES (carry these every tick)
1. **Every wall claim states which of the two it is: OPEN PROBLEM or EXPENSIVE TRANSCRIPTION.** No blur.
   (The blur is exactly what took a wrong destination decision at LATE-97/101.)
2. **The hardest named object gets a dedicated lane the DAY it is named** — not after the easy mass is
   exhausted. (Three times the deepest object was deferred — decoupled around → reclassified as wiring →
   absorbed into a cite — and three times returned with interest.)
3. **Estimate Lane 2 honestly against the measured record: build estimates here run 2–5× short.** Report
   Lane 2 progress by the **skeleton's hypothesis list**, not by narrative.
4. **NON-GOAL (both lanes):** `cited_aoyagi_dln`, the `RlctInterface`, the fibre payoff, any loss-bridge /
   re-pointing. A different theorem in a different vocabulary. No work touches it under this steer; its
   disposition is a separate, later decision.

---

## HOW TO REACH IT — two lanes, in parallel

### Lane 1 — close the shell
Finish the current bounded build **exactly as designed**: wings, corank-one arm, dispatch, wiring,
integration, forced axiom-prints. Discipline (non-negotiable):
- The min-corank-≥2 atom is **ONE named Prop (`cited_aoyagi_product_corank`) with ONE consumption site.**
  Nothing else joins it: no second deferral folded into the socket, no arm weakened to lean on it.
- The socketed composition wears its hypothesis: **`routeMBoxThresholdFinite_of_prodCorank`.** The
  **unsuffixed `aoyagi_learning_coefficient` is NOT minted** until the socket is filled natively (Lane 2).
- Everything outside the socket is **discharged** — no dangling sorries, no assembled-later, no fit
  asserted in prose. **In-file fit-witness for every arm.**
- Forced `#print axioms` on the suffixed composition = `[propext, Classical.choice, Quot.sound]` (the
  socket is a hypothesis, adds no axiom).

### Lane 2 — build the engine that fills the socket
A **dedicated thread at EXPEDITION scale** (do NOT compress into tide language). Formalise the analytic
engine that discharges `cited_aoyagi_product_corank` natively:
- **(a) the resolution construction** — the recursive, branch-selected chart family for the residual
  class, each terminal chart carrying a VERIFIED monomial normal form for integrand and Jacobian,
  transcribed from the source's worked-out construction.
- **(b) the region glue** — finite-cover localization over the box, and threshold invariance under the
  chart maps.
- **Scope excludes** meromorphic continuation and the order θ.
- **First deliverable = a SKELETON, not a brick:** the engine's terminal statement typed so that it IS the
  socket's Prop, with an **in-file example proving the fit before any analytic work begins.**
- **Substrate:** the banked library (measure bedrock, CoV, monomial thresholds, Gamma/Wishart atoms,
  eigendecomposition, QIP family) — **survey it before commissioning anything.** (Three redundant
  commissions this week were three too many.)

### Kill-conditions / witnesses (Lane 2 prerequisite)
Before Lane 2's design pass, the prodcorank discriminators are deposited as **executable battery
witnesses:** joint-center survival, codim-undershoot family, tightness-at-binding-cell. Any
inequality-shaped condition the engine carries gets an **equality-at-binding-cell witness**, not only a
truth scan.

---

## LANE STATUS (live — controller updates each tick)

| Lane | Thread | State | Next gate |
|---|---|---|---|
| **Landed** | genm-integration `@5e9d264e6` (Option B + `_of_prodCorank` rename FF'd) | socket built non-vacuous + plain-route `(□) = routeMBoxThresholdFinite_of_prodCorank` conditional on it; **TWO native gaps** = the d≤1 sorry + the d≥2 cite hypothesis (both visible; mint unbuilt) | close both natively |
| **Lane 1 — design** | `d1design` (pen-and-paper) | IN FLIGHT — honest formal build recipe for the d≤1 arm (which banked atoms compose how + measure-level interfaces + close D-cert §3bis residual + name the new atoms + per-sub-build open-vs-transcription) | recipe lands → commission sub-tides |
| **Lane 1 — 2a** | (lane1shell, on-call) | PENDING recipe — generic dominant-minor cover (EXPENSIVE TRANSCRIPTION, the tractable-first sub-tide) | d1design recipe |
| **Lane 1 — 2b/2c** | (unassigned) | PENDING recipe — 2b corank-one C-transversality atom + 2c a=0 waist density (the genuinely-new-to-formalise atoms) + b=0 Wishart | d1design recipe |
| **Lane 2 — survey** | `l2recon` | ✅ DONE — `recon-map.md` @798601328 (genm-l2recon): plumbing richly banked clean-three (§A–§G); the new heart = §H (c×c chart family + transverse-Jacobian sign-repair); 5 DEAD routes to forbid | feeds the engine spec |
| **Lane 2 — witnesses** | `l2witness` | IN FLIGHT — joint-center survival / codim-undershoot / tightness-at-binding-cell as runnable exhibits | battery lands → gates the engine design pass |
| **Lane 2 — engine** | (not yet commissioned) | PENDING — after l2witness lands; concentrate on §H (treat §A–§G as banked) | skeleton = terminal statement typed = socket's Prop + in-file fit example |

## Socket facts (the one interface between the lanes)
- **Prop:** `cited_aoyagi_product_corank` (RouteMSJDecoratedPeelStep.lean, genm-integration). Non-vacuous:
  the freed-`Γ` triple integral `∫∫∫ freedSchurLoss^(−c′) < ⊤` at `min(M₀−t,M₁−t) ≥ 2`, IH-conditional.
- **Consumption site:** the `d≥2` branch of `innerCorankDescent_lt_top` (`exact hcited …`). Exactly one.
- **Lane 2's engine terminal statement must BE this Prop** (its docstring + signature are the contract).
  When Lane 2 delivers a native theorem of this exact type, Lane 1's socket is filled → the unsuffixed
  `aoyagi_learning_coefficient` is minted at clean-three, no Aoyagi hypothesis.

## Controller hygiene (pitfalls that bit)
- **ROOT CAUSE of the checkout race (l2recon confirmed 2026-07-17):** a worktree agent's **bash runs from the MAIN checkout** while its **Write binds to its isolated worktree**. So any `git checkout -b <branch>` in an agent's brief runs in the CONTROLLER's checkout — switching/detaching my branch. It bit twice this tick (genm-l2recon leak, then genm-d1design detach).
  - **FIX in agent briefs:** do NOT tell agents to `git checkout -b`. Their worktree is already on its own branch; tell them to Write + `git -C <their-worktree>` commit/push, or just work in the pre-branched worktree. (Agents that `cd`/`git -C` into their worktree — as l2recon did — succeed; the bare `git checkout -b` is the hazard.)
  - **FIX in controller flow:** after ANY worktree-agent spawn, and ALWAYS before committing, run `git branch --show-current` — if not `expedition/aoyagi-full`, re-attach: `git checkout expedition/aoyagi-full`. Working tree stays clean so no work is lost; expedition/aoyagi-full keeps pointing at the last real commit. Do NOT delete the stray branches if they are now checked out in agent worktrees (shown with `+` in `git branch`); just re-attach.
  - Symptom to watch: `push` reports "Everything up-to-date" while the remote is behind — means the commit landed on a stray/detached ref, not on expedition/aoyagi-full.

## Updates (running log)
- **LATE-103 (2026-07-17) — Lane 1 RE-SCOPED (not a shell) + Lane 2 survey landed.** lane1shell hit a WALL closing the shell: the d≤1 arm's 2a/2b/2c assembly lemmas DO NOT EXIST (grep-confirmed: dominantMinor/minorCover/deeperFlag_waist_a0/_b0 absent; deeperFlag_waist docstring-only; coupledCell_interior_lt_top is a hypothesis-taking conversion). So the d≤1 arm is a genuine native front = 3 sub-builds, **EXPENSIVE TRANSCRIPTION not open** (the math converges — satred (D) 4 ways, 0/4039; 2b C-transversality + 2c a=0 waist are the costliest = genuinely-new-to-formalise atoms; the one design-residual is D-cert §3bis's a≥u charge accounting, a design-close). lane1shell delivered the sound naming fix (`_of_prodCorank`, FF'd to genm-integration @5e9d264e6) + verified no unsuffixed mint, then stood down. Driving: d1design (recipe pass) commissioned; 2a first (lane1shell on-call), 2b/2c as new-atom builds. **The operator's "bounded shell" premise was itself the design-vs-built blur one level down** — so by the operator's own rule the d≤1 hard objects (2b, 2c-a0) get named + laned now. Lane 2: l2recon DONE (§A–§G banked clean-three, the new heart is §H, 5 dead routes forbidden); l2witness in flight; engine skeleton commissioned after.

## Pointers
- Historical record (the design arc LATE-1..102): [`endgame-lanes.md`](endgame-lanes.md).
- Operator steer + acceptance: [`discuss-at-close.md`](discuss-at-close.md) LATE-102.
- Taste ledger ★★ NOW: [`priorities.md`](priorities.md) LATE-102.
