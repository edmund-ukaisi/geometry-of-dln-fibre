# Codex consult — LIVE+kLDU architectural fork (prompt)

See `/tmp/codex-livekldu-fork.md` content reproduced here for the record.

(Decorrelated xhigh consult fired 2026-06-30 on the architectural fork: fresh
`NodeAchieverChart` bundle with `p := leafPivot` on `phiFlatLiveAt ∘ kLDU`, det chain-rule,
injOn factorization. Answer in `livekldu-fork-answer.md`.)
