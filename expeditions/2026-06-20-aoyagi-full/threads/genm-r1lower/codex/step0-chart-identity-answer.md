1. **VERDICT Q1: WRONG-VEHICLE.** **DERIVED:** as stated, `cov` is for `phiFlatStructV` and requires an exact pure monomial Jacobian, while `interiorDet_leaf_headline_eihd` is for `phiFlatLiveAt` and gives `|u_p|^(minAdm-1) · |det K|^(r+c)`. **DERIVED:** unless those charts are proved identical on the relevant domain and `|det K|^(r+c)` is itself already one of the declared monomial coordinate factors with matching exponent, it cannot fill that field. **INFERENCE:** folding `|det K|^(r+c)` into a bounded unit is not sound in general because `det K` may vanish.

2. **Q2:** **DERIVED:** absorbing `|det K|^(r+c)` into `U' := U · |det K|^-(r+c)` is not measure-theoretically sound as a bounded-unit move unless you have a nonvanishing lower bound for `det K`. **DERIVED:** pretending the Jacobian is only `|u_p|^(minAdm-1)` is also not sound for an exact `cov` identity if the live determinant really has the extra factor. **INFERENCE:** for a divergence proof using the live chart, the sound route would be to keep `|det K|^(r+c)` as an extra nonnegative density factor and prove it has finite positive spectator integral on some set; but that is a different contract from the stated pure-monomial `cov`.

3. **Q3: YES-SEPARATE-BUILD.** **DERIVED:** from the contracts you gave, the eihd determinant naturally belongs to the live-leaf/non-monomial chart route, not the existing R1-LOWER interior `cov` field. **INFERENCE:** the L=2 interior R1-LOWER leg therefore needs either a new pure-monomial chart, phi3333/LDU-style for general `M`, or a rewritten lower-bound contract that permits the extra determinant factor and proves the needed positive finite spectator contribution.

4. Cheapest Lean check: verify whether the `cov` field’s `φ` is definitionally/propositionally the same decoder as `phiFlatLiveAt`; if not, the eihd theorem cannot directly discharge it.