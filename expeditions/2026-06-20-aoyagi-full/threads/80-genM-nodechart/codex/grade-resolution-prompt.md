<task>
Lean 4 / DLN achiever chart. Follow-up to confirm the RESOLUTION of a grading mismatch, to hand a sharp design to the grading author. Earlier I found (Lean #eval) that the INPUT grading (chartIdxEquiv, ChartIdx) partitions Fin N per-layer as [12,12,3] while the OUTPUT grading (paramsEquivFlat, FlatIdx) partitions as [9,9,9] on (3,3,3,3) — so no per-layer bijection exists. You confirmed: the matrix DFrame has rows=output(FlatIdx), cols=input(ChartIdx), two different gradings; need a common square-block refinement.

NEW STRUCTURAL OBSERVATION (the likely resolution): the (3,3,3,3) precedent Frame3333Deriv is an ENDOMORPHISM Fin 27 → Fin 27 (input-coord-i and output-coord-i are the SAME index), graded by a SINGLE frameB : Fin 27 → ℕ (image {0..12}, 13 blocks, sizes [1,1,3,3,3,3,1,1,1,7,1,1,1]). Because rows and cols share ONE index Fin 27 with ONE grading frameB, every toSquareBlock frameB v is AUTOMATICALLY square (rows=cols={i // frameB i = v}). BlockTriangular.det then gives det = ∏_v det(block v).

So the resolution to the mismatch: define Frame_M : Fin N → Fin N as an ENDOMORPHISM using ONE reshape e : Fin N ≃ FlatIdx M for the OUTPUT (Frame_M x := (flatEquivOf e).symm (chartParamsGen (decoder x))), and grade the SINGLE shared Fin N by ONE bLayer. The [12,12,3] vs [9,9,9] was a red herring: it compared two DIFFERENT identifications (chartIdxEquiv vs FlatIdx); the real frame uses ONE Fin N for both.

THE REMAINING CRUX (what I want checked). For the off-block-vanishing `bLayer j > bLayer i ⟹ DFrame entry (i,j) = 0` (one bLayer, shared), I need: OUTPUT-coord-i (graded bLayer i via the e reshape) depends only on INPUT-coords-j with bLayer j ≤ bLayer i. My PROVEN locality (b-0) is: the chart's output chain-LAYER s depends only on input chart-BOUNDARIES ≤ s. But "output layer s" (FlatIdx, the chain layer of the output Params slot) and "input boundary" (ChartIdx, of the input coord) are DIFFERENT partitions [9,9,9] vs [12,12,3]. For a SINGLE bLayer on Fin N to work, bLayer must be a function of the index i that is consistent whether i is read as an input coord OR an output coord — which requires e (the output reshape) to satisfy: bLayer(input reading of i) and bLayer(output reading of i) agree. Since input uses chartIdxEquiv ([12,12,3]) and output uses e→FlatIdx ([9,9,9]), and these partitions DIFFER, NO single bLayer can equal both the input-boundary and the output-layer of the same index i.

So even the "single Fin N endomorphism" framing seems to hit the SAME wall: bLayer can grade the index set, but the off-block-vanishing relates the output-layer of i to the input-boundary of j, and these are governed by DIFFERENT partitions. frameB resolved this at (3,3,3,3) by being a COMMON REFINEMENT (finer than both [12,12,3] and [9,9,9]) — but how is frameB the right grading when its 13 blocks don't obviously align with EITHER the input or output layer partition?
</task>

<output_contract>
4 terse sections:
1. Is the "single Fin N endomorphism + one bLayer" framing the RESOLUTION, or does it hit the same wall (the output-layer-of-i vs input-boundary-of-j being different partitions)? Commit yes/no.
2. What IS frameB, precisely, as a grading? Is it: (a) a common refinement of the input [12,12,3] and output [9,9,9] partitions such that the dependency is one-sided in frameB's order? (b) purely the OUTPUT/Params structure (the chain-layer + within-layer role SCC)? (c) something else? How does a SINGLE frameB : Fin N → ℕ correctly grade a matrix whose dependency relates output-coord-i to input-coord-j — what makes frameB(output i) and frameB(input j) comparable?
3. For the OPAQUE-width generalization: what is the minimal data the grading author must construct? Is it (i) a single bLayer Fin N → ℕ via the OUTPUT reshape e, plus a proof that the dependency is one-sided in bLayer (using my b-0 layer locality + the within-layer Schur SCC), or (ii) genuinely two reindexings (row by output, col by input) into matched blocks? Which is cleaner over opaque widths?
4. The cheapest #eval check to validate the chosen grading on (3,3,3,3) before the Lean build.
</output_contract>

<grounding_rules>
You don't have the repo. The [12,12,3]/[9,9,9] mismatch and frameB's 13 blocks (sizes [1,1,3,3,3,3,1,1,1,7,1,1,1]) are hard facts. Reason about whether a SINGLE grading on a SHARED Fin N can carry the off-block-vanishing when the input-boundary and output-layer partitions of that Fin N differ. Flag inference vs fact. The goal is the minimal correct grading to hand the author, not to re-derive frameB by hand.
