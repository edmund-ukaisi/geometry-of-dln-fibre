**1. VERDICT:** overclaims — the Lean facts do not close the advertised depth-2 composition.

**2. Composition Gap:**  
Fact: (1) and (2) reduce an assumed two-sided comparison `c₀·D ≤ F ≤ c₁·D` to an abstract inverse-power integral bound. Fact: (3) proves a Morse terminal integral for `Fin 4` by applying `radial_morse_dominates_lt_top`.  
Not proved: any chained theorem from the corank-2 core `∫_R∫_S frobSq(R·S)^(-c')` through N2b/split form into (3). The composition exists only in prose.

**3. Threshold Check:**  
Yes: `c' < 2` is exactly the Morse threshold `(m+1)/2` with `m = 3`, so `m+1 = 4`, matching the `1·4` Morse block size.

**4. Honest One-Line Description:**  
These theorems prove an abstract inverse-power comparison lemma and its lintegral form, plus finiteness of the `Fin 4` Morse terminal integral for `c' < 2`; they do not prove the N2b-to-Morse depth-2 composition end-to-end.