**Q1.** The peel is unavoidable: dropping the top block leaves the residual at exponent `c'`, but the IH/core only closes after the `4/2 = 2` exponent shift, i.e. at `c'' = c' - 2 < schurLambda (r-1)`. The shortest correct chain is still N2b `j=1` plus split plus top-row shear plus Morse peel plus carving into `schurResidG_translate_lt_top`. Do not apply the Morse peel as a fixed-`z` pointwise lemma; apply it after exposing the joint free residual variables.

**Q2.** Lowest friction is `RatioIdx0 r := {ij : Fin r × Fin r // ij ≠ (0,0)}` after pivot WLOG, with `CoreIdx := Fin (r-1) × Fin (r-1)` mapped by `(a,b) ↦ (a.succ,b.succ)` and `RestIdx := Fin (r-1) ⊕ Fin (r-1)` for `M21/M12`. Then bridge `Fin N` to `RatioIdx0 r` once using the `piRatioG` off-pivot decoder, and use `MeasurableEquiv.piCongrLeft` plus `MeasurableEquiv.sumPiEquivProdPi` to split functions. CONFIRMED local v4.29: `MeasurableEquiv.piCongrLeft`, `MeasurableEquiv.sumPiEquivProdPi`, `volume_measurePreserving_piCongrLeft`, `volume_measurePreserving_sumPiEquivProdPi`. Yes, the bulk is the index bijection plus readback lemmas, especially `Sc = M22 - Sh rest`. A completely unflattened route is not realistic because the theorem statement already gives `z : Fin N → ℝ`.

**Q3.** Peel after carving. For fixed `Sc`, `0 < frobSq (Sc · S_bot)` can fail, so a pointwise call to `radial_morse_residual_power_le` is the wrong shape. After carving, for each fixed rest/shift `Sh`, the function `(Δ,S_bot) ↦ frobSq ((Δ - Sh) · S_bot)` is positive a.e. by the nonzero-polynomial argument. CONFIRMED repo pattern: `MvPolynomial.ae_eval_ne_zero` plus the corank-3 wrapper `core_T_peel_le_ae_c3`; INFERRED generic target: the same wrapper with `(r-1)` in place of `2`.

**Q4.** Recommend (a), with one precision: do the coordinate carving first, but do not literally translate/enlarge the `M22` domain before applying N2b. The clean order is `pivot WLOG -> z split (Δ, rest) -> pointwise N2b/split -> top-row shear -> Tonelli -> a.e. Morse peel over (Δ,S_bot) -> schurResidG_translate_lt_top per fixed rest -> bounded rest volume`. This avoids the fixed-`z` positivity failure and reuses `schurResidG_translate_lt_top` directly at exponent `c' - 2`. Use a residual radius `K` bounding both the unit `Δ` box and the `T` `S_bot` box, e.g. `K = max 1 T` or `K = 1 + T`, not literally `K = 1` for arbitrary `T`.

**Q5.** Risk 1: generic ratio split/readback after pivot permutations. Mitigation: first build a standalone `zE_G` with simp/readback lemmas for `M22`, `M21`, `M12`, and `Sh`, before touching integrals. Risk 2: joint peel plumbing with a.e. positivity and box radii. Mitigation: prove a standalone `resolvedShiftRG_le` patterned on confirmed `resolvedShiftR2c3_le`, parameterized by fixed `Sh`, `B`, and `K`.

ORDER: carve-first; HARDEST: generic ratio-index split/readback proving `Sc = M22 - Sh(rest)`.