Q1. Pick **A**. B still needs a product presentation of the ratio coordinates before `setLIntegral_prod` or `lintegral_lintegral_swap` can expose the M22 variables. I know no v4.29 one-shot Mathlib lemma for “coordinate projection plus parameter-dependent translation has pushforward density ≤ 1”; proving that would be more work than the explicit product split. CONFIRMED Mathlib tools for A: `MeasurableEquiv.piCongrLeft`, `MeasurableEquiv.sumPiEquivProdPi`, `volume_measurePreserving_piCongrLeft`, `volume_measurePreserving_sumPiEquivProdPi`, `MeasurePreserving.setLIntegral_comp_preimage_emb`, `setLIntegral_prod`, `lintegral_mono_set`.

Q2. Use the same `piCongrLeft` then `sumPiEquivProdPi` mechanism, but avoid a raw `Fin K` complement if possible. Lower friction is to index ratios by `RatioIdx r := {ij : Fin r × Fin r // ij ≠ (0,0)}`, define `CoreIdx r := Fin (r-1) × Fin (r-1)` via `(a,b) ↦ (a.succ,b.succ)`, and let `RestIdx r` be the subtype complement. Then build `(CoreIdx r ⊕ RestIdx r) ≃ RatioIdx r`, and only flatten to `Fin _` at the boundary if some existing chart API demands it. This keeps the M22 block as a matrix-shaped tuple and confines arithmetic casts to one equivalence.

Q3. Do not take Q3 as the low-friction route. The banked local N2b theorem `schur_minorPivot_split` is explicitly stated for the bounded complete-pivoting cell, with `∀ a b, |R a b| ≤ 1` and max-minor hypotheses. A weaker shear-bound variant may be mathematically true, since the existing proof’s comparison core uses the row-shear bound, but that would be a new theorem plus a new partial-blowup Jacobian and an `a`-dependent Morse peel. So Q3 does not reuse the validated plumbing cleanly; it trades the carving for larger new analysis.

Q4. Yes, for the current validated route, the carving is unavoidable. The smallest sharp build is: a structured ratio split `CoreIdx ⊕ RestIdx ≃ RatioIdx`, the associated volume-preserving measurable equivalence, the readback lemma `Sc = M22 - bgShift rest`, the bound `|bgShift| ≤ 1`, and the generic translate/enlarge lemma on `CoreIdx → ℝ`. The translate lemma is local-plumbing over CONFIRMED Mathlib facts: `measurePreserving_add_right`, `MeasurePreserving.setLIntegral_comp_preimage_emb`, and `lintegral_mono_set`.

ROUTE: A; CARVING: UNAVOIDABLE