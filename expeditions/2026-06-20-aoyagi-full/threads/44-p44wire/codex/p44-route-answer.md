1. **Yes.** (A) computes at `deepestPoint Bpr`, while (B) transports the matched point `τ (deepestPoint B)`; arbitrary `Classical.choice` gives no reason those are equal or RLCT-equivalent.

2. **Yes, mathematically true under the exact-rank hypotheses.** For `L = 2`, any two rank-`r` factorizations of the same rank-`r` matrix `B` are related by an internal `GL_{H_1}` gauge, and that gauge is a real-analytic diffeomorphism preserving the product, hence preserving the loss germ and the local RLCT. It can fail if you use noninvertible gauges, leave the exact-rank/no-rank-drop stratum, rely only on a homeomorphism rather than an analytic isomorphism, or let endpoint `GL` actions change the target without simultaneously transforming/stabilizing `B`.

3. The cheaper route depends on the downstream theorem. You can avoid abstract orbit-invariance only by strengthening/rephrasing (A) as a front-aligned theorem for an arbitrary supplied deepest-layers point, then applying it to `τ (deepestPoint B)`; that is still extra mathematics/proof beyond A/B/C. If the downstream result is an infimum over the optimal/deepest set, then yes: keep the theorem scoped to front-aligned `B` and use the permutation-WLOG/BijOn congruence at the infimum level, where point matching is handled globally.

4. **NEW-MATH.** A/B/C only transport RLCT values between matched points; they do not identify `τ (deepestPoint B)` with `deepestPoint Bpr`. To prove arbitrary-`B` NF at its own arbitrary `deepestPoint`, you need a new lemma: local RLCT constancy across the deepest gauge orbit, or an equivalent arbitrary-witness front-aligned theorem.