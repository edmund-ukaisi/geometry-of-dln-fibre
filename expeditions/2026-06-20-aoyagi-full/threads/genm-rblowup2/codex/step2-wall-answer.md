1. **VERDICT:** SAME WALL.

2. **Decisive Reason:** From the stated identities, `φ*(u²·G)=u²·φ*(G)` is a fact about radial/blow-up coordinates already introduced on the active inner boundary: it says old exceptional variables remain passive monomial factors and do not re-enter the residual. It does **not** by itself act on the remaining tail parameters `A'`. The Gram factor `det(Q_b Q_bᵀ)^{-p/2}` is a function of `A'`, not of the peeled radial coordinate `u`, so it is not pulled out for free by the sequential-independence invariant. Analytically, the same deeper product controls both `Q_b` and the shifted reduced-core divisor, so the Gram divisor remains coupled to the reduced-core singularity. Thus the shorter-chain IH controls the reduced-core exponent alone, but gives no spare integrability budget for the Gram Jacobian at the binding cut. That last sentence is an inference from the structure of the displayed outer integral, not a new divergence theorem.

3. **Remaining Sub-Problem:** Build the simultaneous `(S,J)` monomial resolution/double induction that tracks both the reduced-core divisor and the Gram minors of `Q_b` over the shared tail product. The full-row-rank `Q_b` restriction is not essential as a final theorem restriction; it is only essential for this atom formula, and the rank-deficient strata must be handled by recursive deeper boundary charts rather than ignored.