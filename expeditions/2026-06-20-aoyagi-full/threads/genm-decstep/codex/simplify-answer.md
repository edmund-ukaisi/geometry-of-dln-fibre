**Q1 — PROVEN from the stated composition.** Filling `innerCorankDescent_lt_top` completes `decoratedPeelStep_proof`, hence `DecoratedPeelStep`, then `SJStepHyp`, and finally the strong-induction driver with `sjBase1_freeMatrix`. No other stated dependency consumes `FaithfulSJAt`, flag-\(\gamma'\), `DecoratedDescent`, or `sjJointResolution`. Thus the “Q2 dead” obstruction was precisely the unresolved higher-corank descent. The current route still syntactically retains the trivial-decoration shim; deleting that vocabulary would require a small refactor, not additional mathematics.

**Q2 — PROVEN coverage; INFERENCE for the native analytics.** For each pivot cut, \(d=\min(M_0-t,M_1-t)\) satisfies exactly \(d=0\), \(d=1\), or \(d\ge2\). When \(d=0\), the square endpoint and non-square-wing subcases exhaust that branch. There is therefore no uncovered cut. Analytically, the stated \(d\le1\) constructions support the plain IH: transversality/qbox/free-bilinear integration consumes the rank-one factor locally, so no Gram weight crosses into the shorter-chain recursion. This is an inference until those native branch proofs are implemented. For \(d\ge2\), the cited atom may consistently consume the plain IH because its reduced chain has strictly smaller arity; there is no induction cycle.

**Q3 — PROVEN with one qualification.** The guard is correct for the proposed native method: at \(d\ge2\), a single-factor peel leaves the multidimensional joint center \(\{\Gamma S=0\}\), so routing that case to the native branch is unsound. Strictly, non-submersivity also occurs at \(d=1\); the distinction is that the rank-one incidence is resolved by the banked free-bilinear leaf, whereas \(d\ge2\) is the first unresolved higher-rank incidence. The guard does not claim that no native joint resolution could exist.

**Q4 — PROVEN structurally, conditional only on completing the stated native branch proofs and matching the cited interface to the exact lemma signature.** No weighted residual survives outside the cited higher-corank atom, and no decorated IH is needed. The weighted carrier and decorated driver can therefore be removed; only the trivial-decoration compatibility shell remains unless it is renamed/refactored.

**SIMPLIFICATION-SOUND**