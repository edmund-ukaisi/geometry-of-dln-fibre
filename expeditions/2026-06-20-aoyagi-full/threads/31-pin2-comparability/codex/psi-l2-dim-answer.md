1. **Yes, the obstruction is real.** `Matrix.mul` needs the shared middle index type to unify definitionally, not merely be propositionally equal. For variable `L`, `Fin (H ((0 : Fin L).succ) - r)` and `Fin (H ((lastLayer hL).castSucc) - r)` will not unify. Fin proof irrelevance does not make the enclosing `H ... - r` types definitionally equal. Even with `hL2eq : L = 2`, Lean will not use it unless you `subst L` or insert an explicit cast/reindex. So the genuine `Y0 · Z1` action cannot be a clean general-`L` def without an equality bridge.

2. **Ranking: (a) best, (c) distant second, (b) worst.**  
   Best option: **(a)** thread `hL2eq : L = 2` or equivalent into `psiSplitRawL2` and internal helper lemmas, while keeping the external final theorem unchanged. Prefer `L = 2` over `2 ≤ L`, because it gives the exact interface equality; `2 ≤ L` alone is not enough. Use one explicit bridge around either `Y0` or `Z1`, e.g. via `Matrix.reindex`/`finCongr`, so the product is typed at the unique middle interface. Option (c) spreads casts everywhere and will make S3/S4/S2 noisier. Option (b) preserves a general signature but pollutes every downstream statement with irrelevant conditional branches.

3. **Yes, the cutoff/action/S2/S3/S4 helper layer should carry `hL2eq`; no, the cast is not a fundamental derivative landmine if isolated.** `Matrix.reindex (finCongr h)` is just a finite coordinate permutation/transport, so it should be expressible as a linear equivalence or continuous linear map over finite-dimensional matrix spaces [infer]. The landmine is not mathematics; it is elaboration noise from `Eq.rec`/`cast` terms if these casts are inlined through derivative goals. Mitigation: define named cast/reindex helpers once, prove their linear/smooth/derivative facts once, and keep S4 compositions phrased through those named maps. Avoid letting raw `eq_mpr` terms appear inside the main `HasStrictFDerivAt` proof. For S2, prove ContDiff by composing finite-dimensional linear maps and polynomial/rational pieces, with the cast helper treated as a smooth linear map [infer].

4. **You cannot really sidestep the width issue if the def computes the certificate formulas containing `Y0 · Z1`.** You can move the product out of `psiSplitRawL2` only by changing what the helper means: for example, define a general core-slot identity/action and prove it equals the real L=2 formula only later under `hL2eq`. But the moment the actual def contains `P00 = A0·A1 + Y0·Z1` or `K = Z1·⅟P00·Y0`, the middle-width bridge must exist at the def site. A combined object could help only if its type already packages both blocks over one chosen middle index, which is essentially the same cast moved into the reader/packaging layer. So this is useful for API hygiene, not for eliminating the equality obligation.

**Verdict: PATH: use option (a), thread `hL2eq : L = 2` internally and isolate one named reindex bridge.**