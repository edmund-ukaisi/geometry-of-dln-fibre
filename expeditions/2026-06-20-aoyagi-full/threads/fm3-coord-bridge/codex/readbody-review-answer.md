Q1 VERDICT: honest-with-deferral.  
[FACT] The binding summand is functionally the same combinatorial payload: `PUnit` cell, `codim = minAdm M₀`, and an inf-achiever `PivotWitness`.  
[FACT] Nothing in `PivotWitness` says the chart reaches the corresponding stratum.  
[INFERENCE] It is not a smuggle only if used as labelled raw data, with D1/D2/D3 still required before any certified-read/RLCT claim.  
[INFERENCE] If it is fed into a dispatcher as the certified read, it is the same trap in structured form.

Q2 VERDICT: no-hole.  
[FACT] Any theorem using only `split/codim/witness` proves only a combinatorial fold statement.  
[FACT] That fold can see the artificial achiever cell and therefore can compute `½·minAdm` independently of D1/D2/D3.  
[INFERENCE] This is true as an abstract proxy value, not as the analytic RLCT of the geometry.  
[INFERENCE] Stating “RLCT = ½·minAdm” without D1/D2/D3 is an overclaim.

Q3 VERDICT: no-hole.  
[FACT] `r1BindingCellRoot` accepts arbitrary `split` while fixing `codim := minAdm M₀`.  
[FACT] The described declarations do not enforce consistency between the split and actual geometric codimension.  
[INFERENCE] A caller can pass geometrically inconsistent data, and these types will not catch it.  
[INFERENCE] That is exactly the obligation D2 must gate, provided later theorems require D2 before geometric use.

Final: [INFERENCE] “FAITHFUL with the named gap” is sound only as “faithful combinatorial data with explicit missing geometric/analytic gates”; as a certified RLCT/read-body claim, it is an overclaim.