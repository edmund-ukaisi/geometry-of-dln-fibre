**1. VERDICT:** Multi-tide monument. Verified locally: `sjJointResolution` only receives one-shorter `RouteMBoxThresholdFinite`, while the available inner-Γ lemmas expose hypotheses not supplied by that IH. Inference: closing it honestly requires the decorated resolution/principalisation recursion, not a one-tide inequality patch.

**2. MONOTONICITY:** Correct, with one caveat. From the raw definition of `gammaPeelIntegral` as the same front-split integrand over `matBox ∩ pivotChart`, it is bounded by the full same-chain `routeMLayerBoxIntegral M c' 1`. This is non-circular only as: `RouteMBoxThresholdFinite M → gammaPeelIntegral < ⊤`. It is circular if used to prove the current `sjJointResolution` inside the induction step, because the theorem’s available IH is only for shorter chains. Your “peel spine becomes vacuous” characterization is fair for that attempted proof strategy: `box ≤ Σ gamma ≤ C·box` proves no new box-finiteness. The `sjBoundaryPeel` statement itself remains a valid cover inequality; the vacuity is in using same-chain box finiteness to discharge its summands.

**3. ONE-TIDE ESCAPE:** I do not see one. The strong IH cannot black-box the freed Γ integral through the `W > 0`, `Q_b Q_bᵀ` positive-definite, and exponent-region failures. Regime B helps only in its terminal/dominating shape; it does not resolve rank-deficient cross-coupled `Q_b` or vanishing pivot energy. At the binding cut the shifted exponent saturates the reduced threshold, so there is no strict slack for a Hölder/product estimate. Invoking same-chain `RouteMBoxThresholdFinite M` or a theorem downstream of the existing `sjJointResolution` sorry would just launder the target.

**4. NEXT BRICK:** Bank the decoration-level clear-first row-mix lift. Concretely: define `SJDecoration.rowMix`, prove `rowMix_decLoss` from `SJLinGenState.loss_rowMix`, and prove the constant-support specialization using `gen_rowMix_const`/`sharedDivisorExp_rowMix_const`. This is sorry-free-sized, uses banked carrier algebra, and fills the missing “clear-first scalar Schur elimination” half next to the already banked `radialAttach`; it is not pretending to close the analytic peel.