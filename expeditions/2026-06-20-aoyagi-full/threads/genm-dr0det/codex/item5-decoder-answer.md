**Q1.** Correct: chart side should pin the E-pivot, while the boundary factor `B` should read E directly at radial `1`. Let `p = activeSlotE ⟨0⟩ 0 0` and `y = pivotBlowupOn activeM p x`. Pivot case: `(x p) • EfixedReader x 0 0 = x p * 1 = x p = y p = readE_direct y 0 0`. Non-pivot case `(i,j) ≠ (0,0)`: `(x p) • EfixedReader x i j = x p * readE x i j = x p * x (activeSlotE ⟨0⟩ i j) = y (activeSlotE ⟨0⟩ i j) = readE_direct y i j`. This follows from `pivotBlowupOn`: pivot fixed, active non-pivot slots scaled. The needed E-pivot version is new; the banked `readE_pbo` is for the leaf-pivot chart, not exactly this pivot.

**Q2.** Yes, `|det DB| = 1`, but with one correction: on the full ambient chart, `B` also passes the `W` spectator block through when `M2 > 0`. At `Text(2)=0`, the layer-0 component is `A0 = E` exactly, so the E-slots map linearly and directly into the first layer up to fixed reindexing. The layer-1 component is not a nonzero leaf residual; it is the `chainA` residual `W`, again linearly and directly. Thus the full `B` is a coordinate reshape/permutation `(E,W) ↦ (A0,A1)`, not a scaled map. The only determinant ambiguity is sign from fixed reindexing; absolute value kills it. The final `|det|=1` is not yet banked for this exact `B`, but follows from the banked `paramsEquivFlat` linear/measure-preserving reshape plus the new “B equals coordinate pack” lemma.

**Q3.** At `L=2`, boundary `0` is the identity boundary: `Nblk 0 = 0`, `Wblk 0 = 0`, `Rmat 0 = 0`, and it contributes no free residual coordinates. Boundary `1` has `K : 0×0`, `X : M0×0`, and `N : 0×M1`, hence these reads are vacuous when `Text(2)=0`. Boundary `1` has `E : M0×M1`, and these are exactly the active radial slots. Boundary `1` also has `W : M1×M2`; these are spectator slots and become the actual second layer `A1`. The leaf residual `Rfin : 0×M2` is vacuous. So: active/nontrivial radial coordinates are only E, but full `B` also reads W as an identity-like spectator block.

**Q4.** The biggest risk to the proof is the coordinate-pack/reindex bookkeeping, especially remembering the spectator `W` block, not a hidden Schur nonlinearity. Yes: `A0 = E` exactly at `Text(2)=0`. From `schurFrameProd_block_XKNuE`, the bottom-right block is `(X*K*N) + 1*E`; here the summation index through `K` is `Fin 0`, so `X*K*N = 0`. Thus no nonlinear `X·K·N` term survives. The `paramsEquivFlat` part is a fixed linear reshape/reindex, so it can change orientation but not absolute determinant once packaged as measure-preserving. Any Lean pain here is casts and `Fin 0` simplification, not a mathematical scale factor.

VERDICT:
(a) Refined CHART-pins / B-direct design is correct.  
(b) `|det DB| = 1`, with W included as a spectator identity block in the full ambient determinant.  
(c) Cleanest Lean lemma shape: `deepRank0_Bchart_eq_coordPerm` (then derive `deepRank0_Bchart_abs_det_eq_one`).