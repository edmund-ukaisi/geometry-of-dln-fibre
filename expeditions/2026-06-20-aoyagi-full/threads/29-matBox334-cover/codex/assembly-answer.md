**Q1. SOUND.** Use a coordinate split; the pivot being “in the middle” is not a real issue. Cheapest is splitting into `ℝ × ({q // q ≠ p} → ℝ)` first, not forcing an ordered `Fin 8` until you need names. Measurability of the parameterized `J` is the only routine obligation.

**Q2. SOUND.** Your zero-set fix is the right cheap route. The helper should use `B = (1/5) * Q`, `B ≤ A`, and only needs `B = 0 → A = 0`, not full iff. Avoiding this by null-set arguments is strictly harder.

**Q3. FLAWED AS DISPLAYED, SOUND AFTER CORRECTION.** The nested translation route is the cheapest Lean architecture; a one-shot nonlinear shear/Jacobian map is mathematically fine but likely more expensive. The displayed final factor only accounts for `g`; after `A1row0 ↦ T`, the `b0,b1` variables also become a finite volume factor. Also, `g` is not droppable before the `raw ↦ Δ = raw - γβ` translation if you are still in raw coordinates.

**Q4. SOUND WITH THAT CORRECTION.** Dropping `g0,g1` to a finite factor is sound after de-shifting, because the angular bound is uniform for `g² ≤ 1`. But do not undercount: `b0,b1` also remain as finite parameters after the `T` translation. So expect a harmless finite factor like `vol([-1,1]^4)`, not just `vol([-1,1]^2)`.

Recommended assembly: first reduce or state the result for the pivot-normalized `(0,0)` chart; for literal arbitrary `p`, the given banked lemmas do not by themselves connect `Rmat334 p y` to `angularR`, so you need a separate row/column permutation symmetry lemma preserving `matBox`, `frobSq`, and the `A1` measure. Then prove: radial decoupling plus homogeneity; coordinate-split Tonelli giving `radialAxis334_lt_top` times a ratio integral; the ENNReal zero-guard rpow helper for the angular bound; and a standalone ratio-to-resolved finiteness lemma using nested fiber translations `raw ↦ Δ`, `A1row0 ↦ T`, box enlargement to `K = 3`, and finite `b,g` volume. The single highest-risk Lean step is the arbitrary-`p` normalization to the pivot `(0,0)` form; cheapest guard is to isolate it as one lemma before doing any change-of-variables.