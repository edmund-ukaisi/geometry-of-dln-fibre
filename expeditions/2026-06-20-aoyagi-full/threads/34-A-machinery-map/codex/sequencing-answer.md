**1. Recommended Build Order**

1. **routeStep/atlas migration**: Do this first because `R1-upper` is blocked on the carrier mismatch; proving recursion against the old carrier risks throwaway work.

2. **R1-upper `threshold_lt_top` recursion**: This is the stated high-risk long pole and is required for `resolution_charts`, so surface its real shape before spending time on final assembly.

3. **R1-lower `NodeAchieverChart M` producer**: Once upper risk is known, close the lower leg using the already-bankable chart engines; it appears self-contained and unlocks the divergence half of R1.

4. **D1 assembly + value**: After both R1 legs exist, assemble the local value and global infimum statement; before then D1 has nothing solid to assemble.

5. **L2 gauge-chart 5 sorries**: Close the deepest-point minimiser after D1’s target value is stable; these are geometric reduction obligations and do not seem to unblock R1.

6. **headline `½·minAdm = aoyagiLambda`**: Do this only after D1/L2/R1 are sealed, because it is value-identification glue rather than analytic infrastructure.

7. **θ secondary `aoyagiTheta_eq`**: Leave last; it is explicitly optional and should not consume schedule before the main learning-coefficient theorem is closed.

**2. Riskiest Piece**

The riskiest piece is **R1-upper `routeMCore_threshold_lt_top`**: the brief already flags it as the high-risk long pole, and it combines measure-theoretic Schur recursion with the migrated layer-collapsing atlas.

Cheapest early probe: after the routeStep/atlas migration, prove the first nontrivial recursive descent lemma on a generic positive-corank route, checking that the induction measure, atlas domains, and Schur finiteness statement have exactly matching hypotheses. That should expose a carrier/domain mismatch before the full recursion is built. *(inference)*

**3. Dependency Verdict**

I confirm the claim, with the caveat that this is from the brief: **R1-lower chart construction appears independent of the cascade/achiever machinery**. The concrete instances and two reusable engines consume geometric chart data, determinant telescoping, unit bounds, covariance, and image containment, not `achieverRankPattern`, `cascadeTuple`, or `RealizesAchiever`.

The cascade/achiever realizability tie is load-bearing on the **value/upper-leg side**, specifically where rank-pattern realizability forces the binding achiever pattern used by the RLCT recursion/value fold to identify the threshold with `minAdm`/Aoyagi’s value. *(partly inference from the brief’s “feed the value-side / upper-leg” statement)*

**4. Sequencing Traps**

The main trap is delaying the **routeStep/atlas migration**: `R1-upper` may look ready, but its controller is still on the wrong carrier.

Second trap: doing headline algebra too early; `½·minAdm = aoyagiLambda` is useless until R1’s actual threshold theorem is sealed.

Third trap: treating side `θ` work as part of the main path; it can distract from the four gates without unblocking `aoyagi_learning_coefficient`.