1. **INFERENCE.** This is honest only if the result is explicitly advertised as conditional. **FACT:** on the rank-drop branch, `hDeeper` is essentially the desired conclusion itself, so the by-cases proof contributes no mathematical content there. It is logically sound, but becomes circular if downstream recursion uses this theorem to discharge `hDeeper` without a genuinely decreasing argument.

2. **FACT.** Injectivity of \(w\mapsto wA_2\) is exactly full row rank over \(\mathbb R\). For \(q=0\) or \(q=1\), such injectivity is impossible, correctly reflecting that a \(2\times q\) matrix cannot have row rank \(2\). One needs \(q\ge2\) only to ensure the sector is nonempty.

3. **FACT.** With fixed \(U_0,U_1>0\), the threshold is genuinely \(7/2\): the weighted exponents are \(3-8c'/7\) and \(2-6c'/7\), both exceeding \(-1\) exactly when \(c'<7/2\). Taking \(a=\min(U_0,U_1)\) is legitimate; constancy does not remove the coupling—the additive denominator couples the simultaneous corner \(u_0=u_1=0\).

4. **FACT.** A universal `hDeeper` for the same fixed-\(A_2\) radial integral is false. If \(U_1=0<U_0\), finiteness requires \(c'<2\); if \(U_0=0<U_1\), it requires \(c'<3/2\); if both vanish, the mathematical integrand is infinite everywhere for \(c'>0\). Thus rank-drop examples contradict finiteness throughout \(c'<7/2\). A deeper-stratum recursion can restore integrability only by jointly integrating the transverse \(A_2\)-degeneration variables; it cannot imply finiteness of the exact degenerate vertical slice. **INFERENCE:** retaining this premise explicitly is logically sound, but assuming or silently discharging it downstream would smuggle in a false claim.

5. **FACT.** “Positive units iff full row rank” is false for fixed angular vectors. Full rank is sufficient only when the relevant vectors and \(a_{\rm piv}\) are nonzero; it is not necessary because a rank-one matrix may be nonzero on every particular vector being tested. Also, Tonelli assembly remains unproved: the exact determinantal locus may be discarded a.e., but the fiber bounds can blow up when approaching it, and pointwise finiteness would not establish integrability over \(A_2\). Finally, the formal integrand must represent \(0^{-c'}\) as \(+\infty\); ordinary real-power conventions can instead assign zero at a zero base, masking degenerate counterexamples.