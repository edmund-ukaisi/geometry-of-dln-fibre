1. **FACT:** Full residual span is insufficient because factors \(|u_\ell|^{\delta(i,\ell)}\) can vanish on exceptional divisors. **FACT:** Dropping all \(\delta>0\) terms can lose rank—for example, \(z_1\) with \(\delta=0\) and \(z_2\) with \(\delta>0\) span \((z_1,z_2)\), but only \(z_1\) remains. **FACT:** The decisive condition is that the \(\delta=0\) residuals themselves have full rank on the free block, yielding a uniform bound \(C\,\mathrm{frobSq}\) for some \(C>0\). **JUDGEMENT:** Uniformity of every generator is sufficient but stronger than necessary; what is required is a spanning zero-leftover subfamily (and normalization if the displayed constant must literally be \(1\)).

2. **FACT:** The initial carrier is uniform; RADIAL prepends the same exponent to every generator, BLOCK-SPLIT changes none, and valid ROW-MIX cannot create different supports from uniform nonzero inputs. **FACT:** A corank block freed at peel \(j\) does not acquire the radial divisor from peel \(j+1\). **FACT:** Retaining it in the carrier subjected to that later RADIAL would therefore insert a false monomial factor, so it must be integrated or kept in a separate carrier before recursion. **FACT:** No cross-support row-mix is needed: the retained pivot carrier remains one support class. **JUDGEMENT:** For the specified recursion, uniform stable support is forced inductively.

3. **FACT:** Residual anisotropy concerns the coefficients of the linear forms, whereas support anisotropy concerns only exceptional-coordinate exponents. **FACT:** A freed term can have residuals \((\Gamma Q)_{ab}\) while every such generator carries the same accumulated radial monomial, hence \(\delta\equiv0\). **JUDGEMENT:** The two anisotropies are independent; rank variation in \(Q\) does not imply non-uniform support.

CLEAN-BOUND NEEDS: uniform-support-too  
STABLE-SUPPORT: forced-uniform