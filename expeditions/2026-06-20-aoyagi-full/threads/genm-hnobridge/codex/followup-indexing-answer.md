The Lean implementation is correct under the actual convention. I do not see a real off-by-one bug.

Q1. Yes, the raised `tStar` block must be `[q-1, b-2]`, and that is exactly what `raiseTup` implements in [RouteMHNoBridge.lean](/home/ubuntu/workspace/geometry-of-dln-fibre/.claude/worktrees/agent-ad43ca6f12e9d834f/lean/DLNFibre/DLN/RLCT/Validate/RouteMHNoBridge.lean:44). The key bridge is `tStar(j) = Text(j+2)` and `cBlock(j) = Wext(j+1) - Text(j+2) = M(j+1) - tStar(j)`, proved in [RouteMBoundaryCleanMinAdm.lean](/home/ubuntu/workspace/geometry-of-dln-fibre/.claude/worktrees/agent-ad43ca6f12e9d834f/lean/DLNFibre/DLN/RLCT/Validate/RouteMBoundaryCleanMinAdm.lean:39) and [RouteMBoundaryCleanMinAdm.lean](/home/ubuntu/workspace/geometry-of-dln-fibre/.claude/worktrees/agent-ad43ca6f12e9d834f/lean/DLNFibre/DLN/RLCT/Validate/RouteMBoundaryCleanMinAdm.lean:52). So the first failed col-drop at boundary `b` is the cap on `tStar(b-1) = Text(b+1)`, not on `tStar(b-2) = Text(b)`. Therefore the plateau raise `Text(s)` for `s ∈ [q+1, b]` translates exactly to `tStar(j)` for `j ∈ [q-1, b-2]`. It does **not** raise `tStar(b-1)`.

Q2. Yes, `Mval_raiseTup` has exactly the delta decomposition you want, with no endpoint error. `tPrev_raiseTup` proves that the current bump lives on `[q-1, b-2]` while the predecessor bump lives on `[q, b-1]` in [RouteMHNoBridge.lean](/home/ubuntu/workspace/geometry-of-dln-fibre/.claude/worktrees/agent-ad43ca6f12e9d834f/lean/DLNFibre/DLN/RLCT/Validate/RouteMHNoBridge.lean:72). Then [RouteMHNoBridge.lean](/home/ubuntu/workspace/geometry-of-dln-fibre/.claude/worktrees/agent-ad43ca6f12e9d834f/lean/DLNFibre/DLN/RLCT/Validate/RouteMHNoBridge.lean:102) splits the summand difference into the three exact cases:
- `j = q-1`: `uCur = 1`, `uPrevB = 0`, giving `1 - rBlock(q-1) - cBlock(q-1)`.
- `q ≤ j ≤ b-2`: `uCur = uPrevB = 1`, giving `-rBlock(j) = 0` by flatness.
- `j = b-1`: `uCur = 0`, `uPrevB = 1`, giving `+cBlock(b-1) = 0`.

So under the actual Lean convention, the block `[q-1, b-2]` is the correct implementation, and there is no remaining off-by-one bug. The `q=1` / `j=0` admissibility corner is also handled correctly in [RouteMHNoBridge.lean](/home/ubuntu/workspace/geometry-of-dln-fibre/.claude/worktrees/agent-ad43ca6f12e9d834f/lean/DLNFibre/DLN/RLCT/Validate/RouteMHNoBridge.lean:195).