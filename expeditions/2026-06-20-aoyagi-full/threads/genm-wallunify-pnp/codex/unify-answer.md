1. BOTH-BOUNDS: YES — Standard fact: after passing to a finite resolution atlas (not a single global chart), the same normal-crossing form \(F\circ\pi=\text{unit}\cdot u^N\), \(|\Jac \pi|=\text{unit}\cdot u^{\nu-1}\) gives both finiteness for every \(c'<\tfrac12\min\{\text{exponents}\}\) and divergence when some \(\nu_i-cN_i\le 0\) (log-divergence at equality).

2. B-vs-A: SUBSUMED — B is exactly the lower-bound half of A, provided A is proved by the standard chartwise resolution argument over an actual sufficiently small neighbourhood/box, not merely by a germ-level symbolic monomial read-off.

3. RESIDUAL-RISK: The formaliser must check that one chosen small box \(B\) has \(\pi^{-1}(B)\) compactly covered by finitely many normal-crossing coordinate charts with unit factors uniformly bounded above/below, so \(\int_B |F|^{-c'}\) is a finite sum of the corresponding monomial integrals.

4. VERDICT: A honest resolution proof of the exact RLCT already proves B; the only real bookkeeping is the finite-chart neighbourhood/box cover needed to turn local monomial data into the box integral.