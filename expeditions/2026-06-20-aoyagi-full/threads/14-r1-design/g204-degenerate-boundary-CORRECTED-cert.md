# Degenerate-boundary lemma DESIGN cert — CORRECTED (g204, pp-hall, 2026-06-22, #70)

**Supersedes the flat-complement reasoning of g202/g203.** The decorrelated adversarial seat (substitute for
the env-wide Codex outage) broke two claims in g202/g203; both errors are mine, owned here. **The headline is
UNAFFECTED:** `(some M_s = 0) ⟹ rlctAt(deepest) = nReg/2 = aoyagiLambda` STILL holds — only the cert's
*reasoning* + the flat-dimension formula change. The corrected design + the honest record of what broke:

## The statement + the ⊤-trap (unchanged)
`(some M_s = 0) ⟹ rlctAt(deepest) = nReg/2 = r(H_0+H_L−r)/2 = aoyagiLambda` (`lambdaCore = 0`, reduced core
empty). Do NOT route through `rlctAtOn(dlnLoss M 0) 0 = ⊤` (the additive decomposition degenerates).
Compute `rlctAt(deepest)` DIRECTLY.

## (i) Hessian rank = nReg — SOUND + STRENGTHENED (determinantal-tangent, general)
`rank(J) = nReg` at every degenerate-boundary deepest point, `J = dΦ` the differential of the multiplication
map `Φ = ∏C`. **The general reason (seat, stronger than g202's single-orbit):** `J`'s image is generated by
the first + last factors and **surjects onto the tangent of the determinantal variety `{rank ≤ r}` at `B`**
(dimension `nReg = r(H_0+H_L−r)`), capped by the variety. So `rank(J) = nReg` holds regardless of the
factorization or `B` — **single-orbit (g202) is the SPECIAL case; determinantal-tangent is the general one.**
The Gauss-Newton Hessian `J^T J` then has rank `= rank(J) = nReg`. Verified exact over ℚ on **19 configs**
(seat: partial degeneracy `r≥2`, non-monotone valleys, wide interiors, twisted/rank-deficient
factorizations) + my 6 (g198/g199/g201). Deliverable (i) is robust. **Cite the determinantal-variety tangent
argument, not only single-orbit.**

## (ii) The value nReg/2 — STANDS, via SMOOTH-TRANSVERSAL-INTERSECTION (NOT linear flatness — g202 was wrong)
- **flat dimension** `= ambient − nReg = ΣH_sH_{s+1} − r(H_0+H_L−r) = the fibre tangent dimension`. **NOT
  `r²(L−1)` (g203 BROKEN).** g203's `r²(L−1)` (pure gauge) is wrong: witness `(4,2,3,2,4)` r=2
  (`M=(2,0,1,0,2)`): ambient 28, nReg 12, flat = 16, but `r²(L−1) = 12` — GAP 4 (the width-3 interior
  carries fibre directions BEYOND the gauge). More breaks (verified `g204_verify_seat.py`): `(3,1,2,1,3)`
  r=1 flat 5 vs 3; `(2,5,2)` r=2 flat 16 vs 4; `(4,3,1,3,4)` r=3 flat 15 vs 27. **The two formulas coincide
  IFF every interior width = r** — exactly the regime ALL my g203 validation configs sat in (the
  confound I missed: an unrepresentative validation set; a non-bottleneck interior, width > r, carries its
  own fibre/gauge directions). The "M_s=0 dirs absorbed into GL_r, no separate summand" reconciliation was
  the error.
- **the complement is NOT linearly flat** (g202's "loss ≡ 0 along the flat complement" was WRONG): along the
  STRAIGHT Hessian-nullspace direction the loss is exactly `t⁴` (verified: `(2,1,2)` dir `[−1,0,1,0]·t →
  t⁴`; `(3,1,1,3)` kernel dirs → `t⁴`). The straight linear nullspace coordinate **leaves the curved
  zero-locus and re-enters the cone**. `loss ≡ 0` is true ONLY along the CURVED gauge orbit (`C_s → λ·C_s`,
  `C_{s+1} → C_{s+1}/λ`), NOT the linear nullspace. **Acknowledge the t⁴ as a coordinate artifact — do not
  assert it away.**
- **the value survives via the correct argument:** the fibre `{∏C = B}` is a SMOOTH manifold of codim
  `nReg` (rank `dΦ = nReg` = a transversal cut), so in coordinates **ADAPTED to the fibre** (fibre-normal)
  the loss is a clean rank-`nReg` sum of squares + flat ⟹ `rlctAt = nReg/2` (numeric slope → 3/2 for
  `(2,1,2)`, seat). This is the smooth-transversal-intersection argument, NOT linear flatness.

## (iii) rlctAt = nReg/2 (unchanged conclusion, via the smooth-fibre coords)
A rank-`nReg` nondegenerate quadratic on the fibre-normal coordinates + flat along the fibre tangent has
`rlctAt = nReg/2` (the standard fact; reuse L2's smooth-block primitive **on the ADAPTED coordinates**).

## Validation (the seat's 19 + my 6 — value nReg/2 holds everywhere; rank = nReg everywhere)
The value `rlctAt(deepest) = nReg/2` is verified on all configs (audit (3,1,3)→5/2, (1,1)→1/2; my (2,1,2),
(2,2), (3,1,1,3); the seat's 19 incl `(4,2,3,2,4)`, `(2,5,2)`, partial-degeneracy `r≥2`). `rank(J) = nReg`
holds on all (the determinantal-tangent reason). What is WRONG (g203): only the closed form `r²(L−1)` for
the flat dim (right iff interior widths = r). The correct flat dim is `ambient − nReg`.

## What the formaliser builds — the CRITICAL build-route (carry into the build)
`rlctAt_deepest_degenerate : (∃ s, M s = 0) → rlctAt (dlnLoss H B) (deepestPoint H r B) = nReg/2`. Build via
the **smooth-transversal-intersection** argument:
- `dΦ` has rank `nReg` at the deepest (the determinantal-variety tangent / the rank-`r` normal form codim);
- the fibre is a smooth codim-`nReg` manifold; take coordinates ADAPTED to it (fibre-normal split);
- the loss is a clean rank-`nReg` sum of squares in the normal coords + flat along the fibre ⟹ reuse L2's
  smooth-block primitive on the ADAPTED (fibre-normal) coordinates ⟹ `rlctAt = nReg/2`.
- **DO NOT build via the raw linear nullspace** — the `t⁴`-along-the-straight-nullspace bites (the loss is
  not a clean quadratic in the raw coords; it's degree-4 in the linear nullspace direction). The adapted
  (fibre-normal) coordinates are load-bearing. NOT the `rlctAtOn(dlnLoss M 0)` route (the ⊤-trap).

## What broke + why (the honest record — my errors)
- g202: "loss ≡ 0 along the flat complement / genuinely flat" — WRONG. True only along the curved gauge
  orbit; the straight Hessian-nullspace direction gives `t⁴`. I over-read the gauge-orbit flatness as
  whole-nullspace flatness.
- g203: "flat dim = r²(L−1), pure gauge, no vanished-core summand" — WRONG. Right iff every interior width
  = r. I validated only on bottleneck-interior configs (interior = r), the exact coincidence regime, and
  read the 6-config agreement as general. The confound: an unrepresentative validation set.
- What I got RIGHT (sound throughout): the ⊤-trap avoidance, `rank(J) = nReg`, `rlctAt = nReg/2`, the
  direct-Morse architecture. The single-orbit argument is correct but special (the seat's determinantal-
  tangent is the general reason).

## Decorrelation (the substitute channel — Codex down env-wide)
The decorrelated adversarial pen-and-paper seat (substitute for the env-wide Codex outage: the AISI wrapper
hangs on git-ssh before codex starts) red-teamed the claim independently: verified `rank(J) = nReg` on 19
configs over ℚ, found the determinantal-tangent general reason, broke g202's linear-flatness (the `t⁴`) and
g203's `r²(L−1)` (the `(4,2,3,2,4)` witness). Its scripts:
`expeditions/2026-06-20-aoyagi-full/threads/adv-degen-boundary-witness/` (`adv_dln.py` + `adv_run1-9.py`,
exact-rational). I independently re-verified both breaks (`g204_verify_seat.py`: `(4,2,3,2,4)` gap 4, the
`t⁴` along `[−1,0,1,0]`). This is decorrelation doing its job — catching MY over-generalization from an
unrepresentative validation set. The value + the architecture survive; the reasoning is corrected.
