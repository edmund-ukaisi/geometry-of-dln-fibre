codex
**Findings**
- PROVED: Your diagnosis of the gap is correct. For \(u_i < t \leq \text{head}'_i\) we must show \( \mathrm{cLt}(R_i,t) \leq \mathrm{cLt}( \text{head}'_i :: w s_i, t)\). Because \(w s_i\) is shorter (no head slot), this inequality effectively demands that earlier picks have deleted enough small elements. The submultiset bound \( \mathrm{cLt}(R_i,t) \leq \mathrm{cLt}(Y_{\text{multi}},t)\) goes in the wrong direction: it upper-bounds \(R_i\) by the *larger* multiset. Your explicit counterexample \(M=(0,0,1,0), i=1\) shows the static chain fails while the true inequality still holds, so the “static_count + submultiset” route is incomplete.
- PROVED: Closing that gap requires information about how many small elements have actually been erased before stage \(i\). That information is exactly what the inductive invariant \( \mathrm{FMDom}(i)\) carries (or anything equivalent that tracks the history of picks). A “one-shot” static inequality would have to control \( \mathrm{cLt}(R_i,t)\) directly, which in turn needs the counts of earlier \(q_j < t\); those counts arise only from the iterative greedy choices. So the effect of the erases is load-bearing.
- PROVED: With the current consumer lemmas (`dom_head_pick`, `le_maxPick`) you still need the full `Dom(head'_i :: ws_i, R_i)` statement, because they quantify over all \(t\) and use both the head slot and the suffix simultaneously. The static \(t \leq u_i\) portion alone cannot guarantee \(q_i \geq \text{head}'_i\) or preserve the invariant downstream.
- PROVED (Bottom line): The proposed static reduction does not eliminate the need for the inductive invariant. Maintaining (or equivalently re-proving) the effect of the erases via induction remains essential to control the gap \(u_i < t \leq \text{head}'_i\).
tokens used
