**Q1.**  
[FACT] The general construction only needs route (2a) if the goal is `⨅_i monomialThreshold ... = 1/2 · minAdm`. [FACT] An infimum equality by `le_antisymm` needs a lower bound for every chart/leaf and one chart/leaf attaining the bound. [FACT] The stated `foldFamily_iInf_eq_half_minAdm` exactly matches that: C≥ for every leaf codim, plus one achiever leaf containing `minAdm`. [FACT] Full `∀ T ∈ Adm M, ∃ i, stratum i = T` surjectivity is strictly stronger than existence of one minimising `T*`-leaf. [INFERENCE] Full surjectivity is only needed if the chosen API is `resolution_value_of_atlas`, not by the RLCT/value logic itself. [INFERENCE] A hidden dependence would arise only if the available proof of the lower bound C≥ internally uses a global classification of all cells by all admissible strata; that is not forced by the stated theorem shape.

**Q2.**  
[FACT] C≥ as stated is only the codim bookkeeping claim: for each blow-up cell codim `c`, produce some `T ∈ Adm M₀` with `c = (Mval M₀ T).toNat`. [FACT] That lives in the `Adm`/`Mval`/`PivotWitness` layer, not in Core’s `Tuple`/`RepCoord` orbit layer. [INFERENCE] If `routeStep` defines cells and codims by the Schur peel recursion itself, C≥ should be a finite induction over the width-drop bookkeeping plus admissibility-preserving transformations. [FACT] The stronger geometric statement “this blow-up center is the rank stratum and has codimension `Mval T`” is different content. [INFERENCE] That geometric identification may use Core orbit-Kostant/closure machinery, but C≥ does not logically require it unless the chart construction’s definition of cells is geometric rather than combinatorial. [FACT] `schur_straighten_squeeze_exists` still requires a chart-supplied Schur presentation; that is a separate NEW obligation from `PivotWitness`.

**Q3.**  
[FACT] Since `T* ∈ Adm M` and `Mval M T* = minAdm` are already constructible, C=∃ does not logically require full coverage of `Adm`. [FACT] It requires one concrete recursive branch through `routeAtlas` whose accumulated codim list contains `minAdm`. [INFERENCE] The residual theorem is a path-lifting/construction theorem: from the balanced minimiser data, choose legal Schur cells/splits at each step and prove the recursion terminates in a leaf carrying the codim `Mval T*`. [INFERENCE] This is weaker than global surjectivity because it quantifies over one distinguished `T*`, not every admissible `T`. [FACT] It is still a NEW obligation unless a listed green theorem already connects `qStar` to legal `routeStep` choices. [INFERENCE] The likely hard part is compatibility between the `qStar` reduction sequence and the local Schur chart presentations, not orbit closure classification.

**Q4.**  
[FACT] The listed green machinery suffices for the abstract RLCT bridge, the value identity, and the existence of a minimising admissible exponent vector. [FACT] The open `routeStep` still must supply legal cells/splits/codims/witnesses and the explicit Schur presentation hypotheses consumed by descent soundness. [INFERENCE] Minimal missing glue is DLN-chart-combinatorics-scale if cells are defined by the Schur recursion: prove admissibility/codim preservation for every branch, and prove a selected `qStar` branch reaches a leaf with codim `minAdm`. [INFERENCE] Core orbit theory becomes necessary only for the stronger theorem that geometric blow-up centers exhaust or coincide with all admissible rank strata. [FACT] Full surjectivity is not forced by the value route. [INFERENCE] I would not classify this as a residual research gap unless the Schur presentation itself cannot be produced uniformly for general `M`.

VERDICT: IN-REACH (achiever-only)