<task>
Verify, with exact algebra, a claimed polynomial ideal-membership arising in a deep-linear-network
RLCT computation. Be adversarial: try to find a shape (m,k,n) or a structural reason it FAILS.

SETUP (a "reduced node" after one blow-up):
- Â : m×k real matrix with Â[0,0] = 1 a HARD constant (post-blow-up pivot); the other entries free.
- A2 : k×n real, all entries free.
- F := ‖Â · A2‖²_F   (squared Frobenius norm of the product; a polynomial in all entries).
Block-partition Â = [[1, a],[b, D]], A2 = [[β],[Γ]] with a=Â[0,1:] (1×(k−1)), b=Â[1:,0] ((m−1)×1),
D=Â[1:,1:] ((m−1)×(k−1)), β=A2[0,:] (1×n), Γ=A2[1:,:] ((k−1)×n).
Define:
- E_j := (Â·A2)[0,j], j=0..n−1   (the pivot ROW of the product); E_row = (E_0,…,E_{n−1}).
- S := D − b·a   (the Schur complement, (m−1)×(k−1)).
- A2red := Γ.
- Φ := Σ_j E_j² + ‖S·A2red‖²_F.

THE CLAIMS to verify (or refute):
(1) The lower rows of the product decompose as a MATRIX identity, for all m,k,n ≥ 1:
    (Â·A2)[1:,:] = b·E_row + S·A2red.
(2) Consequently F − Φ ∈ ideal(E_0,…,E_{n−1}), with explicit cofactors:
    F − Φ = Σ_j E_j · g_j,   g_j = Σ_i b_i·( b_i·E_j + 2·(S·A2red)[i,j] ).
(3) F and Φ have the SAME zero-set germ at 0: {F=0} = {Φ=0} = {E=0 ∧ S·A2red=0}.
(4) Near 0 there exist c₁,c₂>0 with c₁·Φ ≤ F ≤ c₂·Φ (a two-sided squeeze), and the lower bound c₁>0
    is structural (not just numerical).
</task>

<output_contract>
1. Verify or refute (1) by block-matrix algebra. State the one-line derivation.
2. Verify or refute (2); confirm the cofactors g_j or give corrected ones.
3. Verify or refute (3); identify any shape where the zero-sets differ.
4. Assess (4): does the bounded-linear-perturbation argument give c₁>0 near 0? Note any failure mode
   (e.g. a direction where F/Φ → 0).
5. Flag the structural assumption that makes (1) work, and any (m,k,n) edge case (m=1, k=1, n=1) where
   the statement degenerates or needs care.
</output_contract>

<grounding_rules>
- Exact symbolic algebra only for the load-bearing claims (1)(2)(3). You may use a CAS mentally or
  reason in block form; do not rely on numerics for (1)(2)(3).
- "ideal(E)" = the polynomial ideal generated by E_0,…,E_{n−1}; membership ⟺ vanishing on {E=0}.
- Distinguish a MATRIX identity from a VALUE identity (do not assume any matrix factor is orthogonal).
- Be adversarial: actively look for a counterexample shape before confirming.
</grounding_rules>
