**Q1.** NO. **DERIVED:** `rlctAtOn F 0 ≤ t` is an `sSup` upper-bound statement, so it rules out local integrability for exponents strictly larger than `t`, but it does not rule out local integrability at `c' = t`. **DERIVED:** With the usual measurable-density bridge, strict nonintegrability above `t` can imply box divergence for `c' > t`, but the target asks `t ≤ c'`, including the boundary. **GENERAL-KNOWLEDGE:** Boundary divergence holds for normal-crossing monomials under the stated sharp monomial lemma, but that fact does not transfer to the original flat-coordinate `F` without a change-of-variables or comparable geometric comparison. **DERIVED:** Therefore `rlctAtOn F 0 ≤ (minAdm M)/2` is weaker than the target atom.

**Q2.** REQUIRES-CHART, or at least requires a geometric substitute that compares `F` to the achiever monomial on a positive-measure parametrized region. **DERIVED:** The listed squeeze machinery equates point RLCTs only; it does not produce flat-coordinate box integrals, boundary divergence, or a map from monomial chart coordinates into `cubeBox N ε`. **DERIVED:** Since the general-M achiever family is only combinatorial exponent data, `monomialIntegrand_lintegral_box_eq_top` cannot be pushed to `F` without geometric realization. **DERIVED:** The single-global `phiUnit` route is unavailable by #6/#7. **GENERAL-KNOWLEDGE:** A composed sequence of local blow-ups along the achiever path is the natural resolution-theoretic escape: it need not be one miraculous triangular chart, but it must still come with Jacobian control and a pullback upper bound of the right monomial type. **DERIVED:** For divergence, the needed inequality direction is `|F ∘ φ| ≤ C · monomial`, since then `|F ∘ φ|^{-c'} ≥ C^{-c'} · monomial^{-c'}`.

**Q3.** Tractability ranking for Lean: (c) first if the smooth-block `Φ` has an already-computable box divergence theorem in flat coordinates; otherwise (a) first; (b) last. **DERIVED:** (c) load-bearing sub-lemma: `∀ c' ≥ t, ∀ ε>0, ∫ cubeBox ε ofReal (Φ x)^{-c'} = ⊤`, plus a neighborhood inequality `F ≤ C·Φ` on a box. **DERIVED:** (a) load-bearing sub-lemma: a measurable wedge/tube parametrization with positive transverse measure and `F` bounded above by the critical monomial model, so Fubini reduces to the divergent one-dimensional or monomial integral. **DERIVED:** (b) load-bearing sub-lemma: existence of an achiever blow-up chart `φ` mapping a parameter box into the flat cube, with measurable change-of-variables/Jacobian factor and `|F ∘ φ| ≤ C·monomial` with the exponent data of the achieving leaf. **GENERAL-KNOWLEDGE:** A full chart is conceptually standard but Lean-heavy because it packages geometry, measure pushforward, Jacobian, and pullback asymptotics.

**Q4.** **DERIVED:** The soundness trap is to prove the value theorem `rlctAtOn F 0 ≤ t` by recursion, then silently replace it with the stronger statement `∀ c' ≥ t, ∫ cubeBox ε |F|^{-c'} = ⊤`. **DERIVED:** That replacement loses the strictness in `rlctAtOn_le_of_box_diverges`, whose premise only needs divergence for `t < c'`, not at `c' = t`. **GENERAL-KNOWLEDGE:** A germ may have threshold value `t` while the borderline density is not certified divergent by the `sSup` definition alone. **DERIVED:** In Lean, this manifests as trying to synthesize `hdiv_achiever` from a point-RLCT inequality, with no term proving the boundary box integral.

Bottom line: the honest route is not `rlctAtOn ≤ t ⇒ hdiv_achiever`; that is strictly too weak at the boundary.  
Most tractable route: prove a geometric comparison for an achiever wedge/chart giving `F ≤ C·monomial` and enough measure/Jacobian control to import the sharp monomial divergence.