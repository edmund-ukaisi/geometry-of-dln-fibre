**Q1:** Yes for the raw Lean-totalized `φ_sm` with `Ufun = a²`. The pointwise rate identity fails on the pole: at `a = 0`, Lean’s `b / a` is `0`, so `A⁰A¹ = b·sb`, not `a·z`. The sharp obstruction is especially `a = 0, z = 0, b·sb ≠ 0`: then `F∘φ = (b·sb)² > 0`, while any factorization `F = z²·U` gives `0`. Mathlib fact: the current field uses `Real.rpow`, and for `c > 0`, `(0 : ℝ)^(-c) = 0`; `ENNReal.rpow` would behave differently, but this code wraps real powers with `ENNReal.ofReal`.

**Q2:** Ranking: `(ii)` is cheapest-looking but not sound; `(i)` is the cheapest sound repair; `(iii)` is cleanest architecturally but most code. In `(i)`, existing pointwise charts can supply the new a.e. field by `Filter.Eventually.of_forall`, and the assembly rewrite changes from pointwise `setLIntegral_congr_fun` to an a.e. congruence after dropping the pole null set. `(iii)` avoids breaking old `NodeAchieverChart`, but duplicates the assembly and creates a second chart API. So if you accept editing the banked core, pick `(i)`.

**Q3:** No, `Ufun_sm := |routeMCore M121 (phi121sm u)| / (u 2)^2` does not make the existing `leaf_integrand` hold pointwise. Off `{z = 0}` it repairs the pole slice algebraically, but at `a = 0, z = 0, b·sb ≠ 0`, the LHS is positive while the monomial side has the pivot loss factor evaluated at zero, hence is `0` in the current real-rpow convention. Also, `Ufun_sm` is not a legal existing `Ufun`: measurability and a.e.-positivity are plausible/inferred, but pointwise boundedness on every box fails since on `a = 0, z ≠ 0` it is `(b·sb)² / z²`, unbounded as `z → 0`. The assembly does not only need `leaf_integrand` off `{z=0}`; it rewrites on all of `P` before removing the pivot-null set.

VERDICT: GAP: assembly/structure must change because `leaf_integrand` is also pole-affected; cheapest sound fix is the a.e. `leaf_integrand` field plus an a.e. rewrite in the existing assembly.