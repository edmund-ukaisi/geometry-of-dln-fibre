1. **Q1:** No, I would not call the existing `cov` undischargeable. The direct banked route on `s = V \ {z = 0}` fails, because the Mathlib c-o-v lemma you cite needs `HasFDerivWithinAt` at every point of `s`. But the `cov` field itself can still be proved by applying c-o-v on `s \ N0` and adding back the bad slice, provided `phi` is defined on `N0` with a front-preserving null-image extension. This is an inference about this construction, not a built-in Mathlib a.e. c-o-v fact.

2. **Q2:** Yes, the split route closes if you prove the right LHS subfact. RHS over `s ∩ N0` is zero because null sets do not affect `lintegral`; no finiteness of the integrand is needed. The LHS obstruction is exactly `volume (phi_sm '' (s ∩ N0)) = 0`. That is not automatic for an arbitrary measurable extension. But here you should choose/define the extension so the front block remains identity on `N0`; then `phi_sm '' (s ∩ N0) ⊆ {target front Gram determinant = 0}`, a target algebraic null set. That avoids Luzin-N entirely.

3. **Q3:** Cheapest sound fix is **(b), but in the structural-containment form**, not via a general image-of-null theorem. Rank: first prove `phi '' bad ⊆ targetN0` and `targetN0` null; second, if that somehow conflicts with other fields, introduce an extra-null-set chart variant as in (a); third, build an a.e. raw c-o-v theorem or use threshold transport only after changing the assembly. Option (a) is sound but more invasive than necessary for `cov`. I am confident the null-set lintegral add-back is standard/Mathlib-shaped; exact lemma wrappers may need local proving.

4. **Q4:** Yes, the box-divergence survives deleting `N0`. One-line reason: deleting a volume-null set does not change an `ℝ≥0∞` lintegral, so an integral already equal to `⊤` remains `⊤` on the complement. This is standard measure theory and should be available in Mathlib through null-set/set-lintegral congruence lemmas, perhaps with small wrappers.

VERDICT: EXISTING cov dischargeable via split on `s \ N0` plus a front-preserving extension proving `phi_sm '' (s ∩ N0)` is null.