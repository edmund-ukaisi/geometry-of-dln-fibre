**Q1.** Finding 2 is correct for the **identity-elsewhere dead-leaf** witness: a live block at a non-deepest boundary is killed by the zero deeper suffix. But the stated condition `tStar(L−2) ≥ 1` is not the right Lean-indexed condition. For `tach`, `Text L = tach(L−1) = tStar(L−2)` for `L ≥ 2`; the forced zero `tStar(L−1)=0` is `Text(L+1)`, not `Text L`. The single-live-block dead-leaf witness requires the actual deepest interior `E` block
`(Text(L−1)−Text L) × (Wext(L−1)−Text L)`
to be nonempty. If that block is empty and the suffix remains identity/zero as in finding 2, then that witness gives `Hmat 0 = 0`.

**Q2.** This does **not** force a live-leaf decoder as the only uniform fix. A live leaf is the simplest fix when `Text L > 0`, since `Rfin_L` then gives a terminal nonzero source and the kept path propagates it. But a dead-leaf shallower witness can work if the suffix is not identity/zero: set the intervening `W` blocks to carry the injected column down to the final output. So the recommendation is not “live leaf only”; it is “effective-leaf witness,” with live `Rfin_L` as the terminal case and a carried dead-leaf `E/W` witness when the terminal row space has already vanished.

**Q3.** The leaf-row-empty issue is off by one as stated: `Rfin_L : Text L × Wext L`, and for `tach`, `Text L = tStar(L−2)` for `L ≥ 2`, not `tStar(L−1)=0`. Thus the live leaf is not automatically empty; it is empty exactly when the penultimate Aoyagi rank has already dropped to zero. Your “effective leaf” correction is the right uniform location after this indexing fix. Let `q` be the deepest index `≤ L` with `Text q > 0`; if `q=L`, use `Rfin_L`, and if `q<L`, use the interior transition `Text q > 0 = Text(q+1)` and carry through the deeper `W`s. In terms of `tStar`, if the last positive `tStar` index is `a`, then the last positive `Text` index is `a+2`; if no `tStar` entry is positive, it is the identity boundary `Text 1 = M 0`.

**INFERENCE.** The uniform witness for `2 ≤ L` should be: choose the deepest `q ≤ L` with `Text q > 0`; use `Rfin_L` when `q=L`, otherwise put the pivot in the `E` block at boundary `q` and set `W_q,…,W_{L−1}` as coordinate carriers to the final column. Set `K=I`, `X=N=0`, and all unrelated `E/W/Rfin` slots to zero. Then `Hmat 0` contains one surviving monomial, so `UPolyGen ≠ 0`; `exists_active_block` should be replaced by this effective-leaf selection, not used alone.