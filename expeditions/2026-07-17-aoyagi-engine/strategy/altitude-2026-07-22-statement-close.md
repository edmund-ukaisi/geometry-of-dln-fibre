# Altitude note — statement layer → proof cascade (2026-07-22 ~15:40 UTC)

**Where we came from.** Six days of statement-layer work culminating in seven elder-gated bakes
in ~24 h (redirect → pin → hpos → collapse → node-form [refuted, u³] → step-form → ruling-B).
The bake cadence worked because the loop was fixed: seat SPECIFY finds the defect → elder rules
the OBJECT and authors the verbatim form → render applies → arch-C gates → controller verifies
on the CURRENT tip and integrates. Five statement defects were caught by seats before any proof
consumed them; zero defects were caught after a bake (the guard-domain family — hpos/he_lin/
hlayer — all pre-bake). The payoff roots' axiom footprints never moved: the strengthening stayed
confined to the monument layer by construction, verified per-bake, not assumed.

**Where we are.** The monument's every statement is final. Live cone = 10: eight MonumentAtlas
leaves (the descent, lastLayer, two driver stubs, four leafPath anchors incl. L7's
compactCover), boostReady (Case1Wire), and LearningCoefficient:287 (the summit swap point). The
driver itself carries no sorry — the remaining expedition is leaf-closure only. Producers M4/M7
are proven AND reviewed (7/7 each); §D.3 telescoping is fully provisioned; the (B′) Assembly
absorbs proved leaves incrementally.

**Where we are going, and the risk shape.** The critical path is now boostReady → descent →
L5 assembly (the longest link) with L7 startable in parallel. The risk has MOVED: statement
risk is retired; the live risks are (a) proof-depth risk on the descent's δ=1 strict-transform
arm (mitigated: Codex-first mandated, per-edge-kind map banked), (b) integration-shape risk on
the multiAffine primed wire — the descent proof lives DOWNSTREAM of the atlas (import cycle,
seat-L3T2's finding), so its stub closes only at the Assembly/SUMMIT-RETIRE swap, meaning
case1'/case2' keep a flow-through sorryAx even after boostReady lands — the "wall falls"
headline must be stated modulo that stub (name = content), and (c) coordination risk of three
independent writers (two proof seats + arch-C) converging on MonumentAssembly — held by the
single-writer pens + labeled stagings. The controller's job in this phase: keep the seats
decorrelated, gate integration on re-derived evidence, and resist declaring wall/summit
milestones ahead of the swap actually landing.
