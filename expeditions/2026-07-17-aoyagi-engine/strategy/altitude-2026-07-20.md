# Altitude note — 2026-07-20 (phase transition: close-the-spine → bank-conditional + re-scope)

Written at the phase boundary the heartbeat marks. The close synthesis reads this before the
journal chronology. Companion to `altitude-2026-07-19.md` (the four-lane-frontier altitude).

## Where we were, where we are

**Was** (07-19 → 07-20 morning): the endgame was "close the last wall." The fold-Jacobian
cocycle fell (t14, clean-three), the R7 LeafJacobian discharged, `chartBridgeFaithful_buildTree`
narrowed to ONE mathematical hole (the value walk) + the cover. The plan: land the value lane
→ hbox → mint → unconditional clean-three → PR, this expedition.

**Is**: two nested structural faults surfaced from the value lane in hours — (1) the Lean α
UNDER-implements Aoyagi's Q,P (interior Schur only; pnp-diag), (2) the fold evaluates
LEAF-first but Aoyagi's reduction is ROOT-first, so leaf-first computes a genuinely WRONG RLCT
(pnp-rg). The fix is faithful-mandatory, bounded, and — the decisive reframe — the corrected
fold must be re-derived root-first REGARDLESS of in-place vs fresh, so in-place saves nothing
and re-opens the merged/gated payoff cone. **The call (unanimous): bank the CONDITIONAL SPINE
(landed, clean-three-modulo-hypothesis, integration-verified) + re-scope the fold rebuild + α
completion + value lane to a focused root-first-from-the-start follow-up.**

## The altitude read — why this is progress, not retreat

1. **The durable deliverable is real and large.** The conditional spine
   (`aoyagi_learning_coefficient_of_chartBridgeFaithful`) is the ENTIRE engine + architecture +
   RLCT assembly, clean-three above the atlas, with the two atlas frontiers as an explicit,
   honest hypothesis that discharges to unconditional by ONE `exact` when the follow-up lands.
   Everything the expedition built stands; nothing is lost to the rework. This is the "stackable,
   hole-free layer" the disposition asks for — a solid floor the follow-up stands on.

2. **The fault was caught by design, not luck.** The structural reason it surfaced when it did
   (the ASYMMETRY, and the single most reusable finding of the run): the Jacobian is order-blind
   (det = ∏|det Cᵢ|) and the cover is image-based — both TOLERATE a leaf-first, α-under-implemented
   fold and pass green. Only the loss LOWER BOUND (the value lane) reads the actual diagonal and
   is order- AND gauge-sensitive. So a green build over four ratification passes was NECESSARY and
   NOT SUFFICIENT; the value-sensitive consumer + the decorrelated pen-and-paper hunt were the only
   instruments that could catch a wrong-RLCT spine — and they did, before it shipped. The
   compass/bedrock discipline earned its keep concretely (calibration 20).

3. **The follow-up is well-scoped and tractable, not a slog** (conditional on pnp-full clean):
   the α completion is the SHARED LINCHPIN — decoupling the residual both makes the value leaf
   diagonal AND collapses the root-first cocycle to a clean per-layer suffix sum (M, not the
   L-compounding of the current incomplete α). Built root-first + α-complete from the start, the
   follow-up gets: the M cocycle natively, the isolable value maintenance (child chart outermost
   on the reduced prefix — the acc-confounder gone), the decoupled α — all reusing the untouched
   engine/ledger/oracle/tree/DivBirthInv + the battery-validated root-first value design + the
   rebuild ledger + the α cells. It re-derives ONLY the fold, deliberately, in the right order.

## What the follow-up inherits (the handoff, at altitude)

- **Design closed**: the value lane (loss-t15) — three-state InvVal3, prefix re-base,
  clearedOf/dropThreshold/bmon, all battery-validated in the root-first frame; the isolability
  argument; the parametric det-1/srcBox templates (order-blind). This is the follow-up's spec.
- **Gauge target pinned**: α = full Q,P (interior Schur + pivot-col Lg [layer-S-local] + pivot-row
  Rg [cross-layer forward S→S+1]); confirmed to make the leaf exactly diagonal root-first
  (pnp-rg); the ONE open sub-wrinkle is Lg's det-1 realization (transvection / new atom / issue-3
  — pnp-full is adjudicating).
- **Mint ready**: the conditional spine + the prestage arm + the relocation plan (mint-t19) —
  the unconditional close is one `exact chartBridgeFaithful_buildTree` away.
- **Owed post-spine (unchanged)**: R7 completeness, the (2,2,4) witness, R4-small, zero-width(b),
  and the next-expedition native-λ runway (scout-lr2) — all off the λ cone.

## The meta-lesson (for the harness, not just this paper)

A construction ratified "faithful to X" must be gated by the consumer most SENSITIVE to X, not
the first/cheapest one. Four calibration misses (15/16/19/20) trace one root: fidelity claims
verified against det/measure/image consumers that are blind to order- and gauge-dependent faults.
The value/lower-bound consumer is the gate; the decorrelated hunt is the instrument; a green build
is the floor, never the ceiling. The order-kill was the sharpest possible demonstration — it would
have shipped green and WRONG. Pausing to bank-honest + re-scope-clean at this ceiling, rather than
patch a wrong-order construction under runway pressure, is the disposition working as intended.
