<task>
Adjudicate ONE sharp truth-value with exact reasoning: does a symmetry-orbit transport carry a
COVER (a set-containment), or only an ideal? Give your own independent GO/NO-GO + the single thing
most likely to break it. Do NOT rubber-stamp; a stated conclusion is NOT provided.
</task>

<setup>
We resolve the singularity of a homogeneous loss K(w) = ||A(w)||_F^2 at w=0, where A = C^1 C^2 ... C^L
is a product of matrix factors and w is the flattened tuple of all C-factor entries. The resolution is
an iterated blow-up: a finite tree whose leaves are charts g_c : (resolved coords) -> w-space, and we
need the "cover"
      ball(0, rho)  subset  Union_c  g_c '' dom_c        (dom_c compact),   [empty-escape form]
i.e. the chart images of compact source boxes cover a neighbourhood of 0 in w-space.

FACT (verified): the tree-building oracle emits ONE CANONICAL chart per combinatorial step (it does
NOT fan over the |S| geometric pivots of each blow-up center; the pivot column is pinned to a canonical
value). So the emitted "canonical atlas" covers only a "canonical cone" C_can = { w in ball : at every
node the argmax-modulus pivot is at the canonical position }, and a target whose dominant coordinate is
at a NON-canonical position escapes it (an exhibited eps*e_q escape).

Proposed fix ("transport"): the loss has a symmetry group G generated by (a) row-permutations of C^1,
(b) column-permutations of C^L, and (c) INTERNAL permutations C^(s) -> G_{s-1} C^(s) G_s^{-1} with
G_s a permutation matrix (these cancel in the product). Claim: the full atlas = the G-orbit of the
canonical atlas covers the ball.
</setup>

<facts_established>
Exact sympy checks (no floats):
  - K = ||A||_F^2 is invariant under all of G (row/col perms and internal permutations verified at L=2);
  - every generator of G acts on w-space as a COORDINATE PERMUTATION of the flattened C-entries (hence an
    l2/sup ISOMETRY: it maps closedBall(0,rho) onto itself);
  - a column-perm of the outer factor induces a CLEAN column-perm of the depth-2 Schur residual
    Delta = C22 - C21 C12 (transport acts cleanly at depth, not just at the root);
  - the subgroups fixing an already-used pivot act as the full symmetric group on the remaining block,
    so G acts TRANSITIVELY on all (output-row, internal-index, input-col) pivot positions;
  - a target's dominant coordinate at position q is carried by some sigma in G to the canonical position,
    so sigma(target) lands in C_can.
</facts_established>

<sub_questions>
1. Does G transitive on pivot positions + G acting by isometries actually give
   Union_{sigma in G} sigma(C_can) ⊇ ball, i.e. does the orbit of the canonical cone cover the ball
   (up to a measure-zero tie locus)? Or is there a gap between "every point's dominant coord can be
   canonicalized" and "every point lies in some sigma(C_can)" (C_can constrains argmax at EVERY node,
   not just the top one)?
2. Is transporting a COVER (set-containment of images) legitimate where transporting only the IDEAL
   <A_ij> would NOT be enough? i.e. does sigma . g_canon have image = sigma(image of g_canon), and is
   each sigma . g_canon a genuine resolution chart (Jacobian weight, a.e.-injectivity, compact domain
   all transported)?
3. The one thing most likely to break the COVER (as opposed to the ideal): name it.
</sub_questions>

<output_contract>
- One-line VERDICT: GO (transport carries the cover) | NO-GO (obstruction) | conditional (state condition).
- Sub-questions 1-3 answered, fact vs inference separated.
- The single most-likely-to-break point, concretely.
</output_contract>

<grounding_rules>
Reason from setup + facts. Distinguish PROVE from INFER. If a fact looks wrong, say so. No conclusion
is stated; reach your own.
</grounding_rules>
