<task>
Same DLN (3,3,4) resolution setting as before (loss L = sum of squares of the 12 entries of
P = A1 @ A0; A0 3x3, A1 4x3; 21 coords). We have a group G of LOSS SYMMETRIES: coordinate
permutations sigma with L o sigma = L, generated by (row-perm of A0 + matched A1-column-perm) and
(col-perm of A0). G acts transitively on A0's 9 entries (the 9 possible node-1 "dominant" pivots).

The canonical resolution chart is
   g_leaf(20,p2,p3) = bb(C0,20) o shearH o permP o bb(C1,p2) o bb(C2,p3),
where bb(C,p) is the monomial blow-up (pivot p, direction set C). It resolves the leaf whose node-1
dominant is A0[0,0] (=slot 20). For a general node-1 dominant p1 = sigma(20) we form the
WHOLE-CONJUGATE chart
   g_c = sigma^{-1} o g_leaf(20,p2,p3) o sigma.
Using sigma^{-1} o bb(C,p) o sigma = bb(sigma(C), sigma(p)) [a permutation of centre and directions],
this DECOMPOSES into direct atoms:
   g_c = bb(sigma(C0), sigma(20)=p1) o [sigma^{-1}(shear)] o [sigma^{-1}(perm)]
             o bb(sigma(C1), sigma(p2)) o bb(sigma(C2), sigma(p3)).
So each blow-up is at a PERMUTED centre sigma(p) with permuted directions sigma(C).

CONTEXT for the distinction that matters (Lean build design): we must decide whether g_c is
 (A) a genuinely NATIVE chart -- i.e. the resolution ALGORITHM, run on the configuration whose
     node-1 dominant sits at p1, would ITSELF select the blow-up centre p1 with directions sigma(C0)
     (and likewise the inner centres), via its own centre-selection rule (call it stepUpdate); or
 (B) merely a canonical chart RELABELED by a global transport map (a "Transport334" that carries the
     canonical answer to p1 by fiat), which is bookkeeping, not a native resolution.
The resolution's centre-selection rule picks, at each node, the pivot = the position of the current
dominant/largest-support entry, and blows up the "cross" (row+col directions) through it. The loss and
that rule are the only inputs.

We separately verified (exact toric-LP, all 288 leaves) that every g_c has true rlct >= 4, a single
pivot-cross survivor, and Jacobian exponent >= 7 on the survivor support (divisorMin >= 8), and that
rlct(g_c) = rlct(g_leaf(20,p2,p3)) exactly.
</task>

<output_contract>
Three sections, terse:
1. NATIVE-vs-TRANSPORT: Is g_c native (A) or transport (B)? The crux is whether the centre-selection
   rule stepUpdate is EQUIVARIANT under G: does "run stepUpdate on the p1-dominant config" produce the
   centre sigma(20)=p1 with directions sigma(C0) that the decomposition gives? State the precise
   condition on stepUpdate under which bb(sigma(C),sigma(p)) is exactly what the native algorithm
   emits, and whether the given rule (pivot = dominant position; blow up its cross) satisfies it.
   Flag any way it could FAIL to be native (e.g. tie-breaking, direction-set convention, a node where
   sigma does not map the canonical centre to the native one).
2. VERIFICATION-SOUNDNESS: Is checking "toric-LP rlct>=4 + single pivot-cross survivor + jac>=7 on
   support + rlct(g_c)==rlct(canonical)" on all 288 a SOUND way to conclude divisorMin>=8 uniformly?
   Name any leaf-class the check could miss (e.g. a leaf where the LP optimum is attained off the
   survivor axes, or the survivor is not single-entry) and whether the reported quantities catch it.
3. RESIDUAL RISK: the single most likely way this "all-288 native, divisorMin>=8" conclusion is still
   wrong or overclaimed, and the cheapest exact test to close it.
</output_contract>

<grounding_rules>
- Reason from the given structure; do not invent the stepUpdate rule beyond "pivot = dominant
  position; blow up its cross" -- if you need more of it, say exactly what you need.
- Separate provable fact from plausible inference.
- No code. Give the reasoning and the cheapest discriminating test where relevant.
</grounding_rules>
