# Genesis architecture cert — why the transform-only engine (2026-07-17)

*Controller-authored at genesis, persisting the founding adjudication (operator-directed). The
decorrelated inputs: an external three-seat audit (capstone/banked/frontier), an external paper
read + the aoyagi-tree-reader seat (paper-read-cert.md), the infra-reuse-map seat
(reuse-map-cert.md), and the predecessor's own obl2form verdict (its LATE-120).*

## The zero-slack theorem (the organizing fact)
The target exponent is exactly tight: c' runs to ½·minAdm and minAdm is achieved at binding cells.
Any decomposition step that loses positive exponent anywhere is therefore FALSE at binding cells —
not weak, false. Every predecessor failure was a lossy factorization (5 decoupling forks; route-B's
hpiv; the σ_min bound not p-integrable; the pointwise density fold; the naked Gram-decorated IH,
ε-counterexample; obligation-2's naked weight). Every survivor was exact (CoV, ideal identities,
stratify-and-recurse). Consequence, now a standing rule: exact-steps-only inside the engine.

## The obligation-2 refutation (the immediate trigger)
The predecessor's terminal socket ∫_{A'} det(Q(A')Q(A')ᵀ)^{−(M₀−t)/2} < ⊤ (all M, t) is FALSE:
- (1,1,1), t=0: integrand |y|^{−1} over the box — one-variable divergence.
- (4,4,4,4), t=2: det(QQᵀ) = det(A₂)²det(A₃)² exactly (A₂ square), so the integrand separates and
  each factor diverges (lct(det_n) = 1 < a = 2; shell measure P(|det|<ε) ≍ ε, verified numerically
  n=1,2,4).
Independently found by the external check and the predecessor's obl2form (2 decorrelated lines,
same witnesses). Statement carried unused hypotheses (c', hIH) — the tell. Battery:
`w-naked-weight-111.py`, `w-naked-weight-4444.py`. The true coupled object is not needed on this
expedition's route — the wall object never forms (see below).

## The route decision
Integrate-early (peel a layer by integrating front variables → Gram-weight residuals → re-analyze)
manufactures non-loss-shaped residual obligations whose singularity structure is route-specific;
the deepest one was adjudicated research-grade five decorrelated ways ON THAT ROUTE. Aoyagi's own
proof is transform-only: coordinate changes and blow-ups with tracked monomial Jacobians;
integration happens twice (top CoV pullback; leaf monomial reads). Her DLN construction is
complete on the page (paper-read-cert.md). Operator directive (2026-07-17): full native discharge;
build her machinery as a standalone engine; the socketed/cite path is superseded as a destination.

## The engine, four layers (mass estimate: reuse-map-cert.md)
A. Reduction (Lemma 2/Thm 3 peel) — transcription over banked shear/pivot analogues.
B. Resolution tree (pp.14–22 double induction) — THE BUILD; node data must carry divisor-support
   (sharing) maps: flattening provably breaks (g-coupled-binding-334.py, g-delta-flatten.py).
C. Coverage — THE HARD PART (paper asserts; we reconstruct): charts cover; no untracked chart has
   a smaller ratio; circularity guard (no use of rlct = c*). Design-first, decorrelated.
D. Thresholds + budget — banked wholesale (monomial reads S2-free; minAdm/QIP family; atoms).
Interface: the engine owes exactly `RouteMBoxThresholdFinite (H−r)`; consumer stack proven
(HeadlineGenAssembly, clean-three, force-printed). Worked precedent: RouteMBoxThresholdRR4.

## Standing rules distilled (in brief.md; enforced via battery + review)
1. Exact-steps-only inside the engine. 2. Truth witness at pin time for every obligation.
3. Def-3's printed inequalities are never transcribed (g-def3-broken.py).
