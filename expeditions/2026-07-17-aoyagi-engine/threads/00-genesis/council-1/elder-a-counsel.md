# Council #1, seat A (independent) — controller-condensed record; delivered 2026-07-17 17:51

VERDICT: ADOPT engine-route, gated on three corrections. Battery re-run by the seat (7/7 correct);
killed-route mechanism re-derived from the scripts, not the cert summary; both interface anchors +
the RR4 precedent checked against the territory.

FINDINGS (all integrated at council #1 — see compass.md):
1. RR4 PRECEDENT OVERCLAIM: RouteMBoxThresholdRR4's own docstring (RR4.lean:12–21) marks its inner
   SchurCore/front-peel mechanism a wall at depth ≥ 3 (front-peel IS integrate-early). RR4
   precedents the OUTER plumbing + threshold-reachability only. "Everything else is transcription"
   was visible-progress framing: TWO cost centers — coverage (hard math) + Layer-B tree (hard Lean,
   no inner precedent). → compass live-question rewritten; rr4 landmark why re-scoped; Layer-B risk
   MEDIUM-HIGH.
2. HIDDEN-LOSS RISK at the glue: "non-deepest domination" implemented as a reweighted-residual
   bound is the naked-weight falsity one level up. Safe form: EXACT reduction to a smaller
   arity-IH instance + MinAdmMono. Battery has no glue-level witness — one required before any
   glue/coverage tide. → contract rewritten (one owner); witness commissioned.
3. EXHAUSTIVENESS HUNT: no-smaller-ratio is a universal claim; needs a decorrelated hunt lane
   (the expedition-level kill-condition). → standing counsel + coverage node note.
4. TYPE-STRENGTH BINDING of fork 3: sharing maps as typed fields (flattening = type error). →
   fork 3 amended; architect instructed.
5. Scope fences confirmed clean (θ, cite-1, Thm-1 not load-bearing); Eqs (1)–(5) transcription
   flagged as scope creep toward θ (≤-side banked). Layer-B "integrates anything = off-contract"
   fence kept verbatim in the driver docstring.
6. Landmark counsel: ratify 7, add region-glue as 8th (adjudicated at integration: theorem4
   promoted instead — it owns the mathematical content; the glue contract line + witnesses carry
   seat A's concern), exponent-ledger-bridge weakest-but-keep.
RISKS (ranked): coverage exhaustiveness (kills) > Layer-B build weight > glue lossy re-entry >
theorem4 orphan/cycle > slack-form landing > scope creep.
