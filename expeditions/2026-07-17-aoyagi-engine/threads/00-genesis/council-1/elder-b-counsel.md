# Council #1, seat B (independent) — controller-condensed record; delivered 2026-07-17 17:51

VERDICT: ADOPT engine-route with three repairs (one load-bearing + unowned). Battery re-run (7/7);
mechanisms re-derived from source.

FINDINGS (all integrated at council #1 — see compass.md):
1. DECORATEDDESCENT DUPLICATE-SKELETON (unique to this seat): TWO ∀M routes coexist in the tree —
   `routeMBoxThresholdFinite_of_decoratedPeel` (SOUND-BUT-DEAD, AxCheck:1012–13: the plain IH
   cannot carry the truncated Gram weight at the zero-slack cut — the wall, in the tree, named)
   and `routeMBoxThresholdFinite_of_decoratedDescent : DecoratedDescent → ∀M …` (LIVE-conditional,
   clean-three, AxCheck:1006). The engine is the CONTINUATION of the decorated (tracked-coupling)
   insight, realized as Aoyagi's charts. P6 risk: two pulls toward one Prop. DISPOSITION REQUIRED
   in the adoption commit: coverage-theorem constructively witnesses DecoratedDescent (engine
   discharges the conditional route) OR the decorated hole is tombstoned. → fork 6 added; architect
   instructed to type-check the fit and report.
2. THE ORPHAN: the non-deepest→IH reduction had three nodes pointing at each other and no owner
   (compass named two owners in one clause; region-glue assumed it; theorem4 parked). → contract
   split (theorem4 = reduction; region-glue = assembly; Layer C = tracked cells only); theorem4
   PROMOTED to landmark #8 with a lane.
3. FORK-2 WHY OVERCLAIM: "her paper never forms that object" → corrected to "never forms it NAKED;
   the coupling is tracked through every chart" + the decorated-route lineage. → fork 2 rewritten.
4. "Expected mechanical via co-null covers" = fork vocabulary (P5) → stamped UNWITNESSED + battery
   witness required.
5. RR4 honest form: the (r,r,4) discharge is closed by COMPOSITION (schurRecStep_four sorry-free at
   RouteMSchurFiring.lean:2145; the top theorem carries hstep) — "0-sorry end-to-end" qualified.
6. Landmark counsel: ratify 7 + ADD theorem4-localization (adopted); no 9th.
RISKS (ranked): coverage (kills) > non-deepest reduction unowned (silent hole) > duplicate-skeleton
pull > region-uniformity unwitnessed > pp.14–22 bookkeeping (bounded by the typo ledger).
