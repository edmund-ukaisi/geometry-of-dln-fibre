# threads/ — family index (phase-close 2026-07-27)

NOT bulk-moved: compass witnesses reference thread paths — moving them silently breaks pointers.

- **(3,3,4)-PHASE-CLOSED history** (do NOT extend; kept for compass/journal pointers):
  `01`–`42`, `L3T*`/`L4*`/`L6`/`L7*`/`M4*`, `pnp-*`, `seat-*`, `reroute-R0`/`reroute-R1`.
- **General-d-relevant** (the surface map's engine/probe area):
  `reroute-R2-tubecover`, `reroute-recursion-rzero` (#172), `reroute-R3-*`.

The general-d phase spawns fresh threads. Surface + crux + first-rung probe: `../general-d-surface.md`.
