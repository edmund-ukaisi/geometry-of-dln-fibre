# R3 ideal-side de-risk — elaboration (persisted by controller; seat barred from writing reports)

**Seat:** reroute-R3derisk (decorrelated pen-and-paper exact-algebra of the banked ideal atoms + independent
Codex xhigh — CONVERGE). Codex artifacts: codex-{prompt,answer}.md (this dir).

## Headline (nuanced, honest): NOT a mechanical wall; NOT a clean GO.
The three feared MECHANICAL walls are NONE of them walls — mechanisms PROVEN green in-repo, arbitrary
block-size, coupling-independent. BUT the R3 unified fold AS-SCOPED (route P: the full two-sided hideal
Resolution atlas -> exists_coreResolution) CONSUMES the general-d terminal single-chain principality atlas =
the TRACKED COUPLED MONUMENT (instance-verified, no counterexample, structurally expected, NOT proven general).
Now precisely localized as the ONE thing R3's ideal conjunct depends on beyond the proven mechanisms.

## Key discovery: R1's Corank2AdmissibleLeaf222.lean already discharges the MECHANISMS (general/coupling-indep)
- terminal_bezout_collapse (∀ M D): unit≡1 ⟹ terminal PrincipalInv on FULL V, no shrink; reverse Bézout
  cofactor = constant 𝟙[i=i₀] (no 1/unit).
- coupled_maintenance_region_covers (∀ n D): both ideal directions on univ, coupling cᵢ ARBITRARY (enters as
  continuous cᵢ·rᵢ, never a denominator), given the b-chain (rᵢ = bᵢ/b_p an exact monomial).
- coupled_terminal_region_covers: the coupled corank-2 residual block -> PrincipalInv on FULL univ, unit≡1
  coupling-INDEPENDENT (coupling lives in the dominated b₂,b₃ ∈ ⟨b₁⟩).

## (a) COMPOSITION down a general-d coupled branch — COMPOSES (both directions), PROVEN.
RegionRepresents.trans/.mono/regionRepresents_comp compose; reverse as cheap as forward (radial pivot quotient
= 1 EXACTLY; block-elim reverse cofactor cᵢ·rᵢ polynomial given the b-chain). Arbitrary depth+coupling only
enlarge finite sums/products of continuous cofactors. The "V-lower mirage ×3" was a VALUE-level info-loss
(threshold can't see shared divisors) — a DIFFERENT problem; the ideal invariant carries full symbolic support.
Residual: b-chain threaded as a conjunct (local to run-block, holds by pivot-minimality b_p | bᵢ).

## (b) nbhd=univ general or corank-2-special? — CONDITIONAL (general via unit≡1; unit≡1 general-d = the open end).
MECHANISM general (unit≡1 ⟹ region=univ; unit≡1 coupling-independent at the coupled corank-2 witness). unit ≡ 1
EXACTLY by construction (unipotent shears det≡1 + exact-monomial blow-ups). Where unit≡1 the shrink is VACUOUS.
Residual (the open end): that EVERY leaf's unit≡1 general-d = terminal single-chain principality — instance-
verified (3,3,4), (4,4,4) t=(2,0), (3,3,3,2,2) t=(2,2,1,0) by Gröbner ideal-EQUALITY, no counterexample — general
proof = the tracked monument (worked.tex:651-664; thread-28 deeper-mixed open end).

## (c) deep clearing reads a shallower-cleared coord? — SAFE (mechanism); corner-join at deep mixing = same open end as (b).
Shallower-cleared coords appear ONLY as ∏u monomial spectator left-factors, never denominators/re-clear targets;
clearing local to run-blocks by pivot-minimality; sharing reconciled at the corner-join. Even a hypothetical deep
multiplier reading a cleared coord would break the fresh-core induction / terminal principality = the SAME open end.

## STOP-OR-GO: GO on the mechanisms; the fold's ideal conjunct DEPENDS on an inferred (not proven) monument.
No demonstrated ideal-side MONUMENT WALL. Both decorrelated sources agree the unresolved part is the ARBITRARY-
DEPTH preservation of the exact chart normal form (unit≡1 / single-chain terminal principality / corner-join at
deep mixing) — a WELL-SUPPORTED INFERENCE, not a proof. Objects-only NOT justified now (no counterexample, sound).
**THE GENUINE DECISION (elder + operator):**
- Route P (as-scoped): the full single-chain principality atlas — CONSUMES the tracked monument (inferred-general).
- vs the value-floor re-scope (V / F-lower-coupled, ideal-route-render §209-281): rlct ≥ ½Mval at the MINIMISING
  branch only, coupling-ASSISTED (shared deep factor RAISES the pullback -> ⪰ b_{k₀}² EASIER), NO single-chain
  collapse required, "far smaller surface", claims to AVOID the monument. [CAVEAT: mirage-adjacent — the V-lower
  was retired ×3 as a mirage; the seat argues that was a value-level info-loss, a different problem. Needs elder
  adjudication vs the retired-mirage history.]

## Cheapest discriminating probe (both sources converge): the TRUE gate before the deep fold commits.
Pick ONE deeper-mixed instance (≥3 shared deep factors / higher-L mixing beyond the banked witnesses); compute over
the polynomial ring (Gröbner) SIMULTANEOUSLY: (i) I_terminal = ⟨b_{k₀}⟩ exactly, (ii) det Dg = ±monomial exactly
(unit≡1), (iii) whether the corner-join needs ANY localization. Clean ⟹ inferred→verified one more rung (GO route-P
with confidence). Denominator / unavoidable localization / genuine cleared-coord dependency ⟹ monument bites ⟹
re-scope to the value-floor. Exact-algebra (sympy/Sage/Gröbner), ~1 seat-session.

## Residual risks (ranked)
- R1 [HIGHEST]: general-d terminal single-chain principality (unit≡1 every leaf / corner-join at deep mixing) — the
  tracked coupled monument. Inferred, not proven; instance-verified no-counterexample. Route-P consumes it.
- R2 [HIGH -> R2]: the FAMILY-COVER (union of leaf domains tiles the parent over the real branching tree) — co-located
  on the unified fold via the elder's "same fam l" coupling.
- R3 [MED]: threading the b-chain conjunct (local-to-run-block, pivot-minimality; mechanical, carry not assume).
