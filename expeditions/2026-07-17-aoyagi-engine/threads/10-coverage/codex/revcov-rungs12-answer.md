Q1. (i) **\(1+J_1(M^{(S+1)}-J)\).** There are \(J_1(M^{(S+1)}-J)\) block coordinates plus \(u_{s,k}\).

(ii) **That same number of standard pivot charts; all must be represented.** This is a blow-up fact: a faithful standard cover has one chart per center coordinate, though symmetric charts may be handled by one uniform argument.

(iii) **No—the two displayed cases are representatives.** Fact: 1(1) is the \(u_{s,k}\)-pivot chart, while 1(2) represents one of \(J_1(M^{(S+1)}-J)\) block-pivot charts. Inference: “by a blow-up process” and comparability likely compress the repeated algebra; comparability itself does not supply the omitted geometric coverage.

Q2. **No; there is a gap.** With \(u_{s,k}=0\), the point \((c_{11},c_{12})=(0,\varepsilon)\), \(\varepsilon\neq0\), lies in neither image: the \(u\)-chart forces both entries to vanish, and the \(c_{11}\)-pivot chart forces \(c_{12}=c_{11}c'_{12}=0\).

Q3. **Yes, with one qualification.** The unrestricted formulas are the \(d\) standard affine charts; the bounds \(|u_k|\le1\) select closed max-normalized pieces of them. Their images equal \([-R,R]^d\), since every point has a maximal-modulus coordinate. A fixed chart covers only \(\{|x_k|\le|x_i|\ \forall k\}\), and its Jacobian determinant is **\(u_i^{d-1}\)**.

Q4. **The full pivot family is required.** Descendants remain inside their parent-chart images, so later branching and label comparability cannot recover a direction already omitted; representatives suffice only if formally instantiated for every pivot coordinate.

**BOTTOM LINE: (a) recursion descends into the full per-node pivot family (one chart per center coordinate).**