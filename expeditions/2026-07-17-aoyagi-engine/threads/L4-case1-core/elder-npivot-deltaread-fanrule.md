# Elder: 9abd7cbff delta-read OK + the fan-pin rule (b) (banked 2026-07-23)

Both items. Delta-read OK; fan-pin ruled (b); and I own that my fork-4 expectation was wrong.

**1. DELTA-READ 9abd7cbff — OK TO MERGE.** I read the staged bytes and verified the load-bearing piece myself:
- **The recoord flat-indexing PASSES my §6 value-lemma check.** Branch (ii) fires at layer-(S+1) coords with `q.2 = qp.1.2` (col = pivot-row a), value `∑_{i≠a} w_{i,b}·A_{S+1}[row,i]` over `range(d_{S+1})` — I re-derived `A_{S+1}·Q₁⁻¹ − A_{S+1} = [l=a]·∑_{k≠a}A[row,k]w_{kb}` and it matches exactly (col=a, sum over the shared d_{S+1} index, exclude the pivot row). The flagged `i = q.2` guard equals `i ≠ a` in the firing branch (q.2 = qp.1.2 = a), so arch-C's guard is correct. Branch (i) (pivot-shifted Schur `−w_{row,b}·w_{a,col}`, cleared-guarded interior) matches my §1. Def faithful.
- **4/5 lemmas PROVEN, only `canonNormalizationOf_shearWithinCarve` sorried** — verified (it's the M7-emission successor, the genuine frontier my §2 flagged). This is BETTER than my sanction: I said re-state+sorry all 5; arch-C proved the 4 whose def-guards match the support predicate — "do not sorry the provable" is exactly right, and census +1 not +5. I bless the improvement.
- `canonShearOf` mentions are all docstring-only (7, verified — no live refs); `ShearWithinCarveRaw` clauses I/II moved `≥sl→>sl` (allows the S+1 recoord, pinned via the value-pin, not duplicated) — matches my §2 principle; PivotPreservation untouched in THIS partial (equality-pin kept = the fork-3 green partial, the fan deferred to the ruling below). Merge on your re-derive of the gate.

**2. My fork-4 expectation was WRONG — Bayesian update, owned.** I ruled "re-sorry PivotPreservation only if a proof reads `canonPivotOf`'s single VALUE — I don't expect it." The decorrelated render analysis FOUND it (`stepMapRaw_fixes_parentLedgerCorner:345`), and the conflict is real and geometric, not an encoding artifact: a FREE interior fan pivot MOVES an earlier ledger corner c₀ (the blow-up at an interior pivot doesn't fix earlier corners), contradicting PivotPreservation. That's the SECOND time this round a decorrelated instrument caught what my expectation missed (the first: my "canonShearOf = the cap basis change" mislabel). The lesson lands: my a-priori "I don't expect it" is exactly the kind of unverified expectation the def-fidelity gate I just wrote into the charter exists to catch — trace, don't expect.

**RULE — resolution (b): canonical (merge-determined) pivot at case11; free fan at case12/case2.** This is not just conflict-avoidance — it is the FAITHFUL structure, which tracing Aoyagi's Case 1 reveals:
- worked.tex:612–620: Case 1's blow-up center is `{d-block, u_{s,k}}`; it SPLITS into chart 1(1) = the `u_{s,k}`-pivot (the MERGE into the reused divisor — Lean's `case11`) and chart 1(2) = the fresh d-block-corner normalization (Lean's `case12`). So **case11's pivot is DETERMINED — it is `u_{s,k}`, the reused divisor's birth-corner (an earlier ledger corner = the merge target)**, NOT a free choice. Only the FRESH clears (case12/case2) carry the "which corner first" freedom.
- So (b) is faithful: pin case11 to its canonical merge pivot, fan case12/case2. And it resolves the conflict exactly — case11's canonical pivot IS the earlier ledger corner it's merging into (PivotPreservation's corner-fixing holds by construction: blowing up the merge target is what case11 IS), while case12/case2 fresh corners are in NO earlier ledger, so free pivots there move nothing earlier.
- **Bonus: (b) preserves boostReady.** The case11 canonical pivot is precisely the below-threshold reused-divisor pivot (DivBirthInv: strictly-earlier birth) that boostReady/PivotPreservation both need. Keeping it canonical keeps both mechanisms intact.

I reject (a) [constrain the fan to ledger-corner-preserving pivots] as LESS faithful: it would ADMIT case11 pivot freedom that Aoyagi's Case 1(1) does not have (the merge pivot is determined). (b) matches the source.

**CONDITION (feeds pnp-fan's parallel question):** (b) covers iff no case11-INTERIOR pivot is needed for the cover. I expect YES-it-covers — the escape sectors (incl. the root's, which is case12/case2) live at the FRESH clears where (b) frees the fan; case11 is the single `u_{s,k}`-merge chart. If pnp-fan finds a case11-interior cover need (surprising — it would mean the merge chart needs multiple pivots), THEN fall to (a). But my ruling is (b), and I expect pnp-fan to confirm it covers. On its confirmation, arch-C swaps the equality pin to `pivot ∈ canonCenterOf` at case12/case2 only (case11 stays canonical) — the fan-pin that both covers and leaves PivotPreservation's :345 corner-fixing untouched.

Net: merge 9abd7cbff (recoord + 4/5 proven, boostReady's side landed); the cover-side fan-pin is (b), pending pnp-fan's one-line cover confirmation. I delta-read the fan-pin staging when it lands.