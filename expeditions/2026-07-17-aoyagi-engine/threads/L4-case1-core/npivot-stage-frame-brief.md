# Elder stage-frame brief: the faithful N_pivot elaboration (banked 2026-07-23)

Both done: the stability confirmation (unblocks seat-L3T2 now) + the stage-frame brief (ready to hand to pnp-transport).

**(1) Gap B homogeneity STABLE under N_pivot — CONFIRMED, seat-L3T2 can build the moment the amended staging merges.** The deeper recoord `A_{S+1}→A_{S+1}·Q⁻¹` is right-multiplication = a linear mixing of layer-(S+1) COLUMNS, coefficients from layers ≤ S. `PerLayerDeg1From` asserts degree-1 only for ℓ ≥ supportLayer = S+1. The recoord (a) keeps degree-1 in layer S+1 (linear within-layer substitution), (b) leaves layers > S+1 untouched, (c) pushes any added degree only into layers ≤ S — which the claim EXCLUDES (the `fromLayer` threshold was designed for exactly the "child is degree-2 in the cleared layer below support" case). Vanishing survives too (`A_{S+1}=0 ⟹ A_{S+1}·Q⁻¹=0`). So the STATEMENTS (`coreGen_layerHomogeneous`, `foldResid_layerHomogeneous`, over layerCoords, ℓ≥supportLayer) are stable; `coreGen` isn't touched by N_pivot at all, and `foldResid`'s per-layer degree ≥ support is preserved.

ONE PROVISO for seat-L3T2 to be SWAP-SAFE (so it doesn't re-prove after the N_pivot round): the step atom `homogeneousDeg1On_comp_of_fixing` must be stated GENERICALLY — "any per-layer-linear substitution preserves HomogeneousDeg1On" — NOT hard-coded to canonShearOf. Both the current fold's substitution and N_pivot's (interior Schur + deeper recoord) are per-layer-linear, so a generic step atom proves once and survives the swap; a canonShearOf-specific one re-proves. Tell seat-L3T2 to build the generic step. (This is also just better bedrock — the homogeneity is a property of per-layer-linearity, not of the specific shear.)

**(2) Stage-frame brief authored:** `expeditions/2026-07-17-aoyagi-engine/threads/L4-case1-core/faithful-npivot-brief.md` — hand it to pnp-transport (warm on the witness + honest_clear). It's worked.tex-anchored (443–457 clear + deeper recoord, 565–567 carried form, 609–627 block-blow-up Cases 1&2), pinned to canonFlatten coords, over the three witnesses (2,2,2,2)/(3,3,4)/(3,3,2,2), with deliverables (N_p per chart corner+interior, deeper-recoord bookkeeping, b-chain/M_{s,k} preservation, per-fan-chart boost-readiness+monomialisation), pre-committed acceptance tests (honest_clear reproduces; pnp-fan's escape witness covered-AND-monomialised), and kill-conditions.

**THE ONE CHECK I want to flag as decisive** (§4.3/§6 of the brief): **b-chain / M_{s,k} PRESERVATION.** This is the check that distinguishes the FAITHFUL block blow-up from the composite fallback. A faithful `N_p` must reproduce Aoyagi's exact `b_i=∏_{t̃<i}u` and `M_{s,k}` (worked.tex:571/616/626) so the banked Object-B↔D bridge (`min M_{s,k} = minAdm = cCodim`) is UNCHANGED. If pnp-transport finds `N_p` does NOT preserve them, that's the kill-condition that forces the composite (with its own re-derivation of Object B's combinatorics + an explicit operator call). So this check gates the whole faithful-vs-composite question — I've made it a first-class deliverable + kill-condition, not a footnote. When pnp-transport's certificate lands, my ratification turns on it + the boost-readiness reproduction + the interior-pivot monomialisation.

Both dispatches you listed (arch-C amended staging with the canonFlatten e-pin + hpos + docstring rider + the hparent rider; cover salvage b + lemma Q; seat-L4C ChainCompat.base + (1b)) are consistent with my rulings — proceed. I ratify pnp-transport's certificate on landing, then author the Lean-facing verbatim (canonFlatten-pin spec + faithful N_p spec). Nothing blocks on me until the certificate or the amended staging hash lands for my delta-read.