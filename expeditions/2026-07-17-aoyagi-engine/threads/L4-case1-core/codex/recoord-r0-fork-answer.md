## R0 — NO

\(2u_{010}\) is divisible by neither active exceptional.

- For \(m=u_{000}\), set \(u_{000}=0\) and \(u_{010}\neq0\).
- For \(m=e_2=u_{011}-u_{010}u_{001}\), take \(u_{001}=u_{011}=0\) and \(u_{010}\neq0\). Then \(e_2=0\) but \(2u_{010}\neq0\).

Hence no continuous \(\beta\) near the exceptional locus can satisfy \(2u_{010}=m\beta\). Bare \(u_{010}\) is a projective ratio/clearing coefficient, not an exceptional coordinate or a \(b_i\)-factor: the active factors are generated by \(u_{000}\) and \(e_2\).

The coefficient \(2\) is not a real \(b\)-step. It comes from adding the same uncleared contribution twice: once from the original pivot-column term and once from the unpaired deeper recoord. A \(b\)-step supplies an exceptional factor, not a scalar duplication.

This conclusion assumes, as stated, that slot \(0\)’s extra column is governed by reused divisor \(1\). If Aoyagi’s row assignment exempted that slot, the ruled-field premise would change; nothing in the stated \(b\)-chain makes \(u_{010}\) a divisor.

**VERDICT: R0-NO. The crisis is real.**

## Ranked fork

1. **R3 — best route: an explicit unipotent pullback realization exists.**

   Do not map the pivot-column coordinate \(\gamma\) to zero. Retain it as a chart coordinate and parameterize the raw factors inversely. With
   \[
   Q=\begin{pmatrix}1&0\\-\gamma&1\end{pmatrix},
   \quad
   C_{\rm raw}=Q^{-1}\begin{pmatrix}1&\beta\\0&\Delta\end{pmatrix},
   \quad
   A_{\rm raw}=B Q,
   \]
   one gets
   \[
   A_{\rm raw}C_{\rm raw}=B\begin{pmatrix}1&\beta\\0&\Delta\end{pmatrix}.
   \]
   Concretely, \(u_{100}=w_{100}-\gamma w_{101}\), so
   \(u_{100}+\gamma u_{101}=w_{100}\). This map is triangular with Jacobian \(1\). In the current shear-before-outer-blow-up order, \(\gamma\) is already the chart ratio \(A_S[row,b]/p\); the pivot itself need not equal \(1\).

   **Logical:** the local algebra and determinant-one claim.  
   **Unverified:** compatibility with every general-pivot fold convention.

   Cheapest test: reverse the baked directions—Schur parameterization with \(+\gamma\beta\), deeper pullback with \(BQ\)—and check the \(2\times2\) residual and Jacobian.

2. **R2 — standard picture, but the proposed patch location is imprecise.**

   A blow-up chart does retain \(\gamma=A_{row,b}/p\) while the prepared matrix has a cleared column. That is standard. But `blockBlowupCoordQuot` itself already is the standard quotient \(p\mapsto1,\ x_i\mapsto x_i/p\). The missing object is the whole running-frame parameterization above, not merely a different quotient rule. Properly formulated, R2 is essentially R3 composed with the block blow-up; its total Jacobian is \(p^{|center|-1}\).

   Cheapest test: verify that full chart map has determinant \(p^{|center|-1}\) and clean pullback residual.

3. **R1 — ruled out.**

   Coefficient \(u_{010}\) still fails divisibility by \(u_{000}\) or \(e_2\). Declaring \(u_{010}\) exceptional would contradict the pinned divisor births and alter the Jacobian ledger.

   Cheapest test: reduce \(u_{010}\) modulo \((u_{000})\) and \((e_2)\); both remainders are nonzero.