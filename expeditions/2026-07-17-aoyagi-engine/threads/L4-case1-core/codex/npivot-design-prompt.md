<task>
Work ONLY from this prompt (do NOT read repo files). Write and run FRESH sympy in /tmp; adjudicate by
exact algebra. Correctness gates a Lean formalisation design.

CONTEXT. A resolution of a matrix-product singularity resolves, per step, a BLOCK blow-up of the ideal
generated by all entries of a residual block D (an n x m matrix of independent coordinates d_ij),
followed by a per-chart regular NORMALIZATION. The block blow-up has one affine chart per "pivot" entry
p=(a,b) in D: in the p-chart the pivot d_ab = u (the exceptional coordinate) and d_ij = u*w_ij for the
other block entries (w_ij the chart coordinates). A `Resolution` requires EVERY chart to MONOMIALISE
(the block becomes a coordinate monomial times a unit-triangular matrix) — including charts whose pivot
p is a STRICT-INTERIOR entry (a>0 and b>0), not just a corner (a=0 or b=0).

QUESTIONS (compute, do not assume):
1. Is there a per-chart normalization N_p (a coordinate change, ideally UNIPOTENT = det 1) such that for
   EVERY pivot p (corner AND strict-interior) of an n x m block, after the p-chart blow-up + N_p the
   block equals  u * (matrix with 1 at p, 0 elsewhere in p's row and column, and a Schur-type residual
   block off p's row/column)?  Test n x m in {2x2, 3x3, 2x3}, EVERY pivot.  Give the explicit N_p and the
   residual-block entries.  If some interior pivot CANNOT be monomialised by a unipotent normalization,
   exhibit it (that is the decisive negative).
2. What is the Jacobian |det| of (blockBlowupMap_p . N_p) as a function of u?  Is it exactly u^{|block|-1}
   (|block| = n*m = the codimension of the blown-up ideal), i.e. does the per-chart normalization add
   NOTHING to the block-blow-up Jacobian, so the exceptional coordinate's exponent equals the block
   codimension regardless of which pivot the chart uses?
3. The normalization also recoordinatizes an adjacent "deeper" matrix factor A' by A' -> A' * Q^{-1}
   (Q the unipotent pivot-column-clearing factor). Is this substitution LINEAR in the entries of A'
   (degree <= 1), with coefficients drawn only from the current block's coordinates (NOT quadratic in A')?
   Test on a 2x2 A' with Q = [[1,0],[gamma,1]].
</task>
<output_contract>
Numbered 1-3. For each: the explicit construction + the exact sympy + printed output. State clearly:
(1) does EVERY pivot (incl. interior) monomialise via a unipotent N_p? YES/NO + the residual block.
(2) is the Jacobian exactly u^{|block|-1}, pivot-independent? YES/NO.
(3) is the deeper recoord linear-in-A' with block-coordinate coefficients? YES/NO.
Flag inference vs computed fact.
</output_contract>
<grounding_rules>
Compute with sympy; do not hand-wave. If a step is ambiguous try both readings. Do not assume the
normalization exists — construct it or show it cannot.
</grounding_rules>
