### 1. CLAIM 1 verdict: CONFIRMED

**FACT:** \(D_J\) has dimensions \((M(S)-J)\times(M^{(S+1)}-J)\). Thus the running minimum controls the row/cleared-prefix side and the stopping value of \(J\), while the column side retains the raw next-layer width \(M^{(S+1)}-J\).

### 2. CLAIM 2a verdict: CONFIRMED

**FACT:** \(d=u_{s,k}d'\) is genuine factorization in the local function ring, exactly as encoded by the monomial factors in \(\operatorname{diag}(b)\). Mere vanishing on \(u_{s,k}=0\) is insufficient for continuous coefficients because it need not provide a continuous quotient.

### 3. CLAIM 2b verdict: conditional

**INFERENCE:** In Aoyagi’s analytic/polynomial normal-crossings setting, the per-divisor divisibility conjunction, together with the thresholds \(\widetilde t_{s,k}\), is the weakest equivalent encoding: distinct exceptional parameters are prime, so divisibility by each implies divisibility by their product \(b_i\). This equivalence fails for merely continuous functions; moreover, dividing out one factor need not preserve the others, so a continuous formalisation needs coherent product factorization, not isolated divisibility witnesses. The exponent ledger \(M_{s,k}\) is unnecessary for the reuse factorization itself but remains necessary for the final learning-coefficient exponent calculation.

### 4. CLAIM 2c verdict: CONFIRMED

**FACT:** The recurrence’s \(u_{s,k}\) denotes the exceptional parameter in the current blow-up chart, formed from the currently Schur-reduced residual, not an unchanged root-frame symbol. **INFERENCE:** Consequently its root-coordinate equation may be a transformed expression such as \(u_{011}-u_{010}u_{001}\); naming literal root \(u_{011}=0\) would generally identify the wrong locus.