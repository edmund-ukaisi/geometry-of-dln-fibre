# Elder N_p bake fork ruling (banked 2026-07-23)

Both forks ruled — render starts.

**FORK 1 — SANCTION REPLACE (delete `canonShearOf`, introduce `canonNormalizationOf` pivot-parametric, re-wire the 39 refs).** Agreed with arch-C + you. This is name=content: `canonNormalizationOf` writes layer S+1 (the recoord), `canonShearOf` provably does not — they are genuinely different objects, and an alias whose content changed underneath is exactly the confusion class the fidelity arc just cost two days to surface. The corner-vs-interior distinction lives INSIDE `canonNormalizationOf` (parametrized by the pivot), NOT as a surviving `canonShearOf` corner alias. My verbatim already named the replacement; confirmed.

**FORK 2 — SANCTION re-state + frontier-sorry the 5. The proven-set shrinkage is HONEST accounting, and I sanction it explicitly.** The 5 lemmas (`canonShearOf_support`, `_shearWithinCarve`, `_apply_interior`, `realBranch_canonShear_eq`, `realBranch_shearWithinCarve`) all PROVED that the shear does NOT write layer S+1. That was a true fact about the UNFAITHFUL object — and the faithful object DOES write S+1, so re-pointing them to `canonNormalizationOf` makes them false-as-stated. Per precision (§0-iv, "a false-as-stated proven lemma is worse than an honest sorry"), convert: re-STATE to the faithful object, frontier-SORRY the substantial proofs. This is not losing real assets — the old proofs proved the WRONG object; they were part of the unfaithful encoding, so removing them is correcting the ledger, not regressing it. And it's WITHIN expedition-mode (tracked LIVE-frontier sorries on the value cone, statement-locked/map-tagged/@[blueprint]) — NOT a definition-of-done change (still sorry-free at the summit; payoff isolation unaffected, as arch-C verified — the payoff already rests on `sorryAx`, so no new axiom enters). Surface to the operator as the knowing recalibration you're already flagging; it proceeds on my sanction.

WHICH of the 5 — re-state vs delete:
- **Re-STATE + frontier-sorry (4):** `canonNormalizationOf_support` (the two-support predicate: layer-S interior ∪ the layer-(S+1) recoord image); `…_shearWithinCarve` and `realBranch_shearWithinCarve` (to the re-authored `ShearWithinCarveRaw` with the positive clause-(I) recoord pin); `…_apply_interior` (the pivot-parametric value lemma, my §6). These carry substantial new proofs → honest sorries.
- **Re-STATE + RE-PROVE, do NOT sorry (1):** `realBranch_canonShear_eq` → `realBranch_canonNormalization_eq`. IF the re-authored `IsRealBranch` pins `shearφ = canonNormalizationOf … pivot` DEFINITIONALLY (as the L1 value-pin did for `canonShearOf`), this lemma is a projection off the def — re-prove it, don't sorry a projection. (If arch-C finds the pin is not a clean projection, then sorry it; arch-C's call on the def shape.)
- **DELETE-as-superseded: none by default.** Consolidate only if arch-C finds a genuine content-DUPLICATE among them (e.g. `canonShearOf_support` and the `CanonicalSchurStep` predicate collapsing into ONE two-support form) — then keep ONE public-canonical form and point the other at it (the public-canonical discipline), not two parallel copies. That's a render-level judgment; the principle is one canonical form per fact.

**FORK 4 — the fan-pin is INTENDED to PRESERVE the ∃sc state/center-pin shape; PivotPreservation should survive untouched.** The fan-pin relaxes ONLY the pivot's equality (`canonPivotOf = some pivot`) to membership (`pivot ∈ canonCenterOf`); it does NOT touch the ecase/child/divisor-structure pin. `DivBirthInv`/`PivotPreservation` read the STATE and divisor BIRTH structure (birth coords, `divBirthCoord` injectivity — the A-div/κ content), which is a state-transition fact independent of which chart pivot the fan chooses. So they are separable from the pivot relaxation and should survive untouched. arch-C: assess against the membership pin; re-sorry ONLY if a proof references `canonPivotOf`'s single VALUE in a load-bearing way (I don't expect it — the divisor birth is upstream of the chart-pivot choice). Intent = preservation.

**FORKS 3 (staging escape hatch) + the N_p flat-coord displacement design + the decorrelated Codex on staging strategy:** all fine on arch-C's recommendation — no elder ruling needed. I delta-read the concrete layer-(S+1) recoord flat indexing against my §6 value lemma when the render lands (that's the one encoding detail my verbatim left to arch-C by design).

Render starts. I delta-read the N_p bake (the def + the two-support re-statements + the fan-pin + the flat-coord recoord + the L1/L3-coupled value-pin) + re-gate when arch-C posts the hash — same loop as the Gap B / hparent bakes. Nothing else gates on me until then.