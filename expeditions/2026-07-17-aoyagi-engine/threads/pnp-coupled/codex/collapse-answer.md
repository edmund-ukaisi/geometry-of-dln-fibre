Q1. [FACT] Yes—the projective blow-up of the smooth coordinate center \(\{\Delta=0\}\) works for every \(k\); freeness of the \(k^2\) entries is load-bearing, not any rank property of \(\bar D S\).

Q2. [FACT] No—it extracts \(u\) but leaves the generally nonunit weak-transform core \(G=\|\bar D S\|^2\); monomial-times-unit occurs only in a terminal pivot chart of the full recursion.

Q3. [FACT] Monomialization is unnecessary for reading divisor ratios: one divisor proves the upper bound, whereas the lower bound requires an exhaustive resolution or equivalent integrability argument.

- [FACT] Each projective chart chooses \(\Delta_{ab}=u\), writes \(\Delta_{ij}=u\bar\Delta_{ij}\), and has discrepancy \(k^2-1\); a single global polar normalization is not an analytic chart.
- [FACT] No rank condition on \(\bar D S\) is needed for \(I(\Delta S)=uI(\bar D S)\); normal-crossing compatibility with earlier exceptional divisors is additionally needed for a resolution atlas.
- [FACT] Set \(a_E=\operatorname{ord}_E(I)=\min_{ij}\operatorname{ord}_E(P_{ij})\); then \(\operatorname{ord}_E(F)=2a_E\). If \(a_E\) denotes the scalar loss order instead, the denominator is \(a_E\), not \(2a_E\).
- [INFERENCE] Here all entries are \(E\)-divisible and the pivot entry has exact order \(1\), so \(a_E=1\) without block collapse; \(h_E=Mval-1\) gives \(Mval/2\).
- [FACT] Thus this real divisor alone yields \(\mathrm{rlct}\le Mval/2\).
- [FACT] For the reverse inequality, visible divisors on an unresolved chart cover do not suffice: \(G\) may create lower-ratio divisors; an exhaustive recursive cover controlling every divisor suffices, while normal form merely supplies local principality and easy integration.