<task>
I am adjudicating a preservation lemma inside Aoyagi's resolution-of-singularities recursion
(Aoyagi 2023, "learning coefficient of deep linear networks"). I want you to INDEPENDENTLY derive
two things by exact algebra, from the setup below, without assuming my expectations.

Setup (Aoyagi Cases 1 & 2 recursive blow-up of the matrix-product ideal):
- We resolve the ideal generated by the entries of a matrix product  P = C^(1) C^(2) ... C^(L)  at the
  deepest point (all C^(s) = 0). Widths  M^(1),...,M^(L+1); running minimum  M(S) = min{M^(s): 1<=s<=S}.
- The recursion maintains, indexed by a state (S, J) with 0<=S<=L, 0<=J<=M(S+1), the IDEAL identity
      < P >  =  < diag(b_1,...,b_{M(S)}) . [[E_J, O],[O, D_J]] . prod_{s>S} C^(s) >
  where E_J is the JxJ identity ("cleared pivots"), D_J is the (M(S)-J) x (M^(S+1)-J) residual block,
  and the b_i are monomials in the exceptional (blow-up) coordinates u_{s,k} introduced so far:
      b_0 = 1,  b_i = ( prod_{ tilde-t(u_{s,k}) = i-1 } u_{s,k} ) . b_{i-1}.
  Closed form: b_i = prod_{ tilde-t(u) < i } u.  Hence  b_1 | b_2 | ... | b_{M(S)}  (a divisibility chain).
- The pulled-back product  P o g  (g = composed blow-up substitution so far) equals
  U . [diag(b).[[E_J,O],[O,D_J]].prod_{s>S}C^(s)] . V  for continuous cofactors U, V that are unipotent
  (identity at the origin) with entries that MAY vanish off-diagonal.
- ONE STEP, Case 1 (partial equal block):  b_{J+1}=...=b_{J+J1} != b_{J+J1+1}, with J1 < M(S)-J.
  Blow up { d_ij = 0 (J<i<=J+J1, J<j<=M^(S+1)), u_{s,k}=0 }. The blow-up has two relevant affine charts:
    * Case 1(1): the d-block factors as u_{s,k} . d'  (an EXISTING divisor u_{s,k} absorbs the block);
      set tilde-t(u_{s,k}) := J, and add J1.(M^(S+1)-J) to that divisor's Jacobian exponent. Stay at (S,J).
    * Case 1(2): the first residual row is normalised and  u_{s,k} = u_{S,J+1} . u'_{s,k}  (a NEW divisor
      u_{S,J+1} is introduced); then regular transforms Q (row op) and P (col op) reduce the residual
      D_J'' to [[1,O],[O,D_{J+1}]], advancing J -> J+1.
- The regular Q,P in the Schur reduction split (established separately): the RIGHT column-op P and the
  Schur shear are det-1 invertible source COORDINATE CHANGES (they use kept coords to shear other coords),
  so they belong in g; the LEFT row-op Q is a det-0 projection as a source map (it clears the coordinate
  it is built from), so it CANNOT be a coordinate change and must remain an ideal cofactor.

The two identities I care about ("PrincipalInv"), stated at a state on the ACTUAL pulled-back product P o g:
   (D)  divisibility: every entry of  P o g  is divisible by b_1, with a CONTINUOUS quotient;
   (B)  Bezout: b_1 = sum_ij r_ij . (P o g)_ij  for CONTINUOUS coefficients r_ij  (i.e. b_1 lies in the
        ideal generated by the entries -> the entry-ideal is the PRINCIPAL ideal <b_1>).
</task>

<output_contract>
1. DERIVE the explicit witness-transformation law for ONE Case-1 step, separately for sub-case 1(1) and
   sub-case 1(2): given parent witnesses (the quotients q_ij for (D) and the coefficients r_ij for (B)),
   write the CHILD witnesses q'_ij and r'_ij explicitly, tracking:
     - how the coordinate-change part (blow-up substitution + the det-1 shears) acts (composition?),
     - how the det-0 projection cofactor acts (does it regenerate/re-mix the q,r? which one?),
     - the update  b_1 -> b_1 . u  (when does u appear? which sub-case / which value of J?).
2. State, WITH REASONS, whether EACH of (D) and (B) is preserved at EVERY intermediate state (S,J), or
   whether either one holds only at special states. Be concrete: at an intermediate state with S<L (deep
   layers prod_{s>S}C^(s) still pending, vanishing at the origin), is b_1 actually in the ideal generated
   by the entries of P o g? Give the exact-algebra reason (evaluate at the origin; consider the order of
   vanishing). If one identity fails at intermediate states, say exactly where it becomes true.
3. If the two identities do not both survive per-step, state what the CORRECT step-preserved invariant is
   (the thing a one-step lemma should carry), and how (B)/principality is recovered at the terminal chart
   (S=L+1, where the residual is gone and the matrix is diag(b)).
4. Give a minimal concrete instance (small widths) exhibiting a genuine Case-1 partial-block step, and
   the exact b-vector + witnesses before and after, to ground the law.
</output_contract>

<grounding_rules>
- Exact algebra only. Distinguish clearly what you PROVE from what you conjecture.
- Do NOT assume the two identities are both step-invariant; test it. If you find a counterexample to
  per-step preservation of either, that is a valid and welcome answer.
- "Continuous cofactor that may vanish off-diagonal" is the correct regularity; do NOT require cofactors
  to be nonvanishing (the blow-up y o g = v.u has only the vanishing cofactor v).
- Keep the coordinate-change vs projection distinction sharp; it is the crux of how witnesses transform.
</grounding_rules>
