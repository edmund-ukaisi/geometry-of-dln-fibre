For the stated \((3,3,4)\) instance:

**Q1 — No.**  
Direct substitution gives \(X_{ij}\circ g_p=m_pF_{ij}\), hence \(L\circ g_p=m_p^2\sum F_{ij}^2\); the matched survivor has \(F_a(0)=1\), so \(R_p(0)=1\). Block-blow-up surjectivity independently proves the cover. No loss-isometry structure is required, although matchedness alone would not replace verifying the common factorization.

**Q2 — Yes.**  
Each chart’s cover and sandwich follow from its own map \(g_p\), domain, and matched survivor. Symmetry may enumerate the fan indices, but no certificate for chart \(A\) must be derived from a sibling \(B\).

**Q3 — Same.**  
The cover concerns the survivor region assigned to \(g_p\), while the sandwich holds on that same chart’s domain. No algebraic change of region is forced; mismatched loci instead trigger deeper recursion.

**Q4 — Transport.**  
The reintroducing move would be proving chart \(A\)’s certificate as the \(\sigma\)-image of chart \(B\)’s certificate. Directly constructing the matched blow-up at every pivot makes this entirely avoidable.