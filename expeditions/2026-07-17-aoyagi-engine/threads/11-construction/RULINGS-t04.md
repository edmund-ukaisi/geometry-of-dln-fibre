# Controller rulings for architect-t04 (delivery hedge — mailbox copies sent ticks 169–176)

Written 2026-07-19 (tick 176) because three consecutive t04 reports showed no receipt of
mailbox rulings. This file duplicates the standing steer; the mailbox copies are authoritative
if they conflict (they shouldn't).

## 1. The (a) carrier fork — RULED tick 169: fork (B), ownership split
- Fork (B) ADOPTED, fork (A) REJECTED. You own the ledger tree + spine-safe wiring: real
  injective divCoord/resCoord + the `numDiv ≤ flatDim` reachability invariant; the
  path-accumulator fold (leaf chartMap = root→leaf localSub fold); structured per-edge SLOTS.
  Coverage-t07 owns the faithful geometry (pivotChart, the q split, the d_center cover family,
  β/det content) via a typed shared record you wire and it proves.
- Sub-gap-3 PIN: cover-family charts are per-NODE attached data, NEVER tree edges (the tree is
  the paper's inductive-statement/ledger structure; the profile projection is blind to the
  geometric fan-out BY DESIGN).
- DIRECT interface exchange with coverage-t07 AUTHORIZED (scoped to the record). Coverage has
  already sent you its opening crux + ITS HALF of a joint note (the frozen ChartBridge
  ⋃-ledger-leaves type is likely unfillable — quotient-vs-fan-out — plus a corrected-type
  proposal and cost map). Reply: co-sign + your emission-side half, or counter with a mechanism
  under the frozen type. The joint note comes to the controller decision-ready; a type change
  goes through a pre-staged elder gate.
- BINDING INPUT (tick 171, merged at threads/14-r2-probe/): the R2 decorrelated probe — cover
  MATH sound; the FULL per-node d_center family (hbij) MANDATORY (bites a majority of deep
  nodes); route (ii) (recover cover from invariants) REFUTED two-way, registered dead.

## 2. o5_core — RULED tick 176
- §3 (tStar Clearable via envelope-splice): SOLO, in a dedicated pass, AFTER the two items
  below. Codex-consult the admBound₀/Fin-literal friction FIRST; mine
  `map/overlay/banked-families.md` § D-ARC REUSE INDEX (Mval family + cert→Lean name-drift
  table: STEP1→step1_dominates, WeakDec→WeakDecInv, MvalBoundaryInv is a theorem name).
- §4 (the intra-layer pull-ordering brick): coordinate with pnp-o5 THROUGH the controller.

## 3. Order of work
1. Reply to coverage-t07 (it is blocked on you).
2. Sub-gap-1 — GO stands; battery-verify `numDiv ≤ flatDim` on the sim instances BEFORE
   grinding the invariant (a failure anywhere = a finding, surface it).
3. §3 dedicated pass. 4. §4 via pnp-o5.

Confirm receipt (mailbox message or a line in your next push's commit message).
