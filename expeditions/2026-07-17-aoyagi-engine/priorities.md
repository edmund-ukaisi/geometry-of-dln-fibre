# Priorities — aoyagi-engine

## CHECKPOINT / FRESH START (2026-07-20) — read `charter.md` first
The prior R1–R7 roadmap (chart route: ChartBridge / geoAtlas / α-atlas / `hbox = coverage → the
clean-three headline`) is **SUPERSEDED**. It steered by the headline and drove the second chart-route
drift (the α-atlas `LeafPullback` is category-false). It is kept only in git history; do not re-adopt it.

**The operating frame is now `charter.md`** (the fixed invariant core): we build Aoyagi's machinery as
reusable objects; the learning-coefficient theorem is a corollary/test, not the objective. Progress =
discharging a charter §1 object via a legal §3 category; nothing else is progress.

## Current priority (the NEW PHASE IS NOT YET STARTED)
1. **The uplift** (this checkpoint): charter installed; heartbeat/priorities/roles/brief re-pointed to it;
   attractive category-false Lean holes bannered DO-NOT-FILL; compass split flagged to the elder.
2. **Decide the object list with the operator** (charter §1 is PROVISIONAL): the full-generality statements
   of A (Lemma 1 two-sided — which ideal class), B (the product-ideal resolution — which matrix-product-ideal
   class, incl. the coupled corank≥2 peel), C, D, E. This is a joint math decision in progress, not a build.
3. **Only then** open the new phase (build the objects), under the charter's progress bar.

Do NOT start building objects, dispatch build seats, or re-open the chart route until the object list is
decided with the operator and the elder re-charters the compass around it.

## Standing (unchanged, load-bearing)
- Controller in `.claude/worktrees/aoyagi-engine/root`; NEVER touch `expedition/aoyagi-full` anything.
- Push feature/expedition branches to `origin` freely; PRs / `dev→master` are operator-gated.
- Every commit batch begins with the branch assertion `[ "$(git branch --show-current)" = expedition/aoyagi-engine ]`.
- In-repo memory only; never `~/.claude` global.
