# Priorities — aoyagi-engine (build loop; navigator pass #1 disposition applied 2026-07-17)

1. lane1-derisk — PURSUE (architect seat, t01-r2): concrete (r,r,4)-class CanonicalResolution
   witness (joint 5-conjunct satisfiability — the bundle has NO smallest-instance check yet) +
   RIDER: theorem4 DISSOLVE shape-check (non-deepest cells dispatch via banked domination, not
   chain-IH) + RIDER-light: exponent-ledger survey (map M_{s,k} onto banked minAdm/QIP, no rebuild).
   GATES wave 2.
2. lane2-bridge — PURSUE (covdesign seat, design-first, decorrelated from the skeleton): the
   chart↔integrand CoV bridge (chart maps + loss-pullback identity + Jacobian ledger + unit
   bounds) → the corrected region_glue signature. Its TIDE must not start before this lands.
3. coverage-theorem — DISTINGUISHED SUB-TARGET inside the coming construction tide: separately
   witnessed + own reviewer; never the buried last conjunct (the named hard part holds its lane).
4. monomialization-terminates (construction tide, THE LONG POLE) — WAVE 2: commission when lane 1
   confirms joint satisfiability; lane 1's concrete tree = its base-case/template. Layer-B fence
   held in the brief (deepest stratum + IH-dispatch only; integrates nothing; no re-proving what
   theorem4/IH covers).
5. cov-composer library lane (P8) — WAVE 2: once lane 2 pins the interface.
6. region-glue tide — WAVE 3: gated on lane 2's signature.
7. mint-repoint — PARKED (wiring; waits on hbox).
