# priorities.md — RETIRED (2026-07-20)

This expedition does **not** maintain a priorities ledger. Its function is folded into:
- **`charter.md`** — the goals / objects / progress bar (the *what*, fixed);
- the **heartbeat Memo** — the current focus / top decision in flight (the *state*);
- **`map/claims.yaml`** — node ranking / sequencing / dependencies (the *plan of record*).

Kept as a forwarding stub so re-ground references resolve. Do not re-grow a ranked roadmap here —
that is exactly the file that bloated into the chart-route R1–R7 roadmap and pulled the expedition
back into the drift (see `charter.md` §3). Read the charter.

(Harness note: `docs/policies/expedition.md` still describes `priorities.md` generically as the
operator's taste ledger; that generic mechanism is untouched for other expeditions — this file is
retired for *this* expedition only.)
