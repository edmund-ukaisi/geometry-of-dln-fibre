# Altitude — 2026-07-22 (navigator, the endgame's first full pass)

Phase transition: **LEAF-PROVING (the per-field statement saga) → THE REDIRECT ROUND
(construction-conditioning) → the proof cascade.** This note places the redirect at altitude and
sets the honest distance-to-summit. Written against the tip (8ed6a2b26), two-channel.

## What the redirect IS (altitude read)

The day's ~10 statement defects were all one event: a **free `TreeEdge` field** (center, pivot,
shearφ, nextState, δ) admits a non-construction instance, and a constraint of Aoyagi's recursion
gets re-derived as a bolted-on hypothesis. The paper has no free edges. The redirect closes the
**class**, not the instance: the four one-step leaves get **conditioned on a repaired
`IsRealBranch`** (center/pivot/shear pinned to `canonCenterOf`/`canonPivotOf`/`canonShearOf`), and
the seven per-field constraints become **named derived lemmas** off that one hypothesis. The elder's
frame is exact: the per-field pin set is **the bridge-free shadow of `IsRealBranch`** — when the
coordinate bridge (P12, next-expedition) lands, all pins collapse into one construction pin; the
per-field form was the honest shape until then.

It was **forced by the mathematics, not chosen**: the discriminator came back as a *proof*, not a
read — `DivBirthInv` (kernel-checked at every `buildTree` node) shows every case-11 boost's pivot
sits in a strictly-earlier birth layer, so `pivot ↦ 1` leaves the layer-S support intact, no
descent. The elder was right; the `supportAt`/`edgeδ` def-touch is void. This is the
construction-conditioning collapse the 08:35 process audit *predicted* — architectural convergence,
not drift.

## The rising sea (the frontier this project dodged for multiple expeditions)

The coupled corank ≥ 2 resolution (Object B) is the compass's named hard part. It is no longer
dodged: `StepInvShearChild` (the δ=1 coupled block-center divisibility — the wall's heart) is banked
sorry-free; `BlockDivision`, `exists_graded_decomp` (the shear-graded ∃c re-factoring, case-generic),
and the child-state reductions are banked; `canonCenter` lifted L7 **bridge-free**; L1 +
terminal_bezout are proven. The sea has risen high enough around the wall that the elder's own read
holds: *the wall's proof is short when the statements stop moving* — which is the entire point of
stopping them now. Objects A, C, D are proven; Object E's combinatorial half (θ = a(ℓ−a)+1) is a
registered theorem, faithful (chain-height of the actual binding poset), not a numeric coincidence.

## Honest distance-to-summit (ground-truthed, corrects a stale STATUS.md)

DONE: Objects A/C/D; E-combinatorial; L1 + terminal_bezout (Core); canonCenter integrated; the
banked proof cores. IN FLIGHT: the redirect statement round — one bake (canonCenterOf case-11 fix +
canonShearOf + IsRealBranch shear-pin + 4 one-step leaves conditioned + 6 derived-lemma stubs).
REMAINING PROOF (post-bake), the register:
- 4 one-step obligations — case1/WALL (conj-1 divisibility [banked engine] + conj-2 descend),
  case2 (δ=0 both PROVED; δ=1 + conjB via `exists_graded_decomp` drop-in), lastLayer_clear,
  terminal_edge (δ=0 PROVED; δ=1 dead via hδ0-unreachable).
- **6 derived lemmas off IsRealBranch** — hcenter/hcover/DescendView/ShearWithinCarve/hδ0 +
  **GAP-3 multi-homogeneity** ("the residual IS the block matrix" — the descent engine; the
  DEEPEST remaining structural obligation, elder-ruled provable under ShearWithinCarve via a
  decorrelated Codex pass, C2). This is the one place a "derived lemma" could hide new math —
  hold it a frontier-leaf-candidate until C2 lands strike-able.
- L5 (the fold hinge — folds the 4, discharges the 6, emits FoldProduced ∧ FoldRealizes): the long
  pole. L6 (chart geometry) + L8 (exponents): **path-level, statements already final** (atlas +
  FoldProduced only — untouched by the redirect). L7 (cover, bridge-free).
- The final wiring swap (`LearningCoefficient.exists_coreResolution:287` fossil → via_monument).

The memo's PHASE line is accurate (redirect round); its top quest framing (hbox path-A/B) is
charter-§1-superseded (self-flagged). `map/STATUS.md` is a **day stale** (2026-07-21 — leaves
"parked", E "in build" when both are past that); refresh it at the redirect bake.

## Meta-lessons for the close synthesis

1. **Statement-risk ≫ proof-risk in a free-field skeleton.** The wall's *math* landed in one seat;
   the *statements* took ten rounds. The cost meter is un-audited free fields on quantified
   structures, not sorry count.
2. **Elaborate-then-render, validated by controlled comparison.** The E-lane had the #42 elaborated
   cert up front → three tracks, zero rework. The monument had none → ten reactive rounds. pnp-elab
   retrofitted the template and its mechanical diff found **zero mismatches** — the first positive
   statement-fidelity evidence of the expedition (vs absence-of-caught-defects).
3. **Proposal → lock authorship division.** Two defects were in the elder's own rendered sentences
   (∀ℓ over-reach, ShearWithinCarve (II) scope). The elder rules the object; the proof seat locks
   the form; batteries check both the breaker and the construction side. Now the rule.
4. **A new defect class: the silent catch-all.** `canonCenterOf`'s `| _ =>` mis-handled case-11 —
   a case the 9-instance sim did not force. Def-level exhaustiveness is now a gate item (below).

## Forward (the gate the redirect needs; parallel now)

Gate additions beyond locks+sim+elder-read: (i) **catch-all exhaustiveness** on
canonCenterOf/canonShearOf/DescendView (every StepCase constructor explicit; the sim validates
reached cases, not def completeness); (ii) the **post-redirect free-field re-audit** (the collapse's
own success criterion — no residual free field on the conditioned leaves); (iii) **derived-lemma
sufficiency** (each of the 6 stubs SUFFICIENT for its consumer, not merely true — the class that
caught the hcenter direction gap late). Parallel-now: **L6 + L8 SPECIFY/prove** (path-level, final
statements, provable standalone against their hypotheses — seat-L3T's restored capacity) and the
**Q3 E-residue migration** (quiescent lane, disjoint). Serial-by-nature: the redirect bake and the
interior-leaf/L5/derived-lemma proofs that wait on it. Serial-by-habit (to fix): the proof seats
idling — L6/L8 are provable today.
