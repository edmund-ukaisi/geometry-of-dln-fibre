# Compass — aoyagi-engine

*Seeded at genesis by the controller from the founding adjudication (2026-07-17); the elder council
reviews, amends, and owns it from convening #1 onward. ≤2 pages.*

## The live question
Transcribe Aoyagi's worked-out resolution (DLN 2023, §5) into a Lean engine producing
`∀ M, RouteMBoxThresholdFinite M`, clean-three, no Aoyagi hypothesis. Current load-bearing
sub-question: **the skeleton** — does the resolution-tree datatype (with divisor-sharing data)
elaborate and wire to hbox? **The hard part, named: the coverage theorem (Layer C)** — it holds a
lane from genesis; everything else is transcription over banked substrate.

## Settled forks, WITH WHY
1. **Transform-only over integrate-early** (2026-07-17, operator-directed; evidence: session
   adjudication + obl2form + external refutation). WHY: the target exponent is exactly tight at
   binding cells — zero slack — so any lossy factorization is *false* there, not merely weak. Every
   predecessor failure (5 forks, route-B, σ_min, density-fold, naked-weight) was a lossy split;
   every survivor was exact. Witnesses: `battery/w-naked-weight-111.py`, `w-naked-weight-4444.py`.
2. **Her construction, not a route-specific residual resolution.** WHY: the "product-corank wall"
   was an artifact of the predecessor's decomposition — her paper never forms that object, and her
   DLN construction is complete on the page (direct read + tree-reader cert; conclusion claims the
   resolution map, no future-research caveat; Theorem 1 [3-layer cite] NOT load-bearing).
3. **Divisor-sharing data is mandatory in the tree.** WHY: at corank ≥ 2 the recursion's carried
   monomials couple; threshold-only summaries provably break. Witnesses:
   `battery/g-coupled-binding-334.py` (minAdm(3,3,4)=8, reached only by the coupled path),
   `battery/g-delta-flatten.py` (⟨δx,δy⟩ vs ⟨δ₁x,δ₂y⟩ thresholds differ).
4. **Def 3 as printed is broken** (verified typo; ℓ=1 forces empty selection). Use the geometric
   `½·min_t Mval(t)` form. Witness: `battery/g-def3-broken.py`.
5. **The engine's sole output is hbox** — the one-Prop interface. Consuming stack proven
   (`aoyagi_learning_coefficient_gen`, HeadlineGenAssembly.lean, clean-three, force-printed).

## Non-local invariants (division of labor)
- The engine OWES: `∀ M, RouteMBoxThresholdFinite M`. It DELEGATES: the value's ≤-half (achiever
  divergence — already proven natively upstream), the RLCT-at-a-point geometry (proven), θ (out of
  scope), non-deepest points (to the arity IH / milder-threshold domination inside Layer C).
- Layer B owes *charts + invariant preservation + exponent ledger*; it delegates convergence
  entirely to Layer D's monomial reads. A Layer-B lemma that integrates anything is off-contract.
- Layer C owes *coverage + no-smaller-ratio*; it may consume the whole banked cover/null toolkit;
  it may NOT consume the RLCT value (circularity guard — cannot use "rlct = c*" to rule out a bad
  divisor).

## Open uncertainties, ranked by plan-impact
1. Coverage (Layer C) — the reconstruction; design-first, decorrelated, before its tide.
2. Region-uniformity of Lemma-2/Thm-3 pivot regularity (expected mechanical via co-null covers).
3. Theorem 4's role — transcribe from Entropy-2013, or dissolve into the coverage+IH pattern
   (adjudicate at skeleton time; do not silently assume either).
4. The pp.14–22 index bookkeeping fidelity (paper has known typos — reproduction's ledger first).

## Landmarks (proposed; elder to ratify) — why these
- `mint-repoint` — the destination; keeps the endgame honest.
- `hbox-root` — the single interface; everything meets here.
- `engine-route` — the founding route node; its adoption gate is the expedition's first test.
- `resolution-tree` — the key definition; its shape (sharing data) is the design crux.
- `coverage-theorem` — the key lemma / the hard part; must always hold a lane.
- `exponent-ledger-bridge` — where the analysis meets the banked combinatorics (minAdm forms).
- `rr4-precedent` — the key worked example; the whole engine end-to-end at (r,r,4), 0-sorry.

## Standing counsel
- Fund the general chart-tree CoV composer as a library lane (P8) — its consumers are typed holes.
- Watch for the decoupling disease at the TRANSCRIPTION level: a transcriber "simplifying" the
  carried monomial data is fork-vocabulary; the sharing maps are the content.
- The predecessor's banked bricks are consumed where types fit — never rebuilt, never "adapted"
  by weakening (three redundant-commission events last run; survey before commissioning).
