# The A–E-frame map (re-rooted 2026-07-21 midday; chart-era map archived at claims.yaml @ 70a8ec6cf,
# node-id dispositions in overlay/naming.md § "Chart-era node ids"). Single curated writer: the
# CONTROLLER. Landmarks: elder proposes / controller sets (cap 9).
# Evening refresh (controller, post carto pass #1): Core leaves proven+reviewed, Object E opened
# (P6.1 landed / P6.2 pinned), monument round statuses — see journal 2026-07-21 evening entries.

meta:
  expedition: 2026-07-17-aoyagi-engine
  roots: [aoyagi-summit, object-a, object-b, object-c, object-d, object-e]
  updated: 2026-07-22

nodes:

  # ── the summit (corollary/test — charter §0: steer by the objects, not this) ──────────────

  - id: aoyagi-summit
    kind: claim
    title: "summit: rlctGlobal(lossDLN d 0) = cCodim/2, via the engine (corollary/test)"
    prop: |
      RLCT.Global.rlctGlobal (lossDLN d 0) = ((cCodim d 0 h).toNat : ℝ) / 2, for monotone d,
      0 < N, positive widths, Kostant/QIP nonempty — NO Aoyagi cite invoked (value derived).
    lean: DLNFibre.DLN.Aoyagi.aoyagi_learning_coefficient_via_engine
    status: stated
    tier: new
    owner: controller
    landmark: true
    edges:
      - {type: needs, to: object-b}
      - {type: needs, to: corollary-reduction}
      - {type: needs, to: b-value-cov}
      - {type: needs, to: object-d}
    evidence: [lean/DLNFibre/DLN/Aoyagi/LearningCoefficient.lean, lean/DLNFibre/DLN/RLCT/AxCheck.lean]
    notes: "proof COMPOSED; +sorryAx from EXACTLY exists_coreResolution (AxCheck:1358 informational print); flips clean-three the day object-b lands"

  # ── the charter §1 objects ────────────────────────────────────────────────────────────────

  - id: object-a
    kind: claim
    title: "Object A: RLCT ideal-invariance (Aoyagi Lemma 1), two-sided + weighted — PROVEN"
    prop: |
      Germ-equal generated ideals give equal rlctAt/wrlctAt: both inclusions via
      Cauchy–Schwarz domination + junk-guarded germ-monotonicity (LocallyNullZeros guard).
    lean: DLNFibre.Core.Aoyagi.rlctAt_sumSqFam_eq_of_germ_eq
    status: proven
    tier: new
    owner: "parked: LANDED (seat-A, wave 1); category-NEW workhorse, Mathlib-absent"
    landmark: true
    evidence: [lean/DLNFibre/Core/Aoyagi/IdealInvariance.lean, lean/DLNFibre/Core/Aoyagi/Waypoint.lean]
    notes: "6 leaves + 2 headlines batch-gated; weighted twin wrlctAt_sumSqFam_eq_of_germ_eq; wLocal set-eq feeds B's per-chart value"

  - id: object-b
    kind: claim
    title: "Object B: the product-ideal resolution atlas exists (coupled corank≥2) — THE frontier"
    prop: |
      ∃ res : Resolution (coreGen d e) 0, AtlasRealizesExponents d res — the coupled ideal-route
      atlas: analytic g, two-sided dom-wide ideal identity ⟨∏C⟩=⟨diag(b)⟩, Jacobian certificate,
      hcover BUILD (operator: cite off the table), + the seam's two exponent clauses.
    lean: DLNFibre.DLN.Aoyagi.exists_coreResolution
    status: stated
    tier: new
    owner: mon-geo-lane
    landmark: true
    kill: battery/g-coupled-binding-334.py
    edges:
      - {type: needs, to: b-record}
      - {type: needs, to: b-ladder}
      - {type: needs, to: salvage-adapter}
      - {type: discharges, to: aoyagi-summit}
    evidence:
      - lean/DLNFibre/DLN/Aoyagi/LearningCoefficient.lean
      - expeditions/2026-07-17-aoyagi-engine/threads/31-monument-construction/certificate.md
      - expeditions/2026-07-17-aoyagi-engine/threads/32-monument-formalise/codex/strategy-answer.md
    notes: "the monument skeleton is CLOSING: drivers PROVED from the leaves; leaves L6+L8 PROVED (primed, L6 reviewed 6/6), terminal_edge PROVED in place, case2 = one conjB one-liner, WALL = boostReady-only (3 instruments converging), lastLayer = seat-L4, L5 = the fold convergence node (FoldSpec map, atoms owned), L7 = next commissioning (nav-13: startable now). The final wiring swap replaces LearningCoefficient:287 via the (B′) MonumentAssembly primed drivers after the leaves close; SUMMIT-RETIRE ceremony registered in the owed-register."

  - id: object-c
    kind: claim
    title: "Object C: monomial-ideal RLCT = min (h+1)/(2k) under DivChain (S2 boxed rule) — PROVEN"
    prop: |
      Under the divisibility chain (DivChain) + Measurable unit: chain collapse ∑bₖ² = b_{k₀}²·U,
      wrlctAt = min_d (h_d+1)/(2k_d), and the DLN unit-multiplicity form 2·wrlctAt = min (h_d+1).
    lean: DLNFibre.Core.Aoyagi.monomialSumSq_wrlctAt_eq
    status: proven
    tier: new
    owner: "parked: LANDED (seat-C, wave 1); wave-2 hygiene queued (MonomialBox Core-lift + DLN dedup)"
    landmark: true
    evidence: [lean/DLNFibre/Core/Aoyagi/MonomialRLCT.lean]
    notes: "GUARDED form, not blanket-general (not_divChain_coupled_example = the honest boundary); Measurable-unit delta controller-blessed (junk-0 defect class); charter's coupled/general computation rides B"

  - id: object-d
    kind: claim
    title: "Object D: codim = minAdm = cCodim = qipMin, θ, perm-invariance — BANKED"
    prop: |
      The combinatorial codimension chain: minAdm = cCodim (axiom-clean, + dev's cite-free
      determinantal geometry), qipMin = minAdm (adapter bridge), θ top-component count, perm-inv.
    lean: DLNFibre.Core.Aoyagi.Resolution.divisorMin_eq_cCodim
    status: proven
    tier: new
    owner: "parked: banked (pre-phase + dev merge + adapter bridge)"
    landmark: true
    kill: battery/g-minadm-groundtruth.py
    evidence: [lean/DLNFibre/Core/Aoyagi/Engine.lean, lean/DLNFibre/DLN/Aoyagi/RecursionAdapter.lean]
    notes: "qipMin_eq_minAdm (RecursionAdapter:69); Def-3 typo governed by g-def3-broken (use geometric ½·min Mval)"

  - id: object-e
    kind: claim
    title: "Object E: analytic order ρ — OPENED-SCOPED (operator 2026-07-21); combinatorial half in build"
    prop: |
      1 ≤ boxedOrder (Order.lean interface) + the θ ≠ ρ distinction: numTop(2,2,2,2,2) = 6 ≠
      aoyagiPoleOrder — the pole MULTIPLICITY is not the quiver count. Content build: P6.1 (band
      arithmetic) LANDED; P6.2 (ρ-identification) statement-shaping; analytic zeta seam deferred.
    lean: DLNFibre.DLN.Aoyagi.numTop_d22222_ne_aoyagiPoleOrder
    status: stated
    tier: new
    landmark: true
    owner: seat-E
    edges:
      - {type: needs, to: e-p61-band-arithmetic}
      - {type: needs, to: e-p62-count-identification}
    evidence: [lean/DLNFibre/Core/Aoyagi/Order.lean, lean/DLNFibre/DLN/Aoyagi/ThetaOrderDistinction.lean]
    notes: "the COMBINATORIAL half is CLOSED (P6.1 + P6.2 landed, faithful count c9a4003fe); the only residue is the analytic zeta-pole seam (P7, monument-class, charter §1-E deferral by design)"

  - id: e-p61-band-arithmetic
    kind: claim
    title: "P6.1: banded-interval arithmetic — bandCount ℓ a = a(ℓ−a)+1, unconditional; θ-attachment at the certified selector"
    prop: |
      bandWidth/perJCard/envelopes over free (P,M): perJCard_eq_paper (p.26 fidelity anchor),
      envHi_sub_envLo (width-independence: P,M cancel), bandWidth_sum (area = a(ℓ−a)),
      bandCount_eq; Tier 2: bandCount = aoyagiTheta + thetaCount d r at (paperEll, residueA).
      Count/multiplicity identifications DEFERRED to P6.2 — named in docstrings, never asserted.
    lean: DLNFibre.Core.Aoyagi.OrderCount.bandCount_eq
    status: proven
    tier: new
    owner: seat-E
    edges: [{type: discharges, to: object-e}]
    kill: expeditions/2026-07-17-aoyagi-engine/threads/41-rho-count-object/rho_battery.py
    evidence:
      - expeditions/2026-07-17-aoyagi-engine/threads/41-order-count/statement-card-tier1.md
      - expeditions/2026-07-17-aoyagi-engine/threads/41-order-count/statement-card-tier2.md
    notes: "elder-ratified framing (K4 at this rung = no-masquerade); Tier-2 binding merged, controller gate in flight"

  - id: e-p62-count-identification
    kind: claim
    title: "P6.2: the faithful count — chainHeight(bindingSet M) = thetaCount = a(ℓ−a)+1 — CLOSED"
    prop: |
      The faithful count: chainHeight(bindingSet M) = thetaCount = aoyagiTheta = a(ℓ−a)+1 (max
      CHAIN under componentwise ≤; the ACTUAL poset object, not a numerically-agreeing formula).
      The UPPER+ATTAINMENT incidence content is discharged via the box-partition ≃o
      (sortedBox_orderIso) + the chainHeight transport. Analytic pole seam = P7, deferred.
    lean: DLNFibre.DLN.Aoyagi.bindingSet_chainHeight_eq_thetaCount
    status: proven
    tier: new
    owner: seat-E
    edges: [{type: discharges, to: object-e}, {type: needs, to: e-p61-band-arithmetic}]
    kill: expeditions/2026-07-17-aoyagi-engine/threads/41-rho-count-object/rho_battery.py
    evidence: [expeditions/2026-07-17-aoyagi-engine/threads/41-rho-count-object/certificate.md]
    notes: "CLOSED — wired + AxCheck-rooted c9a4003fe (bindingSet_orderIso_boxPart + bindingSet_chainHeight_eq_thetaCount); the Tier-3 encoding cert = the ≃o; rev-Elane review SURVIVED 7/7. History: pnp-gate discharged (993 cores, blind Codex convergent); 3-track build (sorted-box via banked QIP water-filling / swap-iso via minimiser-endpoints / transport via mclosure); every rfl bridge held at integration; the set identity adm_runMin_filter_eq landed with the merge (admTight def collapsed)."

  # ── Object B's landed halves ──────────────────────────────────────────────────────────────

  - id: b-record
    kind: notion
    title: "the Chart/Resolution record (v4.2) — verified to equilibrium, inhabited"
    lean: DLNFibre.Core.Aoyagi.Resolution
    status: frozen
    tier: new
    owner: "parked: frozen at v4.2 (093475db3); shear pin confirmed NO change needed"
    evidence:
      - lean/DLNFibre/Core/Aoyagi/ProductResolution.lean
      - lean/DLNFibre/Core/Aoyagi/ResolutionInhabited.lean
      - expeditions/2026-07-17-aoyagi-engine/threads/33-shear-pin/certificate.md
    notes: "3 codex rounds + elder + controller ground-truth (v3→v4→v4.1→v4.2); inhabited rank-1 (allOnesResolution) + real blow-up (blowupResolution); he_lin required-on-monument verified"

  - id: b-value-cov
    kind: claim
    title: "monument 1: atlas CoV + engine value chain 2·rlctAt = divisorMin = cCodim — PROVEN"
    prop: |
      rlctAt(∑Fᵢ²) = ⨅ charts (atlas CoV: InjOn-off-null area formula ∘ off-origin C ∘ compact
      subcover); per-chart value = A∘C; ∘ D: two_mul_rlctAt_eq_cCodim. res + min-attainment are
      HYPOTHESES — the value chain never touches exists_coreResolution.
    lean: DLNFibre.Core.Aoyagi.rlctAt_sumSqFam_eq_iInf_charts
    status: proven
    tier: new
    owner: "parked: LANDED (mon-cov + seat-B); rev-cov-fidelity PASS"
    landmark: true
    edges:
      - {type: needs, to: b-record}
      - {type: needs, to: object-a}
      - {type: needs, to: object-c}
    evidence: [lean/DLNFibre/Core/Aoyagi/ProductResolution.lean, lean/DLNFibre/Core/Aoyagi/AreaFormula.lean]
    notes: "weight is the ACTUAL |det Dg| (jacWeightFn) — a false jac declaration cannot game the value; negative guard no_unit_forces_axis_jac_coupled; amplitude/φ folded into unit/hjac (docstring note landed)"

  - id: salvage-adapter
    kind: claim
    title: "the salvage adapter: AtlasRealizesExponents ⟹ hlb ∧ hattain — PROVEN (bookkeeping)"
    prop: |
      An atlas realizing the salvaged Engine tree's terminal exponents yields both QIP conjuncts
      of exists_coreResolution, via minAdm_le_terminalExponents + o5_core_realized +
      qipMin = minAdm = cCodim. Elder-ratified C1 (import boundary, re-audited at integration) /
      C2 (defect witnesses vs decorrelated battery) / C3 (bookkeeping, zero frontier progress).
    lean: DLNFibre.DLN.Aoyagi.hlb_hattain_of_atlasRealizesExponents
    status: proven
    tier: new
    owner: "parked: LANDED (mon-rec); rev-monument-adapter SURVIVED (2 precision escalations applied)"
    edges:
      - {type: discharges, to: object-b}
    evidence:
      - lean/DLNFibre/DLN/Aoyagi/RecursionAdapter.lean
      - theory/aoyagi-2023-reproduction/g-monument-mval-instances.py
    notes: "seam weakened to only-the-minimizer (geometric obligation STRICTLY weaker); C2 decide-anchors (2,2,3,2)→3, (2,2,1,1)→1, (3,3,4)→8, (4,4,4)→12"

  # ── the live frontier ladder (mon-geo recalibration + codex 8-leaf decomposition) ─────────

  - id: b-ladder
    kind: route
    title: "the monument ladder: leaf-2 → rung-(B) d12 → 8-leaf PrincipalInv skeleton"
    status: adopted
    tier: new
    owner: controller
    edges:
      - {type: needs, to: b-leaf2-blowup-atlas}
      - {type: needs, to: b-rung-d12}
      - {type: needs, to: b-principalinv}
      - {type: discharges, to: object-b}
    evidence: [expeditions/2026-07-17-aoyagi-engine/threads/32-monument-formalise/codex/strategy-answer.md]
    notes: "STATEMENT LAYER CLOSED 2026-07-22 ~15:40 UTC (SEVEN elder-gated bakes: redirect 9c51289f4; pin d309bca48 clause-III/(★); hpos 0f8310b1a; COLLAPSE 790aed4e9; node-form 966959855 [REFUTED next round — u³ root counterexample]; STEP-FORM ea846e125 [node-form deleted, both conjBs closed on realBranch_multiAffine_step]; RULING-B → 0ce9c68b9 = CanonicalSchurStep pin as IsRealBranch 4th conjunct + realBranch_canonicalSchurStep projection + boostReady(hslot) + hlayer). Payoff footprints UNCHANGED through all seven (verified per-bake). Derived stubs 6/6 statements final; 5 proved clean-three + canonicalSchurStep projection; multiAffine_step = seat-L3T2's live DESCENT proof. Leaves: L6+L8 PROVED (L6 REVIEWED 6/6); M4 PivotPreservation REVIEWED 7/7; M7 CanonShear REVIEWED 7/7; case1'/case2' structurally complete — sorryAx flows only through boostReady (seat-L4 live) + the descent. §D.3 telescoping FULLY PROVISIONED. (B′) Assembly increment 1 live (896b60b9b); increment 2 GO'd. Remaining: boostReady, descent, lastLayer, L5 assembly (longest link), L7, summit swap, SUMMIT-RETIRE. Census 35 = ~10 live + 25 fossil."

  - id: b-leaf2-blowup-atlas
    kind: claim
    title: "leaf 2: the universal general-D origin blow-up Resolution — PROVEN (the shared atom)"
    prop: |
      blowupResolution (hD : 2 ≤ D) : Resolution (coordFam D) 0 — max-pivot atlas, universal
      Jacobian det (w i)^(D−1), EXACT argmax sector cover (spectator-free origin case).
    lean: DLNFibre.Core.Aoyagi.blowupResolution
    status: proven
    tier: new
    owner: "parked: LANDED (mon-geo tide, closed exemplary); universal-in-D ⟹ no cast transport"
    evidence: [lean/DLNFibre/Core/Aoyagi/OriginBlowup.lean, lean/DLNFibre/Core/Aoyagi/BlowupResolution.lean]
    notes: "dissolves the opaque-width cast wall BY DESIGN; D=2 cousins …2-renamed (see overlay/naming.md); shared atom for rung-(B) AND the monument's per-step blow-up geometry"

  - id: b-rung-d12
    kind: claim
    title: "rung (B): d=(1,2) end-to-end discharge of the geometric obligation's body"
    prop: |
      The obligation's body at d = ![1,2]: instantiate blowupResolution at D = flatDim ![1,2],
      compose the coreGen linearity iso L, seam via o5_core_realized + minAdm ![1,2] = 2 (decide).
    status: proven
    tier: new
    owner: seat-d12
    lean: DLNFibre.DLN.Aoyagi.exists_atlasRealizesExponents_d12
    edges:
      - {type: needs, to: b-leaf2-blowup-atlas}
    evidence: [lean/DLNFibre/DLN/Aoyagi/GeometricAtlasD12.lean]
    notes: "LANDED (post-carto-snapshot): both theorems clean-three (exists_atlasRealizesExponents_d12 + two_mul_rlctAt_coreGen_d12 = the first proven LC instance, cite-free); wired + batch-banked (341); gate-orphan RESOLVED; rev-d12 fidelity in flight"

  - id: b-principalinv
    kind: notion
    title: "PrincipalInv + StepInv: the path invariants (divisibility along the recursion; principality BORN terminally)"
    status: frozen
    tier: new
    landmark: true
    owner: seat-core
    lean: DLNFibre.Core.Aoyagi.PrincipalInv
    edges:
      - {type: needs, to: b-record}
    evidence:
      - expeditions/2026-07-17-aoyagi-engine/threads/33-shear-pin/certificate.md
      - expeditions/2026-07-17-aoyagi-engine/threads/34-case1-invariant/certificate.md
      - expeditions/2026-07-17-aoyagi-engine/threads/42-core-leaves/statement-cards.md
    kill: battery/g-delta-flatten.py
    notes: "thread-34 split: StepInv (divisibility-only, interior) / PrincipalInv (terminal). terminal_bezout PROVEN+reviewed (principality born at the cleared pivot, V′-shrink). M17 predicates (IgnoresCoords/Deg1SupportedOn) live here too."

  - id: b-leaf1-regionrepresents
    kind: claim
    title: "leaf 1: PrincipalInv ⟹ both RegionRepresents directions — PROVEN + reviewed"
    lean: DLNFibre.Core.Aoyagi.principalInv_regionRepresents
    status: proven
    tier: new
    owner: seat-core
    edges: [{type: conjectured-toward, to: b-ladder}]
    evidence: [expeditions/2026-07-17-aoyagi-engine/threads/42-core-leaves/statement-cards.md]
    notes: "clean-three; rev-core SURVIVED (incl. non-circularity + consumer-fit at the driver :809–813); with terminal_bezout, 2 of the monument's 8 leaves are down (cone 6)"

  - id: b-leaf34-case-steps
    kind: claim
    title: "leaves 3–4: Case-2 / coupled Case-1 steps preserve FoldStepInvAt (interior) — THE WALL"
    lean: DLNFibre.DLN.Aoyagi.case1_preserves_stepInv
    status: stated
    tier: new
    owner: seat-L4
    edges: [{type: conjectured-toward, to: b-ladder}]
    kill: battery/g-coupled-binding-334.py
    notes: "THE WALL IS ONE HOLE (2026-07-22): case1_preserves_stepInv' PRIMED in Case1Wire — dispatch body clean-three; conjunct-1 δ=0 ✓ (stepInv_child_delta0), δ=1 case12 ✓ (append + Deg1 bridge + cover equality), conjunct-2 ✓ (node-form one-liner); sorryAx flows ONLY through realBranch_boostReady_case11 (see b-boostready). case2_preserves_stepInv' likewise conjA-proven + conjB one-liner in flight (Case2Wire). The MonumentAtlas stubs stay sorried until the (B′) Assembly swap."

  - id: b-leaf-lastlayer
    kind: claim
    title: "leaf L-last: the last-layer clear preserves the fold invariant (supportAt → ∅ born-unit corner) — fix-2 object, NEW at the bake"
    lean: DLNFibre.DLN.Aoyagi.lastLayer_clear_preserves
    status: stated
    tier: new
    owner: seat-L4
    edges: [{type: conjectured-toward, to: b-ladder}]
    notes: "born at the bake (b7fa66841, :890); the S=L born-unit corner where supportAt = ∅; splits the terminal transport cleanly from terminal_edge_stepInv."

  - id: b-leaf5-path-fold
    kind: claim
    title: "leaf 5: fold the one-step invariants along a root-to-leaf path"
    lean: DLNFibre.DLN.Aoyagi.leaf_stepInv_of_path
    status: stated
    tier: new
    owner: "parked: skeleton rung (C); general-d reasoning over the salvaged tree's WellFounded.fix"
    edges: [{type: conjectured-toward, to: b-ladder}]
    notes: "the fold convergence node (nav-13: THE LONGEST LINK). L5FoldSpec obligation map LANDED (seat-L3T2, stop-on-suspect NONE): the spine (FoldStepInvAt → LastLayerInv → terminal → PrincipalInv), IsRealBranch emission rfl-class BY CONSTRUCTION, the (★)-telescoping decomposition of the collapse clause; atoms M1/M2/M3/M5 (seat-L3T2, easy) + M4 (seat-L6, PivotPreservation, fork dissolved) + M7 (seat-L4, canonShearOf) + M6 (held). Consumes the walls; priceable now that every atom has an owner."

  - id: b-leaf6-chart-geometry
    kind: claim
    title: "leaf 6: path map analytic, origin-fixing, a.e.-injective, ledger Jacobian, squarefree b₁"
    lean: DLNFibre.DLN.Aoyagi.leafPath_chartGeometry
    status: stated
    tier: new
    owner: "parked: skeleton rung (C); statements gated by the shear pin (resolved corrected-(a))"
    edges: [{type: conjectured-toward, to: b-ladder}]
    notes: "PROVED as the primed twin leafPath_chartGeometry' (LeafChartWire, clean-three, rev-L6 SURVIVED 6/6 + Codex concurring; consumes the FoldRealizes Jacobian-collapse clause via hcollapse = hreal.2.2; chart_of_collapse builds all 28 fields). The MonumentAtlas anchor stays sorried until the (B′) Assembly swap + SUMMIT-RETIRE."

  - id: b-leaf7-compact-cover
    kind: claim
    title: "leaf 7: hcover — a.e. compact atlas cover of a ball, BUILD (operator: no cite) — BRIDGE-FREE"
    prop: "∃ ρ > 0, volume (ball 0 ρ \\ ⋃ p, g p '' dom p) = 0 — max-pivot-sector partition"
    lean: DLNFibre.DLN.Aoyagi.leafPath_compactCover
    status: adjudicated
    tier: new
    owner: "BLOCKED on elder fan re-scope (seat-L7 holds; its bridge lane ready)"
    edges: [{type: conjectured-toward, to: b-ladder}, {type: needs, to: b-terminal-edge-stepinv}]
    notes: "⚠ FALSE AS STATED (seat-L7 + Codex, 2026-07-22; controller verified on def MonumentAtlas:840-841). The current lean statement pins pivot to the single canonPivotOf; gmap's root blow-up is OUTERMOST, so every real branch shares one root blow-up and ε·e_q (q≠p in the non-singleton root center) ESCAPES every chart. The canonCenter pin does NOT save it — the cover NEEDS pivots FANNED (all |S| pivot charts per center, the argmax cover the pnp-cover cert actually verified). The COVER CLAIM is TRUE over the FANNED atlas (that's why status=adjudicated not refuted). FIX = fan the pivot in IsRealBranch/canonPivotOf/FoldRealizes + L5 emits the fanned atlas + bridge to the engine's LANDED sorry-free fanned cover (Engine.GeoCoverSpec.geoAtlas; seat-L7 de-risked). Part of the COUPLED spine redesign (elder-owned; with the boostReady shear value-pin). The old 'BRIDGE-FREE stands' banner was the premature-closed confound."

  - id: b-terminal-edge-stepinv
    kind: claim
    title: "terminal_edge_stepInv: the interior→terminal transport — PROVEN (in place, clean-three)"
    prop: |
      Terminal-reaching edge + parent FoldStepInvAt ⟹ (1) StepInv with the LITERAL fun _ : Fin 1 ↦ 1
      residual ∧ (2) ∃ i₀ unit, ContinuousOn ∧ unit 0 ≠ 0 ∧ (coreGen i₀ ∘ g) = b·unit — matching
      TerminalBezout's input verbatim; the chain L3/L4 (interior) → this (last edge) → terminal_bezout.
    lean: DLNFibre.DLN.Aoyagi.terminal_edge_stepInv
    status: proven
    tier: new
    owner: "parked: PROVEN (seat-L3T2 lane 1, 2026-07-22 — δ=1 dead branch via realBranch_terminal_edgeδ [hpos alone]; δ=0 re-homed; clean-three, in place in MonumentAtlas)"
    edges: [{type: conjectured-toward, to: b-ladder}]
    notes: "the elder: 'easy earns a name, not an omission' — expected easy (unit-residual strictly weaker; the quotient absorbs into q′); the consumer-fit example elaborates (verified two decorrelated ways). RE-PARENTED at the bake (b7fa66841, :916): now takes LastLayerInv + hcleared (was the canonCenter-round shape); conclusion still verbatim TerminalBezout-input. The chain: L3/L4 interior → lastLayer_clear_preserves → this → terminal_bezout."

  - id: b-leaf8-realizes-exponents
    kind: claim
    title: "leaf 8: binding axes ↔ terminal t̃=0 divisors; jac+1 = divExp (the seam clauses)"
    lean: DLNFibre.DLN.Aoyagi.leafPath_realizesExponents
    status: stated
    tier: new
    owner: "parked: skeleton rung (C); consumes the salvage adapter's seam shape"
    edges: [{type: conjectured-toward, to: b-ladder}]
    notes: "PROVED as the primed twin leafPath_realizesExponents' (LeafGeometryWire, clean-three, first complete leaf; ported to the baked tip by seat-L3T2). The MonumentAtlas anchor stays sorried until the (B′) Assembly swap + SUMMIT-RETIRE. bindingAxes = {t̃=0 divisors} implements the paper's read-off restriction."

  - id: b-boostready
    kind: claim
    title: "boost-readiness: parent foldResid is Deg1SupportedOn the case11 boost center — BAKED, proof live"
    prop: |
      At a real interior case1(1)-δ=1 edge, Deg1SupportedOn (foldResid p) ed.center (foldRegion p) —
      the SOURCE form. RULED + BAKED (0ce9c68b9): home (2) = CanonicalSchurStep property pin on
      IsRealBranch (4th .step conjunct) + boostReady derived stub gains hslot; content =
      support-form. PROOF obligation: realBranch_boostReady_case11 (Case1Wire). PROOF-SIDE RESHAPE
      (elder+seat-L4 decorrelated-converged 2026-07-22): NOT a one-liner — foldResid p is built
      from p's ANCESTOR shears not ed.shearφ. ⚠ STATEMENT-SIDE RE-OPEN (Codex DIVERGED from the
      elder's proof-side verdict + was RIGHT; controller verified on def MonumentAtlas:819-824):
      CanonicalSchurStep is SUPPORT-ONLY (∀ u k, shearφ u k ≠ 0 → bounds), VACUOUS for identity
      shear — so IsRealBranch does NOT pin shearφ=canonShearOf (value unpinned; center/pivot ARE
      value-pinned at :840-841). The predicate ADMITS R_bad, for which boostReady is FALSE, so it is
      NOT provable from IsRealBranch as baked. FIX = add shearφ=canonShearOf value-pin conjunct to
      IsRealBranch step-arm (COUPLED spine redesign, elder-owned, with the L7 pivot fan). Two
      sub-lemmas once re-baked: (a) combinatorial b-chain divisibility on foldB p (no shear input,
      SAFE under any spine design — seat-L4B proving now); (b) prepared-form path-induction consuming
      the now-pinned canonShearOf (the γ-clearing; blocked on the shear-pin re-bake). hslot IS consumed.
    status: stated
    tier: new
    owner: "seat-L4B (proof; sub-lemma (a) started) ← seat-L4 (turnkey handoff + Codex verdict, then stands down)"
    kill: expeditions/2026-07-17-aoyagi-engine/threads/L4-case1-core/case11_boostready_crosscheck.py
    edges:
      - {type: needs, to: b-principalinv}
      - {type: discharges, to: b-leaf34-case-steps}
    evidence:
      - expeditions/2026-07-17-aoyagi-engine/threads/L4-case1-core/codex/boost-readiness-2222-answer.md
    notes: "instrument 1 (seat-L4 Codex, decorrelated): TRUE on (2,2,2,2) (A1/A2/A3 pass) but NEEDS-CARRYING under the current coarse pins — countermodel R_bad (unprepared shear, γ not Schur-cleared) satisfies full-support Deg1 + scalar foldB + clauses I-III yet is not boost-ready. Resolutions: (1) carry BChainCompatible; (2) CanonicalSchurStep property clause on IsRealBranch (gate-2 partially reinstated with the honest forcing reason; M7's emission cost sunk). Elder HOLDS for instrument 2 (pnp-boost, blind to instrument 1)."

  # ── the reduction + the kill-target ───────────────────────────────────────────────────────

  - id: corollary-reduction
    kind: claim
    title: "the corollary reduction: rlctGlobal(lossDLN) = rlctAt(∑coreGenᵢ²) 0 — PROVEN"
    prop: |
      exists_flatten (LINEAR m.p. origin-fixing homeo flatten) + the Frobenius identity
      lossDLN_zero_eq_coreLoss + coreReduction (deepest-point ∘ homogeneity ∘ homeo-invariance).
    lean: DLNFibre.DLN.Aoyagi.coreReduction
    status: proven
    tier: new
    owner: "parked: LANDED (seat-bridge, wave 1)"
    landmark: true
    edges: [{type: discharges, to: aoyagi-summit}]
    evidence: [lean/DLNFibre/DLN/Aoyagi/LearningCoefficient.lean, lean/DLNFibre/DLN/RLCT/Foundations/GlobalHomog.lean]
    notes: "NEW REUSABLE GlobalHomog (rlctGlobal_comp_homeomorph + rlctGlobal_eq_rlctAt_zero_of_homogeneous, network-generic); ℝ≥0∞ carrier hop DISSOLVED into membership bridges"

  - id: kill-cite
    kind: claim
    title: "DELETE cited_aoyagi_lower_ax — the charter kill-target (close-phase)"
    status: conjectured
    tier: new
    owner: "parked: until object-b lands (summit clean-three); then consumer audit + deletion"
    edges: [{type: needs, to: aoyagi-summit}]
    evidence: [lean/DLNFibre/DLN/RLCT/AoyagiCited.lean]
    notes: "summit already avoids the cite (kill-path verified); deletion = consumer re-home; cited_watanabe_upper_ax = separate proof-target CANDIDATE (charter §3), not settled-external"

  # ── dead-route registry (exits with forwarding — trails, not destinations) ────────────────

  - id: chart-engine-retired
    kind: route
    title: "RETIRED: the α-atlas chart Engine (category-FALSE value holes; second drift)"
    status: retired
    tier: new
    owner: "parked: parts-bin (un-wired, un-built); salvage via specific-module re-import only"
    edges: [{type: forwarded-to, to: object-b}]
    evidence:
      - lean/DLNFibre/DLN/RLCT/Engine/RETIRED.md
      - expeditions/2026-07-17-aoyagi-engine/charter.md
    notes: "no det-1/a.e.-inj chart diagonalises the loss on an open set (compass F1); combinatorial kernel SALVAGED via the adapter (C1 boundary); do NOT fill its sorries — overlay/dead-routes.md"

  - id: naked-weight-route
    kind: route
    title: "REFUTED: naked Jacobian-weight residual obligations (integrate-early)"
    status: refuted
    tier: new
    owner: "parked: refuted at genesis; battery anchor"
    kill: battery/w-naked-weight-111.py
    edges: [{type: forwarded-to, to: object-b}]
    evidence: [expeditions/2026-07-17-aoyagi-engine/threads/00-genesis/architecture-cert.md]
    notes: "false on legal cuts, not merely hard; the zero-slack lesson's anchor (also w-naked-weight-4444)"

  - id: decorated-descent-route
    kind: route
    title: "SUPERSEDED: the predecessor's conditional decorated route to hbox"
    status: superseded
    tier: new
    owner: "parked: tombstoned at genesis adoption (fork 6); trail only"
    edges: [{type: forwarded-to, to: chart-engine-retired}]
    evidence: [lean/DLNFibre/DLN/RLCT/Validate/RouteMSJDecoratedRec.lean]
    notes: "witnessing DecoratedDescent re-imports the refuted decorated-peel object; tombstoned in the Lean docstring"
