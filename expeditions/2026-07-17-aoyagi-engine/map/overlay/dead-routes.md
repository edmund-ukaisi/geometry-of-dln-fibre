# Overlay — dead-route registry + DO-NOT-ENTER register (cartographer, curated layer) — A–E frame

*RE-ROOTED 2026-07-21 (re-root #1, tip 70a8ec6cf). The chart-Engine-era kill catalogue (retired
shapes, D-arc / carrier-arc / geo-atlas kills) is ARCHIVED VERBATIM at
[[archive/dead-routes-chart-era]] — consult it ONLY when salvaging out of the retired parts-bin.
A route here is DEAD as a destination; its trail is kept as a battery anchor / lesson.*

---

## THE dead route of record — the α-atlas chart Engine (RETIRED 2026-07-20; the second drift)

- **What:** `lean/DLNFibre/DLN/RLCT/Engine/` — the whole chart-route programme (ChartBridge /
  region_glue / geoAtlas / LeafPullback / hbox / mint endgame). UN-WIRED from the aggregator
  (0 Engine modules reachable), NOT built. Kept as a **parts-bin**, not deleted.
- **Why dead (compass F1, the category error, killed us twice):** every `(∏C)ᵢⱼ` is a nonzero
  poly, ≢0 on a dense open; exact diagonalisation forces ≡0 (a non-open det-0 projection) — NO
  det-1 / a.e.-injective chart diagonalises the loss on an open set. The gap is invisible to
  Jacobian/cover gates and fails only at the value consumer, late.
- **DO NOT FILL its sorries** (`leafDiagFrob_geoAtlasNorm` = `GeoAlphaGauge.lean:675`,
  `ClearableReify.lean:79`, + the `:= sorry`-form holes RETIRED.md names): category-FALSE or
  off-cone. Closing any hole there is NOT progress (charter §2).
- **Salvage carve-out (the ONLY legal use):** re-import a SPECIFIC audited module into a new
  Object-B module — precedent: the adapter's four salvage roots (C1 boundary: green closure only,
  never ChartBridge*/CanonicalResolution/Geo*; controller re-derives footprints at integration).
  The `IsFullMonomialization` PREDICATE is ledger-combinatorial and salvaged; only its bundle-role
  is retired.
- Pointers: `Engine/RETIRED.md`, charter §3, compass F1 + § History, [[archive/banked-families-chart-era]].

## DO-NOT-ENTER register (fossil zones; census 2026-07-21, grep-verified at 70a8ec6cf)

**24 proof-position sorries tree-wide; 23 are FOSSILS, 1 is THE frontier**
(`LearningCoefficient.lean:287`). Do not "fix" a fossil.

> ⚠ The "1 frontier" line predates the supportAt bake (`b7fa66841`, 2026-07-22): the monument is now a
> 10-leaf RENDERED skeleton — ~8 ON-CONE sorried leaves in `MonumentAtlas.lean` (the proof queue, NOT
> fossils; [[banked-families]] § In-flight). The `Validate/`/`Skeleton` fossil zones below still stand.
> A precise tree-wide recount is a navigator gate-verify item, not done here.

- **Predecessor `Validate/` fossils (18):** `RouteMInteriorLDUContract` ×9 (the densest
  half-built lane), `RouteMSJDeeperFlagCore` ×2, `DeepestGaugeChart:357`, `RouteMLayerCoverGE:133`,
  `RouteMRecursion:257`, `RouteMSJDecoratedPeelStep:86`, `RouteMSJResolution:803`,
  `RouteMSchur:429`, `RouteMSchurGeneral:144`. The integrate-early / naked-weight disease lives
  here (kills `w-naked-weight-111/4444`).
- **Legacy Skeleton stubs (3):** `Skeleton.lean:1094,1140,1197` — superseded by the banked value
  lane; the bare `aoyagi_learning_coefficient` (`Skeleton.lean:1685`) is the OLD chart-era mint
  anchor, NOT the summit (see [[naming]]).
- **Retired Engine (2 + `:= sorry` forms):** see the entry above. (RETIRED.md's "4 sorries" count
  vs grep's 2 proof-position tokens — flagged, see re-root report; the census difference is
  form-counting, not new holes.)

## Live math kills for the A–E frame (destinations that are dead; trails = witnesses)

- **Chart-diagonalisation of the loss (any det-1/a.e.-inj chart) — REFUTED (F1).** Use the
  ideal route: A (ideal-invariance) + B (resolution) + C (monomial rule). Witnesses:
  `threads/25-ideal-route/` certs, compass F1.
- **Row-wise flatten / per-row-multiplicity split of the coupled ideal — REFUTED (F4).**
  The `b_i` share divisors at corank≥2; a flatten returns the WRONG min. Witnesses:
  `battery/g-coupled-binding-334.py` (minAdm(3,3,4)=8 coupled-only),
  `battery/g-delta-flatten.py` (identical multiplicities, different lct). Object B must
  reproduce the coupled `diag(b)`, not decompose it.
- **"Pure-monomial g" per-chart reading — CORRECTED by the shear pin (thread 33, corrected-(a)).**
  g = det-1 unipotent shears ∘ blow-ups (Jacobian exactly 1 per shear; unit ≡ 1 STANDS, right
  conclusion, corrected reason — ADDENDUM, not silent rewrite); the det-0 projection-type row-ops
  are FORCED to remain RegionRepresents cofactors. No corank dichotomy (option (c) false).
  Witness: `threads/33-shear-pin/certificate.md` + 3 independent exact routes.
- **"⊇ Adm / == Adm" as a realization target — REFUTED (stranding; shallow-instance confound).**
  The t̃=0 profile-set is a PROPER subset of Adm at interior-bottleneck widths (84/351). The
  minimizer-only fact (`minAdm ∈ terminalExponents`) is what the adapter salvages. Kill-set
  adequacy rule born here (compass counsel).
- **`MinAdmMono` for threshold preservation — WRONG DIRECTION (name-similarity formaliser trap).**
  Use the minAdm-as-minimum property (`minAdm_le_Mval_toNat`, `Validate/RouteMState.lean:259`).
- **Single-tide pricing of the monument — REFUTED (mon-geo + decorrelated codex, ~5%).** The
  thread-31 certificate's MATH stands; its Lean-cost pricing was optimistic. The ladder is
  leaf-2 → rung (B) d12 → the 8-leaf skeleton; leaves 3–5 content HELD until the skeleton.
- **Hironaka-properness CITE for hcover — REMOVED BY OPERATOR (2026-07-21).** hcover is a BUILD
  for the explicit DLN atlas; if the assembly walls, decompose differently or surface — never
  an axiom.
- **Whole-tree rebuild of the salvageable combinatorics — REJECTED (elder).** "Rebuilding 4000
  kernel-checked lines to avoid a retired NAMESPACE is timidity-disguised-as-rigor." Salvage
  under C1/C2/C3.

## Bake-era discards (the supportAt window ruling, `b7fa66841`, 2026-07-22)

- **two-clock (T/D⁺ deeper-union-shed) shape + shedSlots — DISCARDED.** The elder-ruled `supportAt`
  window replaces it: the `T ∪ D⁺` partition is disjoint via `disjoint_compl_right` (no shed slots, no
  ±1 offset). Trail: the swap-alarm fired on the two-clock's near-inversion → the phantom-retraction
  cycle (the over-deference / false-retraction witness, [[severance-witnesses]] § Process defects).
- **`deeperCoords` island (`deeperCoords` + `deeperCoords_disjoint_atOrBelow`) — DELETED** (false-docstring
  fidelity defect: its "consumers CITE it, never re-derive" docstring was false-as-stated — nothing cited
  it; `supportAt` uses `layerCoords` and the C-complement partition uses `disjoint_compl_right`).
  grep-clean at HEAD (0 matches). Caught at the bake gate.

## Redirect-round supersessions (elder ruling + discriminator PROOF, 2026-07-22)

- **per-field SET (A) forms — SUPERSEDED** (not baked). The seven per-field severance constraints on the
  monument leaves are replaced by ONE construction hypothesis: the leaves CONDITION ON the repaired
  `IsRealBranch`; the per-field constraints + the prepared-multi-affine residual become NAMED DERIVED
  LEMMAS (`hcenter` / `hcover` / `DescendView` / `ShearWithinCarve` / `hδ0` + GAP-3). Elder-ruled,
  discriminator-confirmed (`DivBirthInv` proof-grade); **STAGED PRE-BAKE** — the leaves still carry the
  `supportAt`-window per-field forms until the redirect bakes. The construction-conditioned monument is
  the statement architecture's FINAL form. Trail: journal 2026-07-22 (triple ruling + discriminator verdict).
- **case-aware `supportAt` / `edgeδ` fallback — VOIDED by the discriminator.** A descent at case11 would
  have forced a per-edge-kind support window; the `DivBirthInv` proof showed NO descent at case11 boosts,
  and `supportAt`'s single (layer,cleared) key already tracks both edge-kinds (merge: earlier-layer pivot,
  no descent, cleared unchanged; append: current-layer, descent, cleared+1). No case-aware fallback is
  needed — the uniform `supportAt` stands ([[severance-witnesses]] § Process defects).
- **the bridge-free SHADOW of `IsRealBranch` — now HISTORY.** The five-pin bridge-free-shadow framing
  (when the coordinate bridge lands, all pins collapse) is superseded by the derived-lemma chain; kept as
  provenance only.

## Genesis-era exits (kept: battery anchors)

- **naked-weight route** — REFUTED at genesis (false on legal cuts). Kills fire:
  `w-naked-weight-111.py`, `w-naked-weight-4444.py`. The zero-slack lesson's anchor.
- **decorated-descent route** — SUPERSEDED (fork-6 tombstone;
  `Validate/RouteMSJDecoratedRec.lean:206/:211`). Witnessing it re-imports the refuted
  decorated-peel object.

## Battery disposition (append-mostly; nothing deleted)

Chart-era witnesses in `map/battery/` whose guarded nodes are retired (`g-chartscover-vacuity`,
`g-chart-bridge-pullback`, `g-fake-chartmap-reject`, `g-glue-*`, `g-leaf-chain-separation`,
`g-pivot-conull`, `g-engine-ratio-consistency`, `g-case11-merge`, `g-resrank-minadm`,
`g-coverage-sharing-killcond`) stay ON DISK as the dead routes' battery anchors. STILL-LIVE
guards for A–E: `g-coupled-binding-334`, `g-delta-flatten`, `g-minadm-groundtruth`,
`g-def3-broken`, `g-case2-exponent`, `g-tightness-formA`, `w-naked-weight-*`, `_minadm.py`,
`_rlct.py` (+ `theory/aoyagi-2023-reproduction/g-monument-mval-instances.py`, the C2 anchor).
