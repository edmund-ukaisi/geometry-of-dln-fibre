# STATUS — 2026-07-17-aoyagi-engine

updated: 2026-07-21 (cartographer re-root #1, tip 70a8ec6cf — the A–E frame; chart-Engine era
archived: overlay/archive/, claims @ 70a8ec6cf^. Applies once controller lands claims.yaml.proposed.)

## the one-line position

THE LIBRARY CARRIES ONE SORRY: `exists_coreResolution`'s geometric obligation
(`LearningCoefficient.lean:287` — ∃ res, AtlasRealizesExponents d res: the coupled corank≥2
ideal-route atlas + hcover BUILD). Everything else in the summit's cone is PROVEN: root green
8977 jobs, 334 roots batch-asserted clean-three, cordon clean, DLN cites NOT invoked (kill-path).
`aoyagi_learning_coefficient_via_engine` flips clean-three the day that sorry lands.

## landmarks (9 proposed)

  ★ aoyagi-summit [stated; +sorryAx from exactly the obligation] rlctGlobal = cCodim/2 via engine
  ★ object-a [PROVEN] Lemma 1 ideal-invariance two-sided + weighted — the category-NEW workhorse
  ★ object-b [stated — THE frontier] the coupled resolution atlas; the ONE sorry
  ★ object-c [PROVEN] guarded S2 boxed rule (DivChain; Measurable-unit delta blessed)
  ★ object-d [PROVEN] minAdm = cCodim = qipMin, θ, perm-inv (banked + dev merge + adapter)
  ★ b-value-cov [PROVEN — monument 1] atlas CoV + value chain 2·rlctAt = divisorMin = cCodim
  ★ b-leaf2-blowup-atlas [PROVEN] universal general-D origin blow-up (the shared atom)
  ★ corollary-reduction [PROVEN] rlctGlobal(lossDLN) = rlctAt(∑coreGen²) 0 (+ GlobalHomog reusable)
  ★ kill-cite [queued at close] delete cited_aoyagi_lower_ax once the summit is clean-three

## the frontier ladder (b-ladder; owner of the sorry)

  ✔ leaf-2 universal blow-up LANDED; shear pin RESOLVED corrected-(a) (unit ≡ 1 stands, record unchanged)
  ▶ rung (B) — d=(1,2) end-to-end obligation body [seat-d12, IN FLIGHT; GeometricAtlasD12 gate-orphan until wired]
  ⧗ rung (C) — the 8-leaf PrincipalInv skeleton; leaves 3–5 = THE WALL (coupled Case-1 dependent-width
      factorisation + general-d WellFounded.fix fold); hcover = BUILD, cite off the table (operator)

## live seats / walls / queued

  - seats: seat-d12 (rung B). Closed this phase: seat-A/B/C/bridge, mon-cov, mon-rec, mon-geo,
    pnp-monument, pnp-shear; reviewers rev-cov-fidelity PASS, rev-monument-adapter SURVIVED.
  - walls (named, un-dodgeable): the coupled corank≥2 ideal identity; general-d tree induction; hcover.
  - queued: wave-2 hygiene (MonomialBox Core-lift + DLN dedup); E content build (deferred, in reach);
    stale-docstring fix RecursionAdapter.lean:77 (`AtlasRealizes`); fossil census = 24 sorries tree-wide,
    23 off-cone (predecessor Validate 18, Skeleton stubs 3, retired Engine 2 — do-not-enter register).
