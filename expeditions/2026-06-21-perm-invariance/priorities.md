# priorities.md — `perm-invariance` taste ledger

Ranked by value-of-information × directed suspicion. Controller proposes; operator edits.

## P0 — pursue NOW (gates the ladder)
- **The sizing pass.** Decorrelated (pen-and-paper + Codex): is `cCodim`/`numTop` permutation-invariance
  provable by a ZERO-CITED combinatorial route, and which? Settle the precise statement (`(C,θ)(d) =
  (C,θ)(d∘σ)`, the corner-`r`/`σ` bookkeeping), the route (codimForm/Kostant bijection under permuting `d`
  vs reduce-to-sort), and the honest SIZE. Output: re-scoped ladder + the verdict on whether Thm 5.5
  (equivariant cohomology) is avoidable.

## P1 — pursue after sizing
- **P-cCodim** then **P-numTop** (the combinatorial invariance), then **P-geom** (the geometric corollary
  via the LANDED bridges). Suspicion: the heart is `cCodim d = cCodim (sort d)` (the QIP/explicit forms are
  monotone-only, so the invariance is the genuine new content). Roadmap warns "genuine lift, NOT free."

## unclear-but-keep-going
- Whether the combinatorial route fully avoids the equivariant-cohomology Thm 5.5, or hits a wall there
  (⟹ Cited layer / sub-expedition). Parked until the sizing verdict.
- The precise permutation action (vertices `Fin (N+1)`; how `r`/the corner constraint transform). Pen-and-paper to pin.
