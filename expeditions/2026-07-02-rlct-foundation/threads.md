# Threads — `rlct-foundation`

Per-rung formaliser/reviewer/scout threads (ladder in [`priorities.md`](priorities.md)). One
**builder/committer** per shared worktree at a time; read-only auditors alongside. Controller integrates +
re-gates per [`loop-prompt.md`](loop-prompt.md). Branch `expedition/rlct-foundation` (off `dev` `10edbdc3`).

## Status

| rung | seat | teammate | status | notes |
|------|------|----------|--------|-------|
| R1 | build (SWE/infra) | r1-cordon | ✅ **LANDED** `82c3b006` | citation cordon shipped + **controller-verified** (build 3836 green · gate `UNACCOUNTED=0 CITED=3 LOCATION=0` · sorries `0/3-axiom` · fixtures **13/13**). Parametric `@[cited]` + `collectAxioms` accounting (forget-proof) + `#audit_cited` + enforcing `scripts/cited` + 13 adversarial fixtures; retrofit `AoyagiCited.lean` (3 cites + proved interface + corner-0 payoff); `docs/policies/citation-cordon.md` graduated. Perf: `collectAxiomsBatch` (per-decl timed out >590 s → ~20 s). Aggregator-wired (controller). Codex-xhigh on design; **`rev-cordon` audit: SOUND** (batch = stdlib `collectAxioms`; fidelity OK; doc accurate) → hardened: fixture (f) opaque-hidden axiom + empty-source rejection + doc fix, **battle-test 17/17** (`f8b83751`). |
| R0 | scout | r0-recon | ✅ **DONE** (recon delivered) | **GREENFIELD**: `rlctAt`/`rlctAtOn`/`weightedThreshold` are NOT on `dev` (cross-worktree only; ROADMAP §4b claim stale). Strong BUILD base (Mellin+`cpow` pole, `meromorphicOrderAt` arith, multidim CoV + integrability transport, rpow dichotomy, JapaneseBracket, parametric holomorphy); monuments confirmed absent (cite); coord-change invariance BUILDABLE. Value-vs-pole first-slice flag → operator. Codex CLI down (env). |
| G1 | formaliser | g1-fibre-theta | ✅ **MERGED** (controller-reviewed) | non-monotone fibre `θ` — DROPPED `Monotone d`. Recon calibrated it: `Monotone` was a thin E0-`cTheta` layer, not a fibration problem. FLOOR: `ncard_topDimMinPrimes_fibre_eq_numTop` (+`_of_rank`) + E0-geometric rung `ncard_topDimMinPrimes_sigma_eq_numTop`. CLOSED FORM (perm-invariance was LANDED, no new cite): `ncard_topDimMinPrimes_fibre_eq_cTheta_dminus_sort` (+`_of_rank`) = `cTheta((d∘sort d)−r)` for arbitrary `d`. Two non-monotone witnesses `d=![1,2,1]` (shown in-file). Axiom-clean `[propext,Classical.choice,Quot.sound]`; Codex-reviewed (exit 0). Merged into `rlct-foundation`. |
| R2a | build (analysis) | r2a-threshold | ✅ **MERGED** `62641cc6` + gate extended | cite-free `RLCT.integrabilityThreshold` + `|·|→1` witness; axiom-clean (rev-r2a `#print axioms` ✓). Merged into rlct-foundation; Card-1 fidelity note added (threshold = classical RLCT iff `{K=0}` null — **holds for DLN**, fibre is null); **cordon gate extended to first-party allowlist `[DLNFibre, RLCT]`** — scans 2248 decls (incl. 19 RLCT), battle-test 17/17. The cite-free connecting interface under decision A. |
| R2b | build (analysis) | r2b-zeta + pp-zeta-cert | ✅ **hWorst + FIDELITY-AUDITED** (`5fd47dd9`) — refs corrected, cite honestly sourced; merge-ready (pending operator nod) | Zeta-pole object built: `Core/Analysis/RLCT/{Zeta,Cited,Pair}` — ζ + convergence (cite-free) + the ONE bundled continuation cite + `RLCTPair(λ=−s₀>0, poleOrder)` + Link 1 (`λ = R2a integrabilityThreshold`). Gates green (`scripts/cited` = `UNACCOUNTED=0 CITED=4 LOCATION=0`; `#print axioms` = std-3 + 1 cite; sorries 0). Codex (xhigh) fixed 3 gaps (locality/maximality/pole-regime). **Cite was UNSOUND** — `rev-r2b` + decorrelated Codex CONFIRMED (convergent counterexamples, `K=x²(x−2)⁶`, φ near 0) that `s₀ = −integrabilityThreshold K U` is not just over-strong but **inconsistent**: the unique continuation (pinned by conjunct (a) + identity theorem) is holomorphic at `s=−1/6` (order 0) while (e)+(f) demand a pole of order ≥1 there → the axiom yields `False`. Fix, TWO rounds: (1) r2b-zeta landed **open-U** `∀ x∈U` (`3c1e4392`) — but rev-r2b showed that's STILL unsound (a zero on `∂U` satisfies it vacuously yet blows up the `U`-integral from inside; `K=x²(x−1)⁸`, `U=(−1,1)`, threshold=1/8 vs `s₀=−1/2`); (2) controller re-directed **closure-U** `∀ x∈closure U` (definitive: `K>0` on the compact `closure U∖{x₀}` ⟹ obstruction local at x₀ ⟹ (f) holds). Keep RHS `integrabilityThreshold K U` (supp-φ swap also unfaithful, rev-r2b). Everything else (continuation, maximality, rationality, sign `λ=−s₀`, `poleOrder`, Link 1) confirmed sound. **THIRD issue (controller, on continued probing):** the single-zero fix makes the cite CONSISTENT but **inapplicable to the DLN germ** — `{K_B=0} = mult⁻¹(B)` is the positive-dimensional fibre, so `hUzero` (x₀ sole zero) fails for `K_B` ⟹ `rlct(K_B)` undefined via the cite ⟹ R7/R8 walls. Need a hyp that rules out a SEPARATE worse-threshold zero component but ADMITS the connected fibre. `pp-zeta-cert` re-adjudicated (cert §7, decorrelated Codex): root cause = **altitude confusion** (a REGIONAL conclusion forced into a LOCAL cite). Fix = **three-way split** (do not collapse): **Axiom A** local zeta pole `s₀=−rlct_{x₀}(K)`, NO regional/sole-zero hyp → DLN-admissible at any fibre point + consistent (local identity); **Bridge B** buildable regional lemma (`integrabilityThreshold K U = rlct_{x₀}` under boundary-regular U + worst-point inequality — admits the connected fibre); **Theorem C** the DLN payoff via the EXISTING global Watanabe/Aoyagi cites (`rlct_lossDLN_eq_half_codimRealFibre`), NOT Axiom A at K_B → sidesteps the paper's OPEN local conjecture (L1937). r2b-zeta re-stating (local `rlct_x` object + local Axiom A + local Link 1 + Bridge B). **Merge HELD** (was prematurely called merge-ready). Cite hardened across **4 rounds**: over-strength → inconsistency → open-U leak → DLN-inapplicability → local split; then round-5 `hWorst` (wide-φ). **Round-6 FIDELITY AUDIT** (`rev-fidelity` + Codex, operator-triggered by the "Def 4.1" catch): refs corrected (`Def 4.1` → `Def 8.1(ii)`, `defn:rlct` §8 L1787; `propdefn` → `Prop-Def 8.2` L1803-1808; "Def 4.1" is really the rank loci `Σ^r` §4), per-conjunct attribution split; **NO conjunct over-claims** — rationality of poles follows from real-analytic resolution ALONE (not Bernstein–Sato), largest-pole=−rlct honestly attributed to Saito/SLT. Cite fidelity-clean (`5fd47dd9`). |
| R3–R8 | — | — | ⛔ blocked on R2b | the RLCT foundation + payoff rewire. |

**Cordon coverage — DONE (`62641cc6`+):** the gate now scopes to a first-party namespace allowlist (`Config.nsPrefixes`, default `[DLNFibre, RLCT]`), so the bare-namespace `RLCT` modules ARE covered (2248 decls; `--ns` still overrides for the fixture harness, 17/17). Residual (low-risk, noted): the L7 Mathlib-mirror namespaces (`Matrix`/`Ideal`/… — first-party modules whose decls sit in a Mathlib-overlapping namespace) are NOT in the allowlist (a `Matrix` prefix would catch Mathlib too); they host no cites and are transitively covered via their `DLNFibre` consumers. A module-provenance scan (source-module under `DLNFibre/**`) is the fully-robust generalization if a cite ever needs to live in one.

## Concurrency rule (this expedition)
At most one builder/committer in `.claude/worktrees/rlct` at a time (a second `lake build` corrupts `.lake`;
two `git commit`s race the index). Read-only auditors may run alongside one builder. Rungs on the same file
serialize. R1 (tooling/`scripts`), R0 (read-only recon), and G1 (geometry, disjoint files) can be dispatched
concurrently *only* if they touch disjoint paths + one builder rule holds — otherwise serialize.

_Updated each tick._
