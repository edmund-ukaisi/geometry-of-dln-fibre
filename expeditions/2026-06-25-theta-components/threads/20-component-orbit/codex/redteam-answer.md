**Verdict: sound, for the reduced fibre ring.** I do not see a flaw in the 3-step argument. In fact the landed theorem proves slightly more than the name says: [isSmoothAt_sweepFibre_topComponent](</home/ubuntu/workspace/geometry-of-dln-fibre/.claude/worktrees/theta-components/lean/DLNFibre/Core/FibreComponentOrbitTransport.lean:172>) only assumes `I ∈ minimalPrimes`, not top-dimensionality.

1. **Step 2 is valid.** Over an algebraically closed field, Mathlib has `IsAlgClosed k → PerfectField k`; generic smoothness over a perfect field is exactly the theorem you want. The repo proof uses `dense_smoothLocus_of_perfectField` to get some smooth point of `Spec D`, then uses openness/basic opens in a domain to move to `⊥`: [FibreComponentOrbitTransport.lean:138](</home/ubuntu/workspace/geometry-of-dln-fibre/.claude/worktrees/theta-components/lean/DLNFibre/Core/FibreComponentOrbitTransport.lean:138>). No hidden geometrically-reduced hypothesis is being smuggled in. The perfect-field hypothesis is real; over an imperfect field, reduced finite-type domains can fail to be generically smooth due to inseparability.

2. **The C1 bridge is sound.** The key lemma proves that, in a reduced Noetherian ring, localizing at a prime `q` containing exactly one minimal prime `I` kills `I`: [LocalizationAtComponent.lean:46](</home/ubuntu/workspace/geometry-of-dln-fibre/.claude/worktrees/theta-components/lean/DLNFibre/Core/LocalizationAtComponent.lean:46>). It then builds the actual `k`-algebra equivalence
`R_q ≃ (R ⧸ I)_{q.map}`: [LocalizationAtComponent.lean:96](</home/ubuntu/workspace/geometry-of-dln-fibre/.claude/worktrees/theta-components/lean/DLNFibre/Core/LocalizationAtComponent.lean:96>). For `q = I`, uniqueness is exactly minimal-prime incomparability: [LocalizationAtComponent.lean:182](</home/ubuntu/workspace/geometry-of-dln-fibre/.claude/worktrees/theta-components/lean/DLNFibre/Core/LocalizationAtComponent.lean:182>).

3. **The key worry resolves in your favor.** At the generic point `I`, the ambient reducible scheme does not still “see” the other components. A point `I` lies on another component `V(J)` iff `J ≤ I`; for distinct minimal primes this cannot happen. The C1 lemma formalizes the stronger local-ring statement, so smoothness of `R` at `I` is transported from smoothness of `R ⧸ I` at `⊥`.

4. **Bottom line:** yes, generic smoothness of each irreducible component of the reduced DLN fibre is fully unconditional by this route. The orbit/chart machinery was unnecessary for this narrow statement: `Algebra.IsSmoothAt k I` at a component generic point. It is still relevant for stronger or different goals: labeling components by orbits, proving the orbit-times-affine component description, θ-count transport, explicit chart opens, dimension bookkeeping, or any statement about the nonreduced scheme-theoretic fibre rather than `sweepFibreRing = ... ⧸ vanishingIdeal(...)`.

So the clean result is not suspicious mathematically; it is the standard “reduced finite type over perfect field is generically smooth on every component” principle plus the correct reduced-localization bridge.