<task>
Lean 4 + Mathlib v4.29. I need the cleanest PROOF PATH (or an honest "not feasible at this pin")
for ONE lemma — the char-0 "algebraically independent family ⟹ Kähler differentials linearly
independent" criterion, specialised to give trdeg ≤ differential-span-rank.

CONTEXT. k a field, [CharZero k]. B a k-domain, f : ι → B finite (Fintype ι). K := FractionRing B,
a field, [Algebra k K] [IsScalarTower k B K]. Define
  genericDifferentialRank := finrank K (Submodule.span K (Set.range fun i ↦ D k K (algebraMap B K (f i))))
where D = KaehlerDifferential.D k K : Derivation k K Ω[K⁄k].

GOAL LEMMA:
  (Algebra.trdeg k (Algebra.adjoin k (Set.range f))).toNat ≤ genericDifferentialRank
i.e. trdeg of the subalgebra generated by f ≤ the K-rank of the span of {D(f_i)} in Ω[K⁄k].

I have CONFIRMED present + COMPILING at v4.29:
- KaehlerDifferential.D k K : Derivation k K Ω[K⁄k]; KaehlerDifferential.mapBaseChange R E K :
  K ⊗[E] Ω[E⁄k] →ₗ[K] Ω[K⁄k]; KaehlerDifferential.tensorKaehlerEquivOfFormallyEtale R E K (a LinearEquiv,
  given [FormallyEtale E K]); Algebra.FormallyEtale.of_isSeparable E K [Algebra.IsSeparable E K];
  Algebra.IsAlgebraic.isSeparable_of_perfectField (instance, [PerfectField k]); PerfectField.ofCharZero.
- I PROVED: mapBaseChange k E K is injective when [Algebra.IsSeparable E K] (étale equiv .injective).
- Algebra.trdeg, IsTranscendenceBasis, exists_isTranscendenceBasis, IsTranscendenceBasis.cardinalMk_eq_trdeg,
  trdeg_add_eq, MvPolynomial.trdeg_of_isDomain, trdeg_eq_zero, KaehlerDifferential.mvPolynomialBasis,
  Fintype.linearCombination / range_linearCombination.

THE MATHEMATICAL SUBTLETY YOU FLAGGED EARLIER (and I want resolved): K / k(selected f_i) is NOT
algebraic in general (the map is not generically finite). So I canNOT use "K/E algebraic + separable".
The differentials {D(f_i)} live in Ω[K⁄k] and I want: a maximal alg-indep subfamily of size r=trdeg has
K-linearly independent differentials.

QUESTIONS (be concrete about v4.29 API; flag any lemma name you are NOT sure exists):

1. Is there a Mathlib v4.29 lemma DIRECTLY linking AlgebraicIndependent (or IsTranscendenceBasis / a
   separating transcendence basis) to LinearIndependent of the family (fun i ↦ D k K (x i)) in Ω[K⁄k]?
   Search terms to consider: "KaehlerDifferential ... AlgebraicIndependent", "separatingTranscendenceBasis",
   "Derivation ... LinearIndependent", "Algebra.IsSeparable ... KaehlerDifferential rank",
   "trdeg ... finrank Ω". If such a lemma (even one-directional) exists, name it and its exact hypotheses.

2. If ABSENT, what is the cleanest PROOF? The classical argument: let {x_a} be a transcendence basis of
   K/k that is SEPARATING (exists in char 0 / over perfect k). Then Ω[K⁄k] is K-free with basis {D x_a},
   AND for any f, D f = Σ (∂f/∂x_a) D x_a. An alg-indep subfamily of f's, being part of *some* separating
   transcendence basis (char 0), has independent differentials. Does Mathlib have:
   (a) "separating transcendence basis exists over a perfect field" — name?
   (b) "Ω[K⁄k] is free with basis the D of a separating transcendence basis" — name?
   (c) any of the Etale/Field.lean or Perfect.lean infrastructure that yields finrank Ω[K⁄k] = trdeg k K
       for K f.g. over k? (Even if K is NOT f.g. over k here — K = Frac of a f.g. domain IS f.g. as a
       field extension of k. So finrank Ω[K⁄k] = trdeg k K SHOULD hold and may be the cleanest route:
       then trdeg(adjoin f) ≤ trdeg K = finrank Ω[K⁄k], and separately span{D f_i} relates to ... hmm,
       that gives trdeg K not trdeg(adjoin f). Reconsider.)

3. ALTERNATIVE cleaner route via mapBaseChange injectivity that AVOIDS separating bases: Let r = trdeg of
   adjoin_k(range f). Choose a maximal alg-indep subfamily g : Fin r → B (subfamily of f). Let
   E := the fraction field of k[g] inside K, i.e. E = k(g) ⊆ K — purely transcendental of trdeg r, so
   Ω[E⁄k] is E-free of rank r with basis {D(g_a)}. The map mapBaseChange k E K : K ⊗[E] Ω[E⁄k] → Ω[K⁄k]
   sends 1 ⊗ D_E(g_a) ↦ D_K(g_a). IF this map is INJECTIVE, then {D_K(g_a)} are K-linearly independent
   (image of a K-basis of K ⊗[E] Ω[E⁄k] under an injective K-linear map), giving r ≤ rank span{D f_i}.
   mapBaseChange injective needs [FormallyEtale E K] OR at least the H1Cotangent E K and Ω[K⁄E] vanishing.
   BUT K/E is NOT algebraic, so IsSeparable E K (which I used for the algebraic case) does NOT apply.
   QUESTION: for E ⊆ K with K = Frac(B), B a f.g. k-domain, E = k(transcendence-subbasis), is K
   formally étale / formally smooth / separable over E in the sense Mathlib needs for mapBaseChange
   injectivity? In char 0, K/E is separable (as a field extension, separability = the general
   field-ext notion, not algebraic-separable). Does Mathlib's Algebra.IsSeparable E K hold for a
   non-algebraic separable field extension, or is Algebra.IsSeparable RESTRICTED to algebraic? If
   IsSeparable is algebraic-only, what is the v4.29 typeclass for "K/E separable (possibly transcendental)"
   that feeds FormallySmooth/FormallyEtale and hence mapBaseChange injectivity? (e.g.
   Algebra.FormallySmooth, Algebra.IsSeparable vs a "separable field extension" predicate.)

4. VERDICT. Given v4.29's actual API, is the GOAL LEMMA provable in a few hundred lines, or should I
   land it as a clearly-scoped obligation (sorry-free skeleton with the lemma stated and an English note)
   and build everything else around it? If provable, give the 4-6 step proof skeleton with exact lemma
   names. If you recommend scoping it, say so explicitly and give the single cleanest statement to scope.
</task>

<output_contract>
Sections Q1..Q4. For Q1: the lemma name if it exists, else "absent, searched X". For Q3: a DEFINITE
answer on whether Algebra.IsSeparable is algebraic-only at v4.29 and what the right typeclass is for a
transcendental separable extension feeding mapBaseChange injectivity. End with a one-line VERDICT:
"PROVABLE via <route>" or "SCOPE IT" with the reason.
</output_contract>

<grounding_rules>
Flag every lemma/typeclass name you are not certain exists at v4.29 with "(verify)". Distinguish
"Mathlib has this" from "the math is true but Mathlib may lack it". The verdict (provable vs scope) is
what I am buying.
</grounding_rules>
