<task>
Lean 4 + Mathlib v4.29. Final blocker of a proof. I need ONE Submodule.map / range equality.

CONTEXT (all compiles; in a tactic proof with these in scope):
- `k : Field`, `σ : Fintype, DecidableEq`, `R := MvPolynomial σ k`, `g : Fin m → R`,
  `B := R ⧸ Ideal.span (Set.range g)`. `Algebra B k` (from a surjective aug : B →ₐ[k] k), towers set.
- `Ψ : k ⊗[B] (B ⊗[R] Ω[R⁄k]) ≃ₗ[k] (σ → k)` (iterated Basis.baseChange of mvPolynomialBasis; works).
- PROVEN action lemma:
  `Ψ_D : ∀ p : R, Ψ (1 ⊗ₜ[B] (1 ⊗ₜ[R] KaehlerDifferential.D k R p))
            = fun x : σ ↦ (aug) (algebraMap R B (pderiv x p))`.
- `jacobianTranspose g a : (Fin m → k) →ₗ[k] (σ → k)`,
  `jacobianTranspose_apply c x = ∑ i, eval a (pderiv x (g i)) • c i`. So
  `jacobianTranspose (Pi.single i 1) = fun x ↦ eval a (pderiv x (g i))`.
  Note `aug (algebraMap R B (pderiv x (g i))) = aeval a (pderiv x (g i)) = eval a (pderiv x (g i))`.
  So `Ψ (1 ⊗ₜ (1 ⊗ₜ D(g i))) = jacobianTranspose (Pi.single i 1)`.

GOAL (the remaining `sorry`):
  `(LinearMap.range ((LinearMap.lTensor k (LinearMap.ker (mapBaseChange k R B)).subtype).restrictScalars k)).map Ψ.toLinearMap
     = LinearMap.range (jacobianTranspose g a)`

where `mapBaseChange k R B : B ⊗[R] Ω[R⁄k] →ₗ[B] Ω[B⁄k]`, and
`K := LinearMap.ker (mapBaseChange k R B)` (a B-submodule of `B ⊗[R] Ω[R⁄k]`).

KEY FACTS available:
- `KaehlerDifferential.exact_kerCotangentToTensor_mapBaseChange k R B hsurj`:
  `range (kerCotangentToTensor k R B) = (ker (mapBaseChange k R B)).restrictScalars R` (as R-submodules)
  — so `K` equals the R-range of the R-linear `kerCotangentToTensor`.
- `kerCotangentToTensor_toCotangent x = 1 ⊗ₜ D _ _ x.1` (the conormal sends `toCotangent ⟨p,hp⟩ ↦ 1 ⊗ D p`).
- `I.Cotangent` is generated (R-module) by `toCotangent` of the ideal generators (`I = span (range g)`):
  `Ideal.toCotangent_surjective`, and `I` is spanned by `range g`.
- `LinearMap.lTensor_range : range (lTensor Q g) = range (lTensor Q (subtype (range g)))`.
- `LinearMap.range_eq_map`, `Submodule.map_span`, `Submodule.span` lemmas, `Pi.basisFun`.

PLAN I'm considering: both sides are the k-span of the m vectors `v_i := jacobianTranspose (Pi.single i 1)`.
- RHS: `range jacobianTranspose = span k (Set.range fun i ↦ jacobianTranspose (Pi.single i 1))`
  (since `Pi.single i 1` span `(Fin m → k)`, `Pi.basisFun`/`Submodule.map_span`).
- LHS: the map `(lTensor k K.subtype).restrictScalars k` has range = image of `k ⊗[B] K`. Want this mapped
  by Ψ to the same span. The trouble: `K = ker(mapBaseChange)` is a B-submodule; I want it B-spanned (or
  k-after-tensor-spanned) by `{1 ⊗ D(g i)}`. Via the exact sequence `K` is the R-RANGE of
  kerCotangentToTensor, and `I.Cotangent` is R-generated by `toCotangent ⟨g i⟩`, so `K` is R-spanned by
  `{1 ⊗ D(g i)}`. Tensoring `k ⊗[B] -` then mapping by Ψ should give `span k {v_i}`.
</task>

<output_contract>
Give the COMPLETE Lean tactic proof of the `hRange` goal (or the tightest skeleton with each step a named
lemma + the simp set). Be concrete about:
1. How to get `range ((lTensor k K.subtype).restrictScalars k) = span k (Set.range fun i ↦ 1 ⊗ₜ[B] (1 ⊗ₜ[R] D (g i)))`
   — handling the K-is-R-range-of-conormal-and-conormal-generated-by-g_i step, and the lTensor span.
   (Is it cleaner to first show `range (lTensor k K.subtype) = range (lTensor k (kerCotangentToTensor ∘ something))`,
   or to span K by `{1⊗D(g i)}` and use `Submodule.map_span` + a `lTensor`-of-span lemma?)
2. The RHS span via `Pi.basisFun`.
3. The final `simp [Ψ_D, ...]` matching the two spans, using the `aug (algebraMap ..) = eval a ..` step.

Name every lemma; tag uncertain ones "verify name". I need a proof that ELABORATES, so prefer robust
idioms (`Submodule.map_span`, `LinearMap.range_eq_map`, `Submodule.span_le`, `le_antisymm`) over slick
one-liners.
</output_contract>

<grounding_rules>
- The map in the goal is `lTensor k K.subtype` (B-linear, then restrictScalars k), NOT lTensor of the
  R-linear conormal — be careful about the tensor base ring (B not R).
- If the cleanest path needs an auxiliary `have` that `K` (as a k⊗-relevant submodule) is spanned by the
  generators, state that auxiliary precisely.
</grounding_rules>
