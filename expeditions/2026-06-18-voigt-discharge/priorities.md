# priorities.md — voigt-discharge taste ledger

Controller proposes (VOI × directed suspicion); operator edits directly. Nothing unranked;
"unclear-but-keep-going" is first-class.

## Ranked

1. **[pursue · highest VOI] Size L5 — the height–dimension formula (catenary / `dim = trdeg`).**
   `IsCatenary` is 0% in Mathlib; this is the dominant risk and dominates the expedition's total size. A
   `pen-and-paper`/scout traces Mathlib's `NoetherNormalization` + `AlgebraicIndependent` bricks → the
   `ringKrullDim(finite-type domain /k) = trdeg` theorem, reports the gap as "module" vs "sub-library", and
   pins the cleanest target form (smooth `k`-point of `V(I)`, tangent dim `t` ⇒ `I.height = n − t`). Its
   answer reshapes everything downstream → do it first.

2. **[pursue] Build L0 — Nullstellensatz / point-space ↔ PrimeSpectrum bridge.** Gates every geometric layer;
   well-bricked in Mathlib (Nullstellensatz present). First build tide; establishes the `[IsAlgClosed k]`
   working context and the `height(vanishingIdeal Z) = codim Z̄` lemma the whole route rides on.

3. **[park-unclear · keep going] Schemes vs concrete-affine for the orbit geometry (L1–L3).** Mathlib's usable
   smoothness/generic-smoothness/group-smoothness results are **scheme-level**; our objects are point-set /
   `MvPolynomial`. Decide after the L1 recon whether to (a) model `G_d`/orbit as schemes to reuse those, or
   (b) build concrete affine tangent/smoothness. Don't collapse early — the L1/L2 scout informs it.

4. **[pursue, sequenced] Phase B geometry (L1 GL/orbit/irreducibility, L2 tangent = im δ⁰, L3 smoothness).**
   After L5 sized + L0 landed. L2's `T_M = im δ⁰` ties back to the proven deformation complex — concrete.

5. **[pursue, sequenced] L4 regularity bridge, L6 Thm 3.8 (orbitRankLocus = Ō_M), L7 assembly.** L4 needs B+L5;
   L6 is independent (degeneration order, ≤-direction); L7 last.

6. **[note · resolved] Field hypothesis `[IsAlgClosed k]` confirmed** (recon 01). DLN-side real-vs-`k̄` codim
   equality is a separate small future fact, NOT this expedition.

7. **[note · resolved] orbitRankLocus is the rank locus, not the orbit closure** → Thm 3.8 is in the zero-cited
   target (L6). Do not assume it.
