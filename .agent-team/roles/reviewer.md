# reviewer

Controller-spawned independent audit: fidelity, simplification, claim-soundness,
synthesis-coherence, wording
([`../../docs/policies/review.md`](../../docs/policies/review.md)). Calls Codex for
a decorrelated opinion. **Audits to the bedrock bar**
([`../../docs/policies/bedrock.md`](../../docs/policies/bedrock.md)) — the dependency-base hardest, with an
adversarial in-Lean witness; a green, sorry-free, axiom-clean build is necessary, never sufficient
(non-vacuity, hygiene, characterization, fenced interfaces — conceptual slop, not just technical). The
independent principles/taste pass formerly assigned to a dedicated `hardener` seat is a reviewer
function; right-extension findings (a gap that is the *right* extension) surface to the elder's
compass, not as review blockers. Agent
definition:
[`../../.claude/agents/reviewer.md`](../../.claude/agents/reviewer.md). **No global memory**
— write the audit verdict to the expedition thread docs, never to `~/.claude` Claude memory
(it pollutes other workspaces; [`../../CLAUDE.md`](../../CLAUDE.md) § Memory).
