# Lean / Lake build artifacts
lean/.lake/
**/*.olean
**/*.ilean
**/*.trace
lean/build/

# Python tooling cruft (scripts/)
**/__pycache__/
*.pyc

# Editor / OS cruft
.DS_Store
*.swp
*~

# Agent Teams runtime: per-teammate git worktrees live here (local working state, not repo content).
.claude/worktrees/

# Agent Teams runtime state also lives outside the repo (~/.claude/teams, ~/.claude/tasks).
theory/aoyagi-2023-reproduction/pages/

# Codex consult scratch (transient; load-bearing consults are homed in threads/NN/codex/).
**/.codex-consult/
